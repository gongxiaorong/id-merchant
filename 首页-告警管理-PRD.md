# 告警管理 PRD（产品版）

> 本文从现有商户端 App 能力与已确认产品决策**倒推**整理，面向产品、业务、设计、研发对齐「为什么做 / 做什么 / 做到什么算完成」。  
> 交互与视觉以当前线框原型 `index.html` 为参照，本文不写接口字段与技术实现细节。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 告警管理（车侧设备异常） |
| 文档类型 | 产品需求说明（由现状与已确认决策倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（首页最近告警 · 告警中心 · 告警详情） |
| 状态 | 需求已对齐，可进入研发评审 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

商户日常要盯车：哪台车掉线了、GPS 丢了、是不是被断电。App 里已经有：

- 首页「最近告警」
- 告警中心列表（VIN、类型、时间）
- 四类异常：**车辆离线 / GPS 信号丢失 / GPS 定位过时 / 疑似断电**

但运维同学的真实工作不是「看一眼列表」，而是：

1. **发现** —— 打开 App 就知道有几台车还在异常  
2. **判断** —— 是不是还在坏、多久了、在哪、有没有在租订单  
3. **介入** —— 认领、找车、寻车、必要时忽略  
4. **了结** —— 车自己恢复了，或人确认处理完了  

现状停在第 1 步的「列表展示」，后面三步断了。

### 2.2 核心痛点（倒推）

| # | 痛点 | 业务影响 |
|---|---|---|
| 1 | **只有列表，没有闭环** | 看了等于没看，待处理永远清不掉，也无法区分「有人在跟」 |
| 2 | **设备会自己好，系统却当工单** | 四类告警都可自动恢复；人结案和车自愈混在一起，复盘说不清 |
| 3 | **同一辆车同类异常反复刷** | 列表被刷屏，真正要处理的被淹没 |
| 4 | **告警和消息搅在一起** | 系统通知里混着「车辆离线告警」，和告警中心双通道，术语乱 |
| 5 | **发现到处置路径不顺** | 首页进详情后回跳错、时间含义不清、找车入口被长内容顶到底部 |

### 2.3 产品机会

在**不改底部导航、不碰权限/角色、不动订单主流程**的前提下，把「最近告警 → 告警中心 → 详情」补成一条完整的**车侧异常处置链路**，让商户运维在手机上完成「发现 → 认领 → 车控 → 了结」。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **可管** | 每条告警有清晰状态，能认领、忽略、人工结案 |
| **自愈友好** | 设备恢复后自动关闭，并标明「自动恢复」还是「人工处理」 |
| **不刷屏** | 同一辆车、同一类异常在未了结前只维护一条，刷新最近时间 |
| **路径短** | 首页一眼看到要处理的；详情里立刻能跳车辆、寻车 |
| **职责清** | 告警中心只管车侧设备异常；消息中心只管系统/运营通知 |

### 3.2 成功怎么量（产品口径）

上线后可用下列指标验证（不要求本期做报表，但需求验收应对齐）：

- 打开 App 后，**仍异常**的告警能在首页/待处理队列中被发现  
- 运维能明确区分：**还没人碰 / 有人在跟 / 先不跟 / 已经了结**  
- 了结时能区分：**车自己好了** vs **人确认处理完了**  
- 同类未了结重复上报时，列表条数不爆炸  
- 从告警到「打开这台车 / 寻车」的路径 ≤ 2 次关键点击（进详情后即可见）

### 3.3 本期不做（明确边界）

- 告警规则后台配置、分级阈值、静默时段管理后台  
- 批量忽略 / 批量已读  
- 近 7 日类型统计大盘  
- 派单到具体员工、SLA、多租户权限改造  
- 订单逾期等**业务异常**并入告警中心（继续走订单）  
- 车辆详情里的「历史告警库」（可二期）

---

## 4. 用户与使用场景

### 4.1 主用户

**商户侧运营 / 运维**（门店或中心化车队管理员）

- 设备：手机 App  
- 动机：少漏车、少跑空、异常可交代  
- 能力预期：会点列表和车控，不配置复杂规则

### 4.2 关键场景

**场景 A｜早班扫一眼**  
开门店打开 App → 首页「最近告警」看还有没有待处理/处理中 → 点一条进详情 → 看位置和是否在租 → 决定开始处理或先忽略。

**场景 B｜列表清队列**  
进告警中心 → 默认看「待处理」→ 按类型筛「疑似断电」→ 逐条认领 → 寻车或去车辆详情 → 处理完点「标记已处理」。

**场景 C｜车自己恢复了**  
运维还没来得及处理，车辆重新上线 / GPS 恢复 → 告警自动变成「已关闭 · 自动恢复」→ 不再占待处理红点 → 需要时可在「已关闭」里复盘时间轴。

**场景 D｜误报或已知维护**  
确认是误报或正在保养 → 忽略并选原因 → 列表进「已忽略」；若之后设备真恢复，仍应自动关闭，避免「车好了还挂着已忽略」。

**场景 E｜同一台车反复掉线**  
未关闭前多次上报 → 仍是一条告警，只更新「最近触发」、次数 +1 → 生命周期里能看到每一次触发节点，而不是十条重复卡片。

**场景 F｜从运营数字钻到车**  
首页「车辆运营」点「离线车辆」→ 进入车辆列表并自动筛出离线车（与告警模块配合的运营入口，本期已对齐）。

---

## 5. 范围定义

### 5.1 告警类型（在范围内）

| 类型 | 业务含义（产品表述） | 默认关注度 |
|---|---|---|
| 车辆离线 | 车端长时间无法联通 | 高 |
| 疑似断电 | 疑似供电异常 | 高 |
| GPS 信号丢失 | 定位信号丢失 | 中 |
| GPS 定位过时 | 定位长时间未更新 | 中 |

四类均可能**自动恢复**（条件解除即关闭），产品设计必须以「自愈」为一等公民，而不是纯人工工单。

### 5.2 不进告警中心

| 内容 | 应去哪里 |
|---|---|
| 订单逾期、租还异常 | 订单流程 / 订单列表 |
| 系统维护、版本更新、运营公告 | 消息中心 |
| 车侧 IoT 异常 | **仅**告警中心（消息中心不再重复「车辆离线告警」类条目） |

### 5.3 模块边界

| 模块 | 本期关系 |
|---|---|
| 首页 | 展示仍异常的最近告警；进详情；进告警中心 |
| 告警中心 | 状态/类型筛选、列表、进详情 |
| 告警详情 | 摘要、车控、生命周期、处置 |
| 车辆 | 从详情「查看车辆」「一键寻车」；首页在线/离线跳转列表筛选 |
| 订单 | 详情可展示关联订单；逾期不进告警 |
| 消息中心 | 去掉与告警重复的车侧告警文案 |

---

## 6. 核心产品概念

### 6.1 一条告警是什么

**一条告警 = 某一辆车（VIN）+ 某一类异常，在一次「打开周期」内的处置对象。**

- **打开周期**：从首次异常产生，到被关闭（自动或人工）为止。  
- 周期内设备多次再报同类异常 → **还是这一条**，只刷新最近触发与次数。  
- 关闭后再出现同类异常 → **新的一条**，与历史结案分开。

### 6.2 用户看到的状态（4 档 + 关闭来源）

| 主状态 | 用户理解 | 谁推动进入 |
|---|---|---|
| **待处理** | 还没人接手 | 系统产生 / 从忽略重新打开 |
| **处理中** | 有人认领了，正在跟 | 用户点「开始处理」 |
| **已忽略** | 当前先不跟（有原因） | 用户点「忽略」 |
| **已关闭** | 这件事了结了 | 设备自动恢复 **或** 人标记已处理 |

**已关闭必须带来源**（列表副文案 / 详情字段，**不单独占 Tab**）：

| 关闭来源 | 含义 |
|---|---|
| **自动恢复** | 设备条件已解除，系统关闭 |
| **人工处理** | 运维确认处理完成，手动关闭 |

> 产品表述统一用「已关闭」，再用「自动恢复 / 人工处理」区分来源；不再拆成两个并列终态 Tab。

### 6.3 时间相关概念（给用户看的）

| 展示名 | 含义 |
|---|---|
| **最近触发** | 本周期内最后一次上报该异常的时间（列表主时间、首页同文案） |
| **首次触发** | 本周期第一条异常的时间 |
| **持续时长** | 从首次到「现在」或到「关闭」的时长；**只展示时长**，不附加「已结束」等说明 |
| **关闭时间** | 变为已关闭的时刻 |
| **触发次数** | 本周期内累计触发次数 |

---

## 7. 业务规则（产品规则，非技术）

### 7.1 状态怎么走

```
产生异常 → 待处理
  ├─ 用户「开始处理」 → 处理中
  ├─ 用户「忽略」     → 已忽略
  └─ 设备条件解除    → 已关闭 · 自动恢复   （任意未关闭状态均可）

处理中 → 用户「标记已处理」 → 已关闭 · 人工处理
已忽略 → 用户「重新打开」   → 待处理
已忽略 → 设备条件解除      → 已关闭 · 自动恢复（可保留原忽略原因作备注）

已关闭 → 只读，默认不能「重新打开」
已关闭后同类再异常 → 新开一条待处理
```

### 7.2 必须遵守的交互原则

1. **打开详情 ≠ 开始处理**  
   只看一眼不能把「待处理」清掉。进入「处理中」只能靠显式 **「开始处理」**。

2. **自动恢复优先于人工静音**  
   已忽略的告警，只要设备恢复，仍应变为「已关闭 · 自动恢复」，避免脏数据。

3. **已关闭页面无处置按钮**  
   不展示「仅可查看」类占位文案；需要时只能看摘要、生命周期、去车辆等只读/跳转能力（与当前原型一致：closed 无底栏操作）。

4. **仅「已忽略」可重新打开为待处理**  
   已关闭不重开同一条。

### 7.3 聚合规则（防刷屏）

| 条件 | 行为 |
|---|---|
| 同一 VIN + 同一类型，且存在**未关闭**告警（待处理 / 处理中 / 已忽略） | **不新建**；只更新最近触发、次数 +1；**状态不变** |
| 不存在未关闭（含已关闭） | **新开**一条待处理 |

说明：

- 「已忽略」也算未关闭：忽略后同键再报，不新开单，避免一边忽略一边刷屏。  
- 列表默认按 **最近触发** 从新到旧。

### 7.4 生命周期（整条周期的时间轴）

详情用 **「生命周期」** 记录本周期内发生过的事，而不只记触发：

| 节点类型（用户可见） | 何时出现 |
|---|---|
| 告警产生 | 本周期首次打开 |
| 第 N 次触发 | 未关闭时再次上报 |
| 开始处理 | 用户认领 |
| 已忽略 · 原因 | 用户忽略 |
| 重新打开为待处理 | 从忽略重开 |
| 已关闭 · 自动恢复 / 人工处理 | 结案 |

展示原则：

- **最新在上**（和「最近触发」心智一致）  
- 次数很多时，**默认只展示最近 3 条**，可「展开全部 / 收起」，避免详情过长  
- 车控类动作（查看车辆、寻车）**本期不进时间轴**，减少噪音  

### 7.5 详情信息优先级

高频触发时时间轴会变长，产品上把信息分成两层：

| 层级 | 内容 | 目的 |
|---|---|---|
| **先处置** | 摘要（类型、状态、VIN、位置、订单等）+ **查看车辆 / 一键寻车** | 进详情立刻能行动 |
| **再复盘** | 生命周期（默认折叠） | 查过程，不挡主路径 |
| **底栏** | 按状态：开始处理 / 忽略 / 标记已处理 / 重新打开 | 改状态 |

---

## 8. 功能需求（按用户旅程）

### 8.1 首页 · 最近告警

| 需求 | 说明 |
|---|---|
| 只展示仍异常 | **待处理 + 处理中**，按最近触发取 Top 若干（原型 Top5） |
| 时间文案 | 展示为 **「最近触发」**，不是笼统的「告警时间」 |
| 红点 / 数量 | 与「待处理 + 处理中」口径一致，提示未闭环量 |
| 点条目 | 直接进入该条 **告警详情** |
| 查看全部 | 进入 **告警中心**，默认落在「待处理」 |
| 返回 | 从首页进详情后返回，应 **回到首页**，而不是误进列表 |

### 8.2 告警中心

| 需求 | 说明 |
|---|---|
| 状态 Tab | 全部 / 待处理 / 处理中 / 已忽略 / **已关闭**（可显示各状态数量） |
| 类型筛选 | 全部类型 + 四类异常 |
| 列表信息 | VIN、类型、最近触发、状态角标、触发次数；已关闭显示副文案「自动恢复 / 人工处理」 |
| 排序 | 最近触发倒序 |
| 空态 | 区分「真的没有」与「当前筛选无结果」 |
| 点条目 | 进入详情；返回回到 **告警中心列表** |

### 8.3 告警详情

| 需求 | 说明 |
|---|---|
| 摘要 | 类型、状态、等级、VIN、首次/最近触发、持续时长、位置、关联订单；已关闭时含关闭时间、关闭方式 |
| 快捷入口 | **查看车辆**、**一键寻车** 紧挨摘要下方（固定靠上） |
| 生命周期 | 见 7.4；默认 3 条可展开 |
| 待处理底栏 | 忽略 ｜ **开始处理** |
| 处理中底栏 | 忽略 ｜ **标记已处理** |
| 已忽略 | **重新打开为待处理**（及产品已定的忽略相关能力） |
| 已关闭 | **无操作底栏**，无「仅可查看」提示文案 |
| 忽略弹窗 | 必选原因；主按钮文案统一为 **「确认」**（与全 App 弹窗一致） |
| 返回动效 | 与项目其它二级页一致（叠层滑出，不与列表页切换打架） |

### 8.4 与车辆的联动

| 需求 | 说明 |
|---|---|
| 查看车辆 | 跳到对应 VIN 的车辆详情 |
| 一键寻车 | 复用现有寻车/闪烁鸣笛能力，结果用 Toast 等轻反馈 |
| 首页车辆运营 | 「在线车辆 / 离线车辆」可点 → 车辆列表并带上对应在线筛选，清空其它车辆筛选，便于从运营概览钻取 |

### 8.5 消息中心（配套）

- 不再展示与告警中心重复的「车辆离线告警」类条目。  
- 系统维护、版本更新、运营公告保留。

---

## 9. 信息架构（用户路径）

```
首页
 ├─ 最近告警（待处理/处理中）──点击──► 告警详情 ──返回──► 首页
 ├─ 查看全部 ─────────────────────► 告警中心 ──点击──► 告警详情 ──返回──► 告警中心
 ├─ 车辆运营 · 在线/离线 ─────────► 车辆列表（带筛选）
 └─ …

告警详情
 ├─ 查看车辆 ──► 车辆详情
 ├─ 一键寻车 ──► 车控反馈
 └─ 底栏处置（按状态）
```

---

## 10. 文案与体验约定（产品层）

| 点 | 约定 |
|---|---|
| 列表主时间 | 「最近触发」 |
| 终态 | 「已关闭」+「自动恢复 / 人工处理」 |
| 认领 | 「开始处理」（唯一 pending→处理中 入口） |
| 人工结案 | 「标记已处理」 |
| 持续时长 | 只显示时长数字/文案，不加「（已结束）」 |
| 弹窗主按钮 | 全 App 统一「确认」 |
| 演示能力 | 「模拟设备恢复」等 **不得** 出现在商户 App 界面内；仅设计/评审原型可用外置操作区 |

---

## 11. 验收标准（产品验收清单）

### 闭环与状态

- [ ] 待处理只能通过「开始处理」变为处理中；仅打开详情不变状态  
- [ ] 处理中可通过「标记已处理」关闭，来源为人工处理  
- [ ] 任意未关闭状态在设备恢复后变为已关闭 · 自动恢复  
- [ ] 已忽略可重新打开为待处理；已关闭不可重新打开同一条  
- [ ] 已关闭详情无操作底栏、无多余「仅可查看」文案  

### 聚合与时间

- [ ] 同车同类型未关闭时再触发：列表仍一条，最近触发更新，次数增加，状态不变  
- [ ] 已关闭后再触发：新开待处理  
- [ ] 首页与列表时间文案为「最近触发」  
- [ ] 持续时长仅展示时长  

### 路径与信息

- [ ] 首页只展示待处理/处理中；红点口径一致  
- [ ] 首页进详情返回首页；告警中心进详情返回列表  
- [ ] 详情中查看车辆、一键寻车位于摘要下方（不被长轴顶到底）  
- [ ] 生命周期默认 3 条，可展开全部；含产生/触发/认领/忽略/关闭等节点  

### 边界

- [ ] 告警中心仅四类车侧异常；订单逾期不进告警  
- [ ] 消息中心无重复车侧告警条目  
- [ ] 首页在线/离线数字可跳转车辆列表并带筛选  
- [ ] 弹窗主按钮为「确认」；返回动效与其它二级页一致  

---

## 12. 版本规划建议

| 阶段 | 内容 | 价值 |
|---|---|---|
| **本期（MVP）** | 状态闭环、已关闭+来源、聚合、详情处置、生命周期、首页联动、消息拆分、车控入口 | 能管起来、自愈不脏、不刷屏 |
| **二期** | 通知偏好（类型×推送/站内、静默时段）、批量处置、首页类型摘要 | 少打扰、提效率 |
| **三期** | 车辆详情「当前/历史告警」、规则与阈值配置、简单 SLA | 资产侧复盘与策略运营 |

---

## 13. 待决问题（需产品拍板）

| # | 问题 | 当前默认（原型/会话） | 建议 |
|---|---|---|---|
| 1 | 忽略后是否设「静默窗」，窗外再触发是否升级提醒？ | 未关闭则持续聚合，不新开 | 先保持默认，二期再谈静默策略 |
| 2 | 是否允许待处理直接「标记已处理」，跳过开始处理？ | **否**，必须先认领 | 保持：认领有审计意义 |
| 3 | 详情关联订单是否本期可点进订单详情？ | 可展示，跳转未强制 | 有订单号则建议可跳 |
| 4 | 自动恢复的「关闭时间」以谁为准？ | 产品要求有关闭时间 | 以设备/云端恢复时间为准，App 展示即可 |
| 5 | 高等级（断电/长离线）是否强制更强推送？ | 等级有区分，推送策略未做 | 二期与通知偏好一起定 |

---

## 14. 附录：已确认决策一览（倒推追溯）

| 议题 | 产品结论 |
|---|---|
| 待处理 → 处理中 | 方式 A：详情「开始处理」显式认领 |
| 终态模型 | 统一「已关闭」，用来源区分自动恢复 / 人工处理 |
| 重复上报 | 同 VIN + 类型，未关闭只更新最近触发与次数 |
| 列表/首页时间 | 绑定最近触发；首页文案「最近触发」 |
| 详情过程信息 | 整条生命周期时间轴，非仅触发列表 |
| 详情过长 | 车控上移 + 时间轴默认 3 条 |
| 已关闭 UI | 无底栏操作、无「仅可查看」提示 |
| 与消息 | 告警与公告拆分，去掉重复告警消息 |
| 演示 | 不进商户 App 主体 |

---

## 15. Next step

1. **产品 / 业务**：确认第 13 节待决项；无异议则冻结本 PRD 为研发输入。  
2. **设计**：以现有原型为基线，补空态、无网、推送到达刷新等状态（若研发排期需要）。  
3. **研发**：按本文业务规则实现云端聚合与自动恢复 + App 状态与路径；生产环境去掉一切演示入口。  
4. **测试**：以第 11 节验收清单为主用例，重点覆盖「只看详情不清待处理」「忽略后仍可自动关闭」「聚合不刷屏」。

---

*文档结束。如需拆研发任务或改原型，请基于本 PRD 增量，勿回退第 14 节已冻结决策。*
