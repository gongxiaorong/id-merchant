# 双端统一设计体系计划（电动车租赁 · 商户端 + 用户端）

> 版本：V1.0 计划稿 · 状态：待评审 · 来源：`index.html`（商户管理端高保真原型）实测绘取

## 一、意图摘要

基于商户端原型中**已经跑通的技术栈与 UI 交互**，沉淀一套双端共享的设计体系（Design System）：统一色彩 / 字体 / 间距等 Token，收敛组件与状态规范，把六轮对齐沉淀下来的加载态、转场、面板渲染约定固化为可核对的条款，作为后续双端页面生成、走查、验收的单一事实来源。

**范围界定**
- 本工作区现有 `index.html` 为**商户管理端**（登录、首页经营数据、告警中心、车辆、订单、我的）；
- **用户端**为姊妹项目，其源文件不在当前工作区；本计划先以双方已对齐的约定覆盖，缺口见「开放问题」。

## 二、事实来源（Source Assets）

| 来源 | 内容 | 在本体系中的角色 |
|---|---|---|
| `index.html`（427KB） | 商户端高保真原型：Vue 3 + UnoCSS runtime + lucide 图标 | 视觉 Token、组件形态、动效参数的唯一提取源 |
| `locales/{zh-CN,zh-HK,en,id}.json` | 四语言文案，`模块.键名` 命名 + `{n}` 插值 | i18n 规范与领域词表来源 |
| `_recon_report.txt` | 文案盘点：47 条遗漏候选（MISS）、8 条死键候选（DEAD） | i18n 治理待办 |
| 已验证规则 ×3 | demo-ops-per-page / loading-mutual-exclusion / loading-and-transition-conventions | 直接固化为体系条款，不再重新讨论 |

## 三、技术栈基线（双端一致）

| 项 | 现状 | 体系决策 |
|---|---|---|
| 框架 | Vue 3（CDN 全局构建，`createApp`） | 保持单文件 HTML + Vue 3 全局构建的原型交付方式 |
| 样式 | UnoCSS runtime + 少量内联 `<style>` 变量 | 新增样式一律走 `:root` CSS 变量 + UnoCSS 原子类；禁止散落硬编码 hex |
| 图标 | lucide 0.344 UMD | 图标仅用 lucide 线性风格，禁止混用 emoji 当功能图标 |
| 视口 | 移动端 H5：`width=device-width, initial-scale=1, user-scalable=no` | 按 375–430px 移动画布设计，禁止出现横向滚动 |
| 持久化 | localStorage | Tab 位置、语言偏好等 UI 态持久化 |
| i18n | 四语言 JSON 运行时切换 | 所有用户可见文案必须走语言包键，禁止写死 |

## 四、色彩 Token（实测自原型，非猜测）

### 4.1 核心 Token（`:root` 现值即标准值）

| Token | 现值 | 角色 |
|---|---|---|
| `--bg` | `#F6F7F9` | 页面底色 |
| `--surface` | `#FFFFFF` | 卡片 / 弹层面板 |
| `--fg` | `#111111` | 主文本 |
| `--muted` | `#999999` | 次要文本 |
| `--border` | `#E5E5E5` | 分隔线 / 描边 |
| `--accent` | `#2B3F5F` | 商户端主色（深海军蓝），浅底 `#EBF1FD` |
| `--accent-2` | `#FF6A00` | 强调橙，**每屏 ≤2 次**（主 CTA / 关键提醒） |
| `--link` | `#2F6FEB` | 链接 / 可点文本 |
| `--success` | `#17A34A`（底 `#ECFDF3`） | 成功 / 在线 |
| `--danger` | `#DC2626`（底 `#FEF2F2`） | 危险 / 告警高频色 |

### 4.2 待收敛项（提取发现的缺口）

- [ ] **Warning 色未收敛**：`#D97706`、`#F59E0B`、`#E67E22` 三种琥珀色并存 → 收敛为一个 `--warning` + 一个 tint 底（候选 `#D97706` / `#FFF3E0`）
- [ ] **muted 缺中间档**：正文次级大量使用 `#6B6B6B`，与 `--muted #999999` 形成两档但未命名 → 增加 `--fg-secondary`
- [ ] **tint 家族命名化**：`rgba(43,63,95,.06~.4)` 海军蓝透明阶、`rgba(0,0,0,.04~.85)` 黑透明阶 → 定义 `--accent-a6/a12/a35`、`--black-a4/a6/a45` 遮罩与按压态专用档
- [ ] 派生色一律用 `oklch()` 计算，禁止再引入新裸 hex

## 五、字体与排版（实测）

- 字族：`-apple-system, BlinkMacSystemFont, 'SF Pro Text'/'SF Pro Display', 'PingFang SC', system-ui, sans-serif`（数据密集工具型产品，display/body 同族成立；Display 仅在字重与字号上区分）
- 字号阶梯（现用值 → 归一化语义档）：

| 语义档 | px | 用途 |
|---|---|---|
| `title-lg` | 20 | 页面大标题 / 经营数据关键数值 |
| `title` | 17 | 导航栏标题、区块标题 |
| `body-emph` | 14 | 正文强调、列表主信息 |
| `body` | 13 | 默认正文 |
| `caption` | 12 | 辅助说明、表单标签 |
| `micro` | 10–11 | 徽标、角标、时间戳 |

- [ ] 数字场景（金额、电量、经纬度）启用 `font-variant-numeric: tabular-nums`

## 六、形状 / 间距 / 层级（实测）

| 类别 | 阶梯 | 用途映射 |
|---|---|---|
| 圆角 | 4 / 8 / 12 / 16 / 20 / 9999(pill) / 50% | 4 输入框与小控件 · 8 卡内嵌块 · 12 卡片与主按钮 · 16 大容器 · 20 弹层顶部 · pill 状态徽标 |
| 阴影/按压 | `rgba(0,0,0,.04/.06/.08)` 常规 · `.12/.14` 浮起 · `.45/.85` 遮罩 | 只允许这三组黑透明阶 + accent 透明阶 |
| 安全区 | `env(safe-area-inset-bottom)` 已接入 | 所有底部固定元素（TabBar、弹层、CTA）必须让出 |
| z-index | 需固化：内容 < 吸顶 < TabBar < mask < sheet < toast < loading | [ ] 补一节具体数值表 |

## 七、组件覆盖清单

**双端共享组件**（形态与状态完全一致）：
- 导航：底部 TabBar、页顶返回栏
- 按钮：primary（navy）/ accent-2 CTA / ghost / danger 文字钮；触控目标 ≥44px
- 表单：手机/邮箱/账号输入、验证码行、只读行、搜索筛选 pill 行（参照 `orderPillRow`）
- 列表卡片：车辆卡（电量主/副、在线离线态）、订单卡、告警卡（等级徽标 + 时间线 timeline）
- 弹层：底部 sheet（`pkg-picker-sheet`、`orders-date-sheet`）、居中 confirm dialog（设防/报失类二次确认）、toast（轻提示，85 处调用）
- 加载三件套：骨架屏（shimmer）、下拉刷新（`ptr-*` 各页独立容器）、全局居中 loading（yadi-loading）
- 地图套件：定位图（用户点 + 车 pin + 侧边操作 `side-actions` + 导航）、轨迹回放（时间轴 bar + 选点 sheet）、点聚合开关
- 空状态：图标 + 主句 + 副句（参照 `home.noAlerts / noAlertsSub` 双句式）

**商户端特有**：经营数据统计卡、告警处置流（开始处理 → 时间线 → 关闭/忽略）、退款记录、改密流程（verify → reset）、员工/门店管理入口。

**用户端特有**：扫码用车、换电柜导航、套餐购买、个人钱包（待用户端源确认后补全）。

## 八、加载态与转场规范（已验证规则，直接固化）

### 8.1 三类 Loading 互斥矩阵（loading-mutual-exclusion）

| 场景 | 唯一允许的反馈 | 禁止叠加 |
|---|---|---|
| 首屏进入 | 骨架屏 | ✗ 不得同时出现顶部刷新圈 / 居中 loading |
| 下拉刷新 | 顶部刷新指示器（ptr-spin） | ✗ 不得触发骨架屏或居中 loading |
| 页内动作模拟接口 | 全局遮罩 yadi-loading | ✗ 骨架屏保持熄灭，下拉回调不并发 |

### 8.2 转场时长与曲线（实测统一）

| 场景 | 参数 |
|---|---|
| hover / 按压 / 色彩微交互 | `.15s` ease |
| 弹层显隐 opacity | `.2s ease` |
| sheet 位移 | `.28s cubic-bezier(.32,.72,0,1)`（iOS 手感基准曲线） |
| 双向动画（mask + sheet） | mask 与 sheet 各包独立 transition，进出对称 |

### 8.3 demo-ops 面板（demo-ops-per-page）

- 原型操作面板按**当前页**条件独立渲染（`v-if` 以页为单位，共 93 处引用）；
- **次页面不继承上级页面的操作区块**；
- 新增页面必须同步声明该页自己的 demo 操作集合，否则面板不渲染该页区块。

## 九、双端差异策略

| 维度 | 商户端 | 用户端 |
|---|---|---|
| 主色 | `--accent` 深海军蓝 `#2B3F5F`（工具感、低刺激） | 结构同体系，主色待用户端源确认后绑定（不臆造） |
| 信息密度 | 高密度数据卡 + 处置流 | 任务流为主，密度放宽 |
| 权限呈现 | 角色（店长/员工/财务/运维/管理员）决定可见模块 | 无角色树，仅账号态 |
| 共享不变量 | Token、组件状态机、加载互斥、转场曲线、i18n 机制 —— 两端完全一致 | ← |

## 十、国际化规范

- 键名：`模块.键名`（如 `home.bizStats.todayRevenue`）；插值统一 `{n}` 占位；
- 新增文案必须同时补齐 zh-CN / zh-HK / en / id 四份；
- 治理待办（来自 `_recon_report.txt`）：[ ] 核销 47 条 MISS 候选（补键或删硬编码）；[ ] 清理 8 条 DEAD 死键（version.forceUpdate 等）；
- 文案长度弹性：德/俄等长文本不涉及，但 en 必须在卡片布局下不溢出（按钮文案留 120% 余量）。

## 十一、使用规则（Usage Rules）

1. `--accent-2` 每屏最多 2 处；同一动作全页只有一个 primary 实心按钮；
2. 交互态对比度只升不降：hover/active 在 L 通道 ±0.06–0.12 移动，禁止把前景改淡；
3. 所有可聚焦元素必须有 `:focus-visible` 焦点环；disabled 是唯一允许降对比的状态；
4. 状态色语义唯一：success=成功/在线，danger=危险/断电/告警高等级，warning=中风险，禁止跨义复用；
5. 冻结约束：本体系落地后，双端既有已确认页面不因「统一」而被动改版；新增内容适配体系，存量改动须逐项授权。

## 十二、验收检查（Acceptance Checks）

**P0（不通过则不能交付）**
- [ ] 六核心 Token 与本文件表格逐一相等，页面无游离裸 hex；
- [ ] 任一页面同时刻至多一种加载反馈处于激活态（对照 8.1 矩阵逐触发点核查）；
- [ ] demo-ops 面板逐页独立，抽查两个次页面确认无继承区块；
- [ ] 四语言 JSON 键集合一致，recon 报告 MISS/DEAD 清零或有结论；
- [ ] 触控目标 ≥44px；正文对比度 ≥4.5:1。

**P1**
- [ ] warning 色收敛完成；muted 中间档命名化；
- [ ] 动效全部落在 8.2 参数表内，无自定义曲线逃逸；
- [ ] 空状态、toast、confirm 均有对应组件条目而非页面私写。

## 十三、TODO 与开放问题

- [ ] **开放问题 1**：用户端源文件能否提供？提供后按第四节同样方法实测其主色与特有组件，替换第九节的占位判断；
- [ ] **开放问题 2**：金额数字是否引入 mono/tabular 专属展示档（影响第五节）？
- [ ] **开放问题 3**：z-index 数值表（第六节遗留项）需要一次全页扫描定档；
- [ ] 交付物形式确认：设计体系文档页（单 HTML 规范站，含 Token 表 + 组件样例 + 加载互斥演示）是否符合预期？

## 十四、下一步（Next Step）

1. 你审阅并直接编辑本文件（勾选/修改任意章节均可）；
2. 对三个开放问题给出结论，或在回复中说「按推荐默认处理」；
3. 确认后切到 Design 模式：我将依据本计划生成 `design-system.html` —— 单页可交互规范站（Token 总表、组件样例、Loading 互斥演示、双端差异对照），作为双端后续开发的对照基准。
