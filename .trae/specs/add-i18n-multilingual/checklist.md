# Checklist

- [ ] i18n 架构：`locales/` 目录已创建，包含 `zh-CN.json`、`zh-HK.json`、`en.json`、`id.json` 四个文件
- [ ] i18n 架构：`t(key, params)` 函数已实现，支持点分路径查找
- [ ] i18n 架构：`t()` 支持占位符插值（如 `{name}`、`{count}`）
- [ ] i18n 架构：`loadLocale(lang)` 异步函数已实现，通过 `fetch` 加载外置 JSON
- [ ] i18n 架构：`messages` 响应式对象存储已加载的语言包数据
- [ ] i18n 架构：`loadLocale` 支持缓存已加载语言包（不重复 fetch）
- [ ] i18n 架构：缺失 key 时 fallback 到 `zh-CN`，再缺失返回 key 本身
- [ ] i18n 架构：`fetch` 失败时 toast 提示并保持原语言
- [ ] i18n 架构：页面启动时先加载 `zh-CN` 再加载 `appLang` 对应语言包
- [ ] i18n 架构：`t`、`messages`、`loadLocale`、`localeLoading` 已挂载到 Vue setup 返回值
- [ ] i18n 架构：`<html lang>` 属性随 `appLang` 动态变化
- [ ] zh-CN.json：登录模块文案完整（手机号/邮箱/账号登录、验证码、协议等）
- [ ] zh-CN.json：底部 Tab 文案完整（首页/车辆/订单/我的）
- [ ] zh-CN.json：首页模块文案完整（经营数据/车辆运营/告警/消息中心）
- [ ] zh-CN.json：车辆模块文案完整（列表/详情/地图/轨迹/新增编辑/筛选/车控）
- [ ] zh-CN.json：订单模块文案完整（列表/详情/筛选/退款/客户资料/操作）
- [ ] zh-CN.json：我的/设置模块文案完整（菜单/设置/语言切换/账号/车型/客服等）
- [ ] zh-CN.json：告警模块文案完整（详情/状态/操作按钮/忽略弹窗）
- [ ] zh-CN.json：通用文案完整（toast 消息/确认取消/空态/区号选择等）
- [ ] zh-CN.json：Demo 调试面板文案完整（权限组名/权限项名/操作按钮）
- [ ] zh-CN.json：版本检测弹窗文案完整
- [ ] en.json：所有 key 与 zh-CN 一一对应
- [ ] zh-HK.json：所有 key 与 zh-CN 一一对应
- [ ] id.json：所有 key 与 zh-CN 一一对应
- [ ] 模板替换：登录页所有硬编码文案已替换为 `t()` 调用
- [ ] 模板替换：区号选择页所有硬编码文案已替换
- [ ] 模板替换：首页所有硬编码文案已替换
- [ ] 模板替换：车辆模块所有硬编码文案已替换
- [ ] 模板替换：订单模块所有硬编码文案已替换
- [ ] 模板替换：我的/设置模块所有硬编码文案已替换
- [ ] 模板替换：底部 Tab 栏文案已替换
- [ ] 模板替换：版本检测弹窗文案已替换
- [ ] 模板替换：Demo 调试面板文案已替换
- [ ] JS 替换：所有 `toast()` 调用中的硬编码消息已替换
- [ ] JS 替换：`permGroups` 中 `name`/`n` 字段已替换为 i18n key
- [ ] JS 替换：`menuList` 中 `label` 字段已替换为 i18n key
- [ ] JS 替换：`dialCodes` 中 `name` 字段已替换为 i18n key
- [ ] JS 替换：`demoPagePermCtx` 中 `title` 等字段已替换
- [ ] JS 替换：`selectLang` / `selectLangFromLogin` 已改为调用 `loadLocale` 异步加载
- [ ] JS 替换：其他零散硬编码中文已替换（modal 标题、验证消息等）
- [ ] 验证：通过 HTTP 服务器访问，zh-CN 下所有页面文案正确显示
- [ ] 验证：English 下所有页面文案已翻译，无残留中文
- [ ] 验证：繁體中文下所有页面文案已翻译，无残留简体中文
- [ ] 验证：Bahasa Indonesia 下所有页面文案已翻译，无残留中文
- [ ] 验证：切换语言后界面即时更新，无需刷新
- [ ] 验证：占位符插值正确工作
- [ ] 验证：UI 布局未被破坏（长文案无溢出/截断）
- [ ] 验证：登录页语言切换 → 登录后主界面保持所选语言
- [ ] 验证：语言包加载失败时正确 fallback（模拟网络错误）
- [ ] 验证：已加载语言包的缓存机制正常工作（切换回已加载语言不重复 fetch）
