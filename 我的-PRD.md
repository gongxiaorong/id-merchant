# 我的 Tab PRD（产品版）

> 本文从当前原型 `index.html`（我的 Tab）**倒推**整理，覆盖门店切换、车型、账号、职位、客服、订单配置等**商户运营配置**能力。  
> **不在本期改造**底层权限引擎/角色树模型的产品边界：职位权限为「配置与展示」，与研发权限中台对齐方式见待决项。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 底部导航 · **我的** Tab |
| 文档类型 | 产品需求说明（由现状原型倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（`tab==='profile'` 全部分支页） |
| 状态 | 配置类页面可点；系统设置 + 退出登录 + 登录全套 + 个人信息绑定/换绑 + 修改/重置密码已落地 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

门店管理者除了盯车盯单，还要：

- 在多家门店间切换上下文  
- 维护租用车型的租金/押金  
- 给员工开账号、启停用  
- 配置职位与可见权限树（展示层）  
- 维护客服电话与服务时段  
- 配置「到期前多久变待归还」「超期多久变违约」  

这些能力若只在 PC 后台，店长在店内无法及时改。

### 2.2 核心痛点

| # | 痛点 | 影响 |
|---|---|---|
| 1 | 门店上下文不清 | 看错店的车与单 |
| 2 | 车型价格改不了 | 前台报价与系统不一致 |
| 3 | 员工账号靠人肉同步 | 离职仍可登 |
| 4 | 逾期规则写死 | 无法按门店策略调 |

### 2.3 产品机会

「我的」作为**商户配置中心**入口：个人身份 + 常用功能列表，二级页完成配置，不污染首页/车辆/订单主路径。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **身份可见** | 当前管理者姓名、手机 |
| **门店可切** | 切换后全局门店上下文更新（首页店名等） |
| **车型可维** | 搜、编辑月租/押金/排序 |
| **账号可管** | 增删改查、启停、职位标签 |
| **职位可配** | 树形权限勾选（平台端/商户端分组） |
| **客服可配** | 电话 + 服务时段 |
| **订单规则可配** | 待归还提前天数、违约超期天数 |
| **系统可设** | 切换语言、查看版本、退出登录二次确认 |

### 3.2 成功怎么量

- 切换门店后首页标题与数据上下文一致（产品口径）  
- 车型保存后车辆新增可选到该车型价格逻辑（业务）  
- 禁用账号不可再登录（后端约束，App 展示状态）  
- 订单配置保存后影响订单状态自动流转（与订单 PRD 联动）  
- 退出登录需二次确认；确定后清空本地凭证并进入登录页  

### 3.3 本期不做

- 推送总开关、静默时段等通知偏好（与告警二期可合并）  
- 应用内自动更新 / 版本强更流程  
- 重构权限中台、数据权限、多租户隔离模型  
- 头像上传 / 姓名编辑完整流程（本期只读展示）  
- 找回密码 / 注册（修改/重置密码本期已落地）  
- 审计日志大盘  

---

## 4. 用户与场景

| 场景 | 路径 |
|---|---|
| A 切店 | 我的 → 切换门店 → 点目标店 |
| B 调月租 | 车型管理 → 编辑 → 改月租/押金 → 保存 |
| C 开员工号 | 账号管理 → 新增 → 填账号职位 → 保存 |
| D 停用离职 | 账号 → 禁用 |
| E 配店长权限 | 职位管理 → 编辑 → 勾选权限树 |
| F 挂客服电话 | 客服信息 → 新增 |
| G 调逾期策略 | 订单配置 → 改天数 → 保存 |
| H 切语言 | 我的 → 设置 → 切换语言 → 选语种 |
| I 退出 | 我的 → 设置 → 退出登录 → 确认 → 登录页 |
| J 登录 | 登录页选方式 → 填表 + 勾选协议 → 登录进首页 |
| K 区号 | 手机登录 → 点区号 → 搜索/选中 → 回填 |

---

## 5. 范围与信息架构

### 5.1 我的主屏

| 区域 | 内容 |
|---|---|
| 身份卡 | 头像占位、姓名、手机 |
| 常用功能列表 | 见菜单 |

### 5.2 菜单项（原型）

| id | 标签 | 副文案 | 行为 |
|---|---|---|---|
| store | 切换门店 | 当前门店名 | 进门店列表 |
| model | 车型管理 | — | 进车型列表 |
| account | 账号管理 | — | 进账号列表 |
| pos | 职位管理 | — | 进职位列表 |
| cs | 客服信息 | — | 进客服列表 |
| config | 订单配置 | — | 进配置页 |
| settings | 设置 | — | 进系统设置 |

### 5.3 二级 / 三级页

```
我的
├─ 门店列表
├─ 车型管理 → 编辑车型
├─ 账号管理 → 账号详情 / 新增账号 / 编辑账号
├─ 职位管理 → 职位详情 / 新增|编辑职位
├─ 客服信息 → 新增|编辑客服
├─ 订单配置
└─ 系统设置
   ├─ 切换语言
   └─ 退出登录（确认弹窗）→ 登录页
```

---

## 6. 核心概念

### 6.1 门店上下文

当前选中门店影响首页店名及（产品期望）车辆/订单数据范围。切换即生效并返回。

### 6.2 车型

| 字段 | 说明 |
|---|---|
| 名称 | 只读（编辑页不可改名，与原型一致） |
| 车辆数 | 展示 |
| 月租 / 押金 | 可编，必填 |
| 排序 | 可编 |
| 最后更新时间 | 展示 |

### 6.3 账号

| 字段 | 说明 |
|---|---|
| 账号 | 新增可填；编辑只读；支持「已存在则回填」 |
| 姓名 * | |
| 手机（区号+号） | 区号与登录共用全局选择页；触发仅展示 +XX |
| 邮箱 | |
| 职位 * | 多选：店长/管理员/员工/财务/运维 |
| 备注 | |
| 状态 | 启用 / 禁用 |
| 创建时间 | 展示 |

### 6.4 职位

| 字段 | 说明 |
|---|---|
| 名称 * | |
| 上级职位 | 新增下级时带入 |
| 门店级别 * | 代理商门店 / 运营门店 |
| 备注 | |
| 权限树 | 平台端权限 / 商户端权限，分组展开，全选，子项勾选 |

### 6.5 客服

电话（区号与登录/绑定/账号同一全局选择页，触发仅 +XX）、服务开始/结束（时:分，分档 00/30）、备注。

### 6.6 订单配置

| 项 | 含义 |
|---|---|
| 订单结束前 N 天 → 待归还 | 到期前提醒流转 |
| 订单超期 M 天 → 已违约且不可开锁 | 与车辆开锁能力联动（业务规则） |

### 6.7 系统设置

| 项 | 说明 |
|---|---|
| 切换语言 | 进入语言列表；选中后 `loadLocale` 拉取 `locales/{id}.json` 并回写 chip；与登录共用 `appLang` |
| 当前版本 | 静态只读，展示 `V1.60` |
| 退出登录 | 底部主操作；点击弹出二次确认，避免误触 |

---

## 7. 业务规则

### 7.1 导航

| # | 规则 |
|---|---|
| P1 | 二级页左上返回；有保存类顶栏动作时右上展示 |
| P2 | 危险操作（删除职位/客服、禁用、退出登录等）走确认弹窗 |
| P3 | 我的 → 设置 进入「系统设置」真实页 |

### 7.2 门店

| # | 规则 |
|---|---|
| P4 | 列表展示名称+地址；当前店打勾 |
| P5 | 点选即切换并关闭页（或返回我的） |

### 7.3 车型

| # | 规则 |
|---|---|
| P6 | 支持按名称搜索；空态区分无数据/无匹配 |
| P7 | 月租、押金必填校验；保存 loading |
| P8 | 卡片底部「编辑」动作条 |

### 7.4 账号

| # | 规则 |
|---|---|
| P9 | 搜索：账号/姓名/手机号 |
| P10 | 新增：账号失焦校验存在性——空/已存在回填/将新建 三种提示 |
| P11 | 已存在账号时基础字段可禁用编辑（与原型 `accBasicDisabled` 一致） |
| P12 | 职位至少选一个 |
| P13 | 列表可进详情；可编辑；可启用/禁用 |
| P14 | 手机号列表可脱敏展示 |

### 7.5 职位

| # | 规则 |
|---|---|
| P15 | 支持搜索职位名 |
| P16 | 详情展示基本信息 + 权限树只读展开 |
| P17 | 详情可「新增下级职位」 |
| P18 | 编辑/新增可勾选权限；分组全选 |
| P19 | 删除需确认 |

### 7.6 客服

| # | 规则 |
|---|---|
| P20 | 电话必填；结束时间不得早于开始（校验） |
| P21 | 列表编辑/删除；右上新增 |
| P21a | 服务时段时/分：表单内只读展示框（点击触发）；**禁止**内嵌 `<select>` 滚轮 |
| P21b | 点击开始/结束时间任一框 → 底部抽屉弹窗（半透明遮罩 + 圆角顶 sheet）；内含「时」「分」两列滚轮（分：00/30）；顶栏取消/确定 |
| P21c | 取消不改值；确定回填对应 sh/sm 或 eh/em；遮罩点击等同取消；底层表单不可操作 |

### 7.7 订单配置

| # | 规则 |
|---|---|
| P22 | N、M 为正整数，最小 1 |
| P23 | 顶栏保存；保存中防重复点 |
| P24 | 配置对订单自动状态生效（与订单 Tab、车控开锁限制联动） |
| P24a | 每项规则**仅保留一处天数控件**：句中可编辑输入框（`.login-field`），**禁止**标题文案再回显同一数字 + 下方第二输入 |
| P24b | 文案结构：`订单结束前 [N] 天…` / `订单超期 [M] 天…`；辅助说明保留在控件下方 |

### 7.8 系统设置与退出登录

| # | 规则 |
|---|---|
| P25 | 系统设置顶栏标题「系统设置」；左上返回我的 |
| P26 | 通用设置：切换语言（可跳转）+ 当前版本（只读 `V1.60`） |
| P27 | 切换语言页四选一（简体中文 / 繁體中文（中國香港） / English / Bahasa Indonesia）；选中打勾；回填设置页与登录 chip；**并热加载外置语言包**，P0/P1 UI 文案随语种更新 |
| P28 | 底部「退出登录」唤起居中确认弹窗；底层设置页变暗不可操作 |
| P29 | 弹窗标题「确认退出登录」，正文「退出后需重新登录。」；取消保持登录；确认清空本地凭证并进入登录页；主按钮文案统一为「确认」 |
| P30 | 退出后进入登录全套模块（见 7.9）；勾选协议 + 必填非空即可登录回流 |

### 7.9 登录模块（含区号 / 多方式 / 表单校验）

| # | 规则 |
|---|---|
| P31 | 登录主页顶栏：无返回；右语言切换展示完整语种名（与切换语言列表一致）；中部通用图标占位，无产品名/登录方式副标题；区号/语言子页保留返回 |
| P32 | 所有方式必须勾选「我已阅读并同意《用户隐私政策》」后，登录按钮才可点；未勾选置灰 |
| P33 | 输入框有清除；密码框有明文/密文切换；验证码发送后 60s 倒计时，期间不可重发 |
| P34 | 手机号行左侧区号触发仅展示 +86 等区号（无国旗/CN）；进区号页可搜索；列表仍可有国旗+名称+区号；选中浅蓝底+描边，点击回填 |
| P35 | 手机号：密码/验证码可互切；原型仅非空校验，不校验位数/号段 |
| P36 | 邮箱：密码/验证码两子模式；原型仅非空即可发码；与手机、账号模式可互跳 |
| P37 | 账号+密码：工号类；非空即可；可跳转手机/邮箱登录 |
| P38 | 获取验证码：账号字段非空 + 已勾选协议 + 非倒计时内才可点 |
| P39 | 登录按钮：必填非空 + 协议已勾选即可登录进首页（原型不做格式/凭证鉴权） |

### 7.10 个人信息与手机号 / 邮箱绑定换绑

| # | 规则 |
|---|---|
| P40 | 我的顶部身份卡点击进入「个人信息」；头像、账号、姓名常驻；账号只读；头像/姓名无箭头、不可进二级页 |
| P41 | 手机号/邮箱：已绑定展示完整内容；未绑定展示「未绑定」灰色占位；均可点进绑定页 |
| P42 | 密码管理：有手机或邮箱任一时展示；`hasPwd` 为真文案「修改密码」，否则「设置密码」；手机邮箱均未绑则整行隐藏 |
| P43 | 绑定页标题固定「绑定手机号」「绑定邮箱」；左上返回个人信息；换绑时顶栏下方展示「当前已绑定：…」；首次绑定不展示 |
| P44 | 手机绑定：区号（仅 +XX）+ 手机号 + 验证码；邮箱绑定：邮箱 + 验证码；输入/区号/验证码按钮样式与登录页一致（`.login-field` / `.login-code-btn` / `.login-submit`），可一键清空 |
| P45 | 获取验证码：联系方式非空且非倒计时内可点；发送后 60s「重新获取 (Ns)」；原型不做严格格式校验 |
| P46 | 主按钮：首次「绑定」、已绑「确认替换」；联系方式与验证码均非空才高亮可点 |
| P47 | 提交成功：toast 成功 → 回写个人信息展示 → 返回个人信息；失败（如已绑其他账号）toast 提示并停留本页保留表单 |
| P48 | 全局区号页共用（国旗 + 名称 + 区号 + 浅蓝选中 + 搜索）：登录 / 绑定手机 / 账号新增·编辑 / 客服编辑；触发条统一 `.login-field-dial` 仅 +XX；分别回填 `loginDial` / `bpArea` / `fa.code` / `fc.code` |

### 7.11 修改 / 重置密码

| # | 规则 |
|---|---|
| P49 | 个人信息「密码管理」入口：已有密码文案「修改密码」，无密码「设置密码」；需至少绑定手机或邮箱之一，否则整行隐藏 |
| P50 | 点击进入**同一核验页**（`pwdVerify`，默认优先手机）：已绑手机/邮箱为**无边框只读文案**（非输入框）+ 验证码；获取验证码 60s 倒计时；「下一步」仅验证码非空可点 |
| P51 | 删除 Tab / 独立页跳转；「下一步」下方渠道切换**复用登录页主切换**（`.login-switch-primary` + `mt-7 space-y-4`）：手机渠道展示「邮箱验证」、邮箱渠道展示「手机号验证」；点击为**页内切换**（仅改 `pwdChannel`，清空验证码与倒计时，不换 `profileSub`）；仅绑一种时不展示对侧入口 |
| P52 | 核验通过进入「重置密码」：新密码 + 确认新密码，各 8–32 位；支持显隐与清除；两次一致且长度合规后「确认」高亮；返回回到同一核验页（保留当前 `pwdChannel`） |
| P53 | 提交成功：toast → `hasPwd=true` → 返回个人信息；长度不符/两次不一致 toast 提示并停留本页 |
| P54 | 验证码框 / 主按钮样式与登录、绑定一致（`.login-field`）；联系方式只读无 `.login-field` 边框；原型不做后端复杂度规则 |

### 7.12 表单视觉分层（方案 A，全配置/认证表单）

| # | 规则 |
|---|---|
| P55 | **三层**：Canvas `#F6F7F9` → Surface 白卡 `.form-card`（1px `#E5E5E5`、12px 圆角）→ Field `.login-field`（`#F7F8FA` + 1px `#EBEDF0`、高 48px） |
| P56 | **覆盖页**：登录主表单；绑定手机/邮箱；修改密码核验 + 重置密码；车型编辑；账号新增/编辑；职位新增/编辑；客服新增/编辑；车辆编辑/新增 |
| P57 | **只读字段**（账号名、车架号等）卡内纯文案，无 `.login-field` 边框 |
| P58 | **例外**：列表搜索条、职位权限树勾选区、车型选择虚线强调条（可保留虚线蓝底，仍放在白卡内）；订单配置天数已并入句中 `.login-field`，不再双显 |

---

## 8. 功能需求

| ID | 需求 | 优先级 |
|---|---|---|
| F-P1 | 身份卡（入口/三态） + 菜单列表 | P0 |
| F-P2 | 切换门店 | P0 |
| F-P3 | 车型列表与编辑 | P0 |
| F-P4 | 账号 CRUD + 启停 | P0 |
| F-P5 | 职位 CRUD + 权限树勾选 | P0 |
| F-P6 | 客服 CRUD | P0 |
| F-P7 | 订单配置两项 | P0 |
| F-P8 | 系统设置（切换语言 / 当前版本 / 退出登录） | P0 |
| F-P9 | 退出登录二次确认 + 登录页回流 | P0 |
| F-P12 | 登录全套：手机/邮箱/账号 × 密码/验证码 + 区号 + 协议校验 | P0 |
| F-P13 | 个人信息页（支持未绑/已绑/未设密码三态） | P0 |
| F-P14 | 手机号绑定/换绑（区号 + 验证码 + 状态） | P0 |
| F-P15 | 邮箱绑定/换绑（验证码 + 状态） | P0 |
| F-P16 | 修改/重置密码（核验 + 双输入设置） | P0 |
| F-P10 | 推送总开关 / 静默时段 | P2 |

---

## 9. 验收清单

- [ ] 菜单七项入口正确（设置进系统设置页）  
- [ ] 顶部身份卡进入个人信息  
- [ ] 个人信息页支持三种绑定状态展示与入口文案动态切换
- [ ] 个人信息页头像和姓名不再支持点击跳转（纯展示）
- [ ] 手机/邮箱未绑定显示「未绑定」；已绑定显示完整值并可点进绑定页  
- [ ] 首次绑定无顶部旧联系方式；换绑展示当前已绑定  
- [ ] 绑定按钮文案：首次「绑定」/ 换绑「确认替换」  
- [ ] 手机绑定含区号选择；验证码 60s「重新获取 (Ns)」  
- [ ] 绑定/账号/客服区号列表与登录页完全同一套交互/样式（无原生 select）  
- [ ] 绑定页输入框与登录页 `.login-field` 边框/底色一致  
- [ ] 配置/认证表单为方案 A：灰底 canvas + 白卡 `.form-card` + 浅灰 field（登录/绑定/改密/车型/账号/职位/客服）  
- [ ] 账号新增·编辑、客服编辑：区号触发条同登录，选后回填 fa.code / fc.code  
- [ ] 提交成功回写个人信息并返回；表单清空  
- [ ] 密码管理：双未绑隐藏；有绑定时「设置密码」/「修改密码」  
- [ ] 修改密码核验：已绑手机/邮箱无边框只读 + 验证码；下一步下渠道切换同登录主切换（`.login-switch-primary`）**页内切换**渠道  
- [ ] 核验下一步进入重置密码；确认按钮需两次密码一致且 8–32 位  
- [ ] 重置成功回写 hasPwd 并返回个人信息  
- [ ] 切店后当前店标识与首页店名一致  
- [ ] 车型月租/押金必填与保存  
- [ ] 账号新增三种账号态提示正确  
- [ ] 禁用/启用有确认或明确反馈  
- [ ] 职位权限树展开/全选/保存  
- [ ] 客服时段校验  
- [ ] 客服开始/结束时间：点击唤起底部时分滚轮（非内嵌 select）；确定回填；遮罩禁底层操作  
- [ ] 订单配置：句中单一天数输入（无文案回显 + 下方第二输入）；保存成功反馈  
- [ ] 删除类操作二次确认  
- [ ] 系统设置展示语言与版本 V1.60  
- [ ] 切换语言可选中并回填；Tab/按钮/Toast/Dialog/登录表单等 P0/P1 随 `locales` 切换  
- [ ] 语言包按需加载，主 `index.html` 不内嵌四语文案  

- [ ] 退出登录弹窗：取消停留设置页；确定进登录页  
- [ ] 未勾选协议时登录/获取验证码置灰  
- [ ] 手机密码 / 手机验证码 / 邮箱密码 / 邮箱验证码 / 账号密码可互切  
- [ ] 区号搜索与回填  
- [ ] 验证码 60s 倒计时限流  
- [ ] 密码显隐与输入清除  

---

## 10. 版本规划

| 阶段 | 内容 |
|---|---|
| **本期** | 门店/车型/账号/职位/客服/订单配置；系统设置；登录全套；个人信息 + 绑定换绑 + 修改/重置密码；**i18n 方案 1**（`locales` 外置 P0/P1） |
| **V1.60** | 外置四语包 + `t()`/`loadLocale`；登录/设置切换热更新壳文案 |
| **二期** | 推送与告警通知偏好（可与告警二期合并）；应用更新；找回密码/注册 |
| **三期** | 与权限中台深度对齐、操作审计 |

---

## 11. 待决问题

| # | 问题 | 倾向 |
|---|---|---|
| 1 | 职位权限树是否写真实鉴权还是仅展示？ | 需与后端权限模型对齐后定；原型为可配 UI |
| 2 | 切店是否清掉车辆/订单筛选缓存？ | 建议清空筛选，避免串店条件 |
| 3 | 账号「已存在回填」跨店是否允许？ | 业务定 |
| 4 | 订单配置是否按门店隔离？ | 倾向按当前门店 |
| 5 | 支持语种清单与是否热切换 UI 文案 | **已定（V1.60）**：四语外置 JSON；P0/P1 热切换；演示数据/长业务文案可二期补 key |
| 6 | 登录验证码真实通道与错误码文案 | 本期原型模拟发送；真机对接短信/邮件服务 |
| 7 | 绑定「已绑其他账号」等错误码是否弹窗还是 toast | 原型 toast；真机对齐错误码表 |
| 8 | 换绑是否需校验旧联系方式验证码 | 本期仅校验新联系方式；若合规要求双验则二期 |

---

## 12. 与其它 Tab 边界

| 模块 | 关系 |
|---|---|
| 首页 | 展示当前门店名；数据随门店 |
| 车辆 | 新增选车型来自车型管理；违约不可开锁读订单配置 |
| 订单 | 待归还/违约自动流转读订单配置 |
| 告警 | 无直接配置；通知偏好二期可放设置 |
| 登录 | 退出登录后进入全屏登录；非 Tab 内页面；登录成功默认进首页 |

---

## 13. Next step

1. 业务确认权限树是否本期接真鉴权。  
2. 研发按菜单拆配置 API；切店做上下文刷新；登出清 token。  
3. 登录对接鉴权、短信/邮件验证码与隐私协议 H5。  
4. 设计补推送偏好信息架构（二期）。  
5. 测试重点：切店串数据、账号启停、订单配置生效、退出登录回流、登录按钮/验证码状态机。  

---

*文档结束。*
