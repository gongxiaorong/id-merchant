# 订单 Tab PRD（产品版）

> 本文从当前原型 `index.html`（订单 Tab）**倒推**整理，对齐商户侧订单查询、状态处置、退款、客户资料等能力。  
> **订单业务异常不进告警中心**（见 `告警管理-PRD.md`）；告警仅可展示关联订单号。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 底部导航 · **订单** Tab |
| 文档类型 | 产品需求说明（由现状原型倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（列表、详情、退款记录、客户补充资料、退款/归还 sheet、筛选） |
| 状态 | 主路径可点；状态机以原型按钮矩阵为准，可进评审对齐 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

商户订单侧日常要做：

1. **找到单** —— 按订单号/账号/手机/邮箱/VIN  
2. **看清状态** —— 从创建到完成/取消/违约  
3. **推进状态** —— 确认支付、取消、归还、退款、报失联动  
4. **钱与材料** —— 退款记录、客户补充证件/备注  

没有独立订单中心时，取还车与退款只能靠线下或其它系统。

### 2.2 核心痛点

| # | 痛点 | 影响 |
|---|---|---|
| 1 | 状态多、动作随状态变 | 按钮若写死易做错 |
| 2 | 支付渠道分线上/线下 | 待支付动作不同 |
| 3 | 退款与归还金额需可控 | 超退风险 |
| 4 | 客户资料散落聊天工具 | 审核/纠纷难追溯 |

### 2.3 产品机会

在订单 Tab 内完成：**搜筛列表 → 详情信息 → 状态动作 → 退款/资料子页**，且不把逾期等业务异常塞进告警中心。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **可检索** | 多 scope 搜索 + 快捷 Pill + 筛选面板 |
| **状态可读** | 列表/详情状态标签一致 |
| **动作跟状态** | 底栏主/次按钮随状态与支付方式变化 |
| **钱可退可查** | 退款/归还录入 + 退款记录列表 |
| **资料可管** | 补充资料增删（受状态限制） |

### 3.2 成功怎么量

- 常用状态（待取车/进行中/待还/超期）可从 Pill 一键过滤  
- 每个状态底栏动作与产品矩阵一致，无幽灵按钮  
- 可退金额不超过上限  
- 取消态无主操作底栏  

### 3.3 本期不做

- 商户端「创建新订单」完整下单向导（列表无主 FAB 下单）  
- 订单逾期并入告警中心  
- 复杂审批流、分账、发票  
- 与车辆详情的**真实**双向深链（原型车辆卡为 toast 模拟，二期打通）  
- 权限细到「仅财务可退款」的底层改造（展示层可后续加）  

---

## 4. 用户与场景

| 场景 | 路径 |
|---|---|
| A 查单 | 选 scope=手机号 → 搜索 → 进详情 |
| B 待取车取消 | Pill 待取车 → 详情 → 取消订单确认 |
| C 线下确认支付 | 待支付 + 线下 → 确认支付 |
| D 归还并退部分押金 | 进行中/待还/超期 → 归还车辆 → 填租金/押金退款 |
| E 完成后再次退款 | 已完成 → 退款 → 退款记录可查 |
| F 补证件 | 待支付/待取车 → 客户补充资料 → 拍照/文字 |
| G 超期报失 | 已超期/已违约 → 车辆报失（次按钮） |

---

## 5. 范围与页面清单

| 页面 | 说明 |
|---|---|
| 订单列表 | 搜索、Pill、筛选、卡片列表 |
| 订单详情 | 订单信息、支付信息、车辆卡、资料入口、底栏动作 |
| 退款记录 | 历史退款卡片列表 |
| 客户补充资料 | 图片/文字资料列表 + 添加 |
| 退款/归还 sheet | 租金退款、押金退款、原因、合计 |
| 订单筛选 sheet | 状态、支付渠道、支付状态、**创建时间**（底部日期滚轮） |
| 确认弹窗 | 取消等危险操作 |

---

## 6. 核心概念

### 6.1 订单状态（全量）

| 值 | 展示名 |
|---|---|
| created | 已创建 |
| pendingPay | 待支付 |
| pickup | 待取车 |
| progress | 进行中 |
| return | 待还车 |
| overdue | 已超期 |
| complete | 已完成 |
| cancelled | 已取消 |
| breach | 已违约 |

### 6.2 快捷 Pill（列表）

全部 · 待取车 · 进行中 · 待还车 · 已超期（带数量）。

### 6.3 搜索 scope

订单号 · 账号 · 手机号 · 邮箱 · 车架号。

### 6.4 列表卡片

- 订单号 + 状态标签  
- 有车状态：VIN + 金额；无车状态：仅金额  
- 客户姓名+手机、方案、时间  
- 已取消卡片可降低透明度  

### 6.5 详情信息块

**订单信息**：编号（可复制）、用户（可呼叫）、开始/结束时间、租车方案、当前状态、取消时间（取消态有值否则 —）。

**支付信息**：租金、押金、支付状态、实付、支付方式、支付时间（随是否已付变化）。

**车辆卡**（适用状态）：VIN、车型、在线示意 → 跳转车辆（本期可 toast，二期真跳）。

**客户补充资料入口**：资料份数摘要。

---

## 7. 业务规则

### 7.1 筛选与列表

| # | 规则 |
|---|---|
| O1 | Pill、搜索、筛选面板共同过滤；空态可清除筛选 |
| O1a | **订单状态双向同步**：快捷 Pill 与筛选 sheet「订单状态」共用 `ordersFsSt`；点 Pill 即时写状态；sheet 点「确定」后回写状态并刷新 Pill 高亮（sheet 选了非 Pill 状态时 Pill 回「全部」） |
| O2 | 筛选：订单状态、支付渠道（2C2P/线下）、支付状态、**创建时间** |
| O2a | **创建时间**：默认**近一个月**（含今天共 30 日）；起止**不可置空**；重置/清除筛选时回到默认近一个月（不计入「有筛选」角标） |
| O2b | **创建时间交互**：只读触发框 → 底部抽屉 **年/月/日** 滚轮（对齐轨迹时间 sheet）；取消不改；确定回填；起>止时自动对齐另一端 |
| O3 | 下拉刷新列表 |

### 7.2 状态 → 底栏动作（与原型矩阵对齐）

| 状态 | 主按钮 | 次按钮 | 备注 |
|---|---|---|---|
| 已创建 | 取消订单（危险样式） | — | |
| 待支付 · 线上 | 取消订单 | — | |
| 待支付 · 线下 | **确认支付** | 取消订单 | 双按钮 |
| 待取车 | 取消订单 | — | |
| 进行中 | 归还车辆 | — | 打开归还/退款 sheet |
| 待还车 | 归还车辆 | — | |
| 已超期 | 归还车辆 | 车辆报失 / 解除报失 | 双按钮 |
| 已违约 | 归还车辆 | 车辆报失 / 解除报失 | 双按钮 |
| 已完成 | 退款 | — | 打开退款 sheet |
| 已取消 | **无底栏** | — | 可看退款记录（若适用） |

| # | 规则 |
|---|---|
| O4 | `showActions = 状态 ≠ 已取消` |
| O5 | 主按钮危险色：创建/待支付(非线下确认)/待取车 的取消类 |
| O6 | 报失态下次按钮文案切换为「解除报失」，样式中性 |

### 7.3 退款 / 归还

| # | 规则 |
|---|---|
| O7 | sheet 标题：归还流程用「归还车辆」，完成后再次退用「退款」 |
| O8 | 租金退款、押金退款可编辑，有上限（原型：租金 ≤800、押金 ≤500，**上限以后端为准**） |
| O9 | 超限钳制；原因选填；展示合计；确认提交 |
| O10 | 退款记录入口：`已完成` 或 `已取消` 时顶栏展示「退款记录」 |
| O11 | 退款记录字段：时间、状态、租金退款、押金退款、操作账号、来源、原因；空态友好 |

### 7.4 客户补充资料

| # | 规则 |
|---|---|
| O12 | 入口卡片始终可进（只读或可编） |
| O13 | **可编辑**状态：已创建、待支付、待取车 |
| O14 | 其它状态只读：不可添加/删除 |
| O15 | 类型：拍照上传（证件类标签）/ 文字备注 |
| O16 | 删除需二次确认；图片可预览 |

### 7.5 与其它模块

| # | 规则 |
|---|---|
| O17 | 订单逾期/超期/违约 **不进** 告警中心 |
| O18 | 消息中心可有「逾期提醒」类运营消息，与告警分离 |
| O19 | 待还/超期自动规则天数在「我的 · 订单配置」维护 |

---

## 8. 功能需求

| ID | 需求 | 优先级 |
|---|---|---|
| F-O1 | 多 scope 搜索 + Pill + 筛选 | P0 |
| F-O2 | 订单列表卡片与空态 | P0 |
| F-O3 | 详情信息 + 状态动作矩阵 | P0 |
| F-O4 | 取消等确认弹窗 | P0 |
| F-O5 | 归还/退款 sheet 与金额校验 | P0 |
| F-O6 | 退款记录页 | P0 |
| F-O7 | 客户补充资料 CRUD（状态门控） | P0 |
| F-O8 | 超期/违约报失联动 | P0 |
| F-O9 | 详情 → 真车辆详情 | P1 |
| F-O10 | 告警详情关联订单可点回本单 | P1 |

---

## 9. 验收清单

- [ ] 九种状态标签展示正确  
- [ ] Pill 数量与列表一致  
- [ ] 各状态底栏按钮与 §7.2 矩阵一致  
- [ ] 已取消无底栏操作  
- [ ] 线下待支付可确认支付 + 取消  
- [ ] 退款金额不超上限，合计正确  
- [ ] 退款记录在完成/取消可进  
- [ ] 资料仅创建/待支付/待取车可编辑  
- [ ] 删除资料有确认  
- [ ] 订单异常未出现在告警中心  

---

## 10. 版本规划

| 阶段 | 内容 |
|---|---|
| **本期** | 列表搜筛、详情状态机、退款/归还、资料、退款记录；V1.57 状态双向同步；**V1.58** 创建时间（默认近一月、不可置空、底部日期滚轮 O2a/O2b） |
| **二期** | 车辆/告警真深链；逾期 SOP 文案与强制联系；批量导出 |
| **三期** | 商户创建订单、优惠券、分账 |

---

## 11. 待决问题

| # | 问题 | 当前默认 |
|---|---|---|
| 1 | 退款金额上限固定还是按实付动态？ | 原型固定示意；**研发须按实付剩余可退** |
| 2 | 「确认支付」是否写支付流水号？ | 本期一键确认即可 |
| 3 | 报失是否同步车辆资产报失状态？ | 产品倾向同步车辆 `lost` |
| 4 | 已取消是否允许再次退款入口？ | 可看记录；是否发起新退款跟支付态 |

---

## 12. Next step

1. 业务确认 §7.2 按钮矩阵与真实状态机无偏差。  
2. 研发按状态表实现动作，金额以后端可退余额为准。  
3. 设计补：支付中、退款失败、无网。  
4. 测试以 §9 为主用例，重点「状态 × 按钮」全组合。  

---

*文档结束。*
