# 订单 Tab PRD（产品 + 联动规则完整版）

> 本文从当前原型 `index.html`（订单 Tab）**倒推**整理，对齐商户侧订单查询、状态处置、退款、客户资料等能力，并整合订单详情页的信息卡展示、原型操作面板、底栏动作三者的联动规则。
> **订单业务异常不进告警中心**（见 `告警管理-PRD.md`）；告警仅可展示关联订单号。
>
> **合并说明**：本文档由 `订单-PRD.md`（产品版）与 `订单详情-联动规则计划.md`（联动规则版）合并而成。联动规则的「四维状态模型」「信息卡展示规则」「原型操作面板规则」「底栏动作矩阵」「场景映射」「实现指引」已分别归入对应章节。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 底部导航 · **订单** Tab |
| 文档类型 | 产品需求说明（由现状原型倒推）+ 联动规则计划 |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（列表、详情、退款记录、客户补充资料、退款/归还 sheet、筛选） |
| 状态 | 主路径可点；状态机以原型按钮矩阵为准，可进评审对齐 |
| 版本基准 | 联动规则基于 V1.5（冻结版本）；列表筛选 V1.57 状态双向同步、V1.58 创建时间滚轮；V1.59 移除订单状态「待支付」（支付由支付状态独立判断、存量回退已创建）、重命名「待取用/待归还」；V1.60 快捷 Pill 精简为 5 个常用状态（全部/待取用/进行中/待归还/已超期）并支持左右滑动 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

商户订单侧日常要做：

1. **找到单** —— 按订单号/账号/手机/邮箱/VIN
2. **看清状态** —— 从创建到完成/取消/违约
3. **推进状态** —— 确认支付、取消、归还、退款、报失联动
4. **钱与材料** —— 退款记录、客户补充证件/备注

没有独立订单中心时，取还车与退款只能靠线下或其它系统。

### 2.2 核心痛点

| # | 痛点 | 影响 |
|---|---|---|
| 1 | 状态多、动作随状态变 | 按钮若写死易做错 |
| 2 | 支付渠道分线上/线下 | 未支付动作不同 |
| 3 | 退款与归还金额需可控 | 超退风险 |
| 4 | 客户资料散落聊天工具 | 审核/纠纷难追溯 |

### 2.3 产品机会

在订单 Tab 内完成：**搜筛列表 → 详情信息 → 状态动作 → 退款/资料子页**，且不把逾期等业务异常塞进告警中心。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **可检索** | 多 scope 搜索 + 快捷 Pill + 筛选面板 |
| **状态可读** | 列表/详情状态标签一致 |
| **动作跟状态** | 底栏主/次按钮随状态与支付方式变化 |
| **钱可退可查** | 退款/归还录入 + 退款记录列表 |
| **资料可管** | 补充资料增删（受状态限制） |

### 3.2 成功怎么量

- 常用状态（待取用/进行中/待归还/超期）可从 Pill 一键过滤
- 每个状态底栏动作与产品矩阵一致，无幽灵按钮
- 可退金额不超过上限
- 取消态无主操作底栏

### 3.3 本期不做

- 商户端「创建新订单」完整下单向导（列表无主 FAB 下单）
- 订单逾期并入告警中心
- 复杂审批流、分账、发票
- 与车辆详情的**真实**双向深链（原型车辆卡为 toast 模拟，二期打通）
- 权限细到「仅财务可退款」的底层改造（展示层可后续加）

---

## 4. 用户与场景

### 4.1 产品场景路径

| 场景 | 路径 |
|---|---|
| A 查单 | 选 scope=手机号 → 搜索 → 进详情 |
| B 待取用取消 | Pill 待取用 → 详情 → 取消订单确认 |
| C 线下确认支付 | 支付状态=待支付 + 线下 → 确认支付 |
| D 归还并退部分押金 | 进行中/待归还/超期 → 归还车辆 → 填租金/押金退款 |
| E 完成后再次退款 | 已完成 → 退款 → 退款记录可查 |
| F 补证件 | 已创建（未支付）/待取用 → 客户资料 → 拍照/文字 |
| G 超期报失 | 已超期/已违约 → 车辆报失（次按钮） |

### 4.2 联动场景 → 规则映射（8 组）

基于四维状态模型，以下 8 组场景覆盖全部底栏动作组合：

| # | 订单状态 | 支付状态 | 支付方式 | 报失状态 | 底栏动作 |
|---|---|---|---|---|---|
| 1 | created | pending | online | 不展示 | 取消订单 |
| 2 | created | pending | offline | 不展示 | 确认支付 + 取消订单 |
| 3 | pickup | paid | 不展示 | 不展示 | 取消订单 |
| 4 | progress / return | paid | 不展示 | 不展示 | 归还车辆 |
| 5 | overdue / breach | paid | 不展示 | 未报失 | 归还车辆 + 车辆报失 |
| 6 | overdue / breach | paid | 不展示 | 已报失 | 归还车辆 + 解除报失 |
| 7 | complete | paid / partialRefund | 线上支付 | 不展示 | 退款 |
| 8 | complete / cancelled | refunding / partialRefund / fullRefund | 线上支付 | 不展示 | 退款记录 |

> 说明：订单状态不含「待支付」，是否支付由支付状态（dPaySt）独立判断；存量 pendingPay 数据统一回退为 created（已创建）。

---

## 5. 范围与页面清单

| 页面 | 说明 |
|---|---|
| 订单列表 | 搜索、Pill、筛选、卡片列表 |
| 订单详情 | 订单信息、支付信息、车辆卡、资料入口、底栏动作 |
| 退款记录 | 历史退款卡片列表 |
| 客户补充资料 | 图片/文字资料列表 + 添加 |
| 退款/归还 sheet | 租金退款、押金退款、原因、合计 |
| 全部筛选 sheet | 状态、支付渠道、支付状态、**创建时间**（底部日期滚轮） |
| 确认弹窗 | 取消等危险操作 |

---

## 6. 核心概念

### 6.1 四维状态模型

订单详情页的展示与动作由四个维度共同决定：

| 维度 | Ref | 取值范围 | 说明 |
|---|---|---|---|
| 订单状态 | `dSt` | created / pickup / progress / return / overdue / complete / cancelled / breach | 订单生命周期主状态（是否支付由 dPaySt 独立判断） |
| 支付状态 | `dPaySt` | pending / processing / paid / refunding / partialRefund / fullRefund | 原型面板仅暴露前三项，detail 映射全量 |
| 报失状态 | `orderLost` | false / true | 仅 overdue / breach 时展示 |
| 支付方式 | `dPm` | online / offline | 仅 dPaySt=pending 时可切换 |

### 6.2 订单状态（全量）

| 值 | 展示名 |
|---|---|
| created | 已创建 |
| pickup | 待取用 |
| progress | 进行中 |
| return | 待归还 |
| overdue | 已超期 |
| complete | 已完成 |
| cancelled | 已取消 |
| breach | 已违约 |

> 备注：订单状态不含「待支付」；是否已支付由支付状态（dPaySt）独立判断，存量 pendingPay 数据回退为已创建。

### 6.3 快捷 Pill（列表）

仅 **5 个常用状态**：全部 · 待取用 · 进行中 · 待归还 · 已超期（带数量）。单行横向排布，超出屏宽可**左右滑动**（不换行）。其余状态（已创建/已完成/已取消/已违约）不进 Pill，仅可在筛选面板选择。

### 6.4 搜索 scope

订单号 · 账号 · 手机号 · 邮箱 · 车架号。

### 6.5 列表卡片

- 订单号 + 状态标签
- 有车状态：VIN + 金额；无车状态：仅金额
- 客户姓名+手机、方案、时间
- 已取消卡片可降低透明度

### 6.6 详情信息块

#### 订单信息卡（上半部分）— 冻结

| 字段 | 展示规则 |
|---|---|
| 订单编号 / 用户信息 / 时间 / 方案 | 始终展示 |
| 当前状态 | 始终展示，单 chip，样式由 `stg(dSt)` 决定 |
| 取消时间 | 仅 dSt=cancelled 时展示具体时间 |
| 报失状态 | V1.4 已新增，保持不动 |

> 订单信息：编号（可复制）、用户（可呼叫）、开始/结束时间、租车方案、当前状态、取消时间（取消态有值否则 —）。

#### 支付信息卡（下半部分）— 冻结

| 字段 | 展示规则 |
|---|---|
| 租金 / 押金 | 始终展示 |
| 支付状态 | 始终展示，文字+颜色由 `payStatusColor` 决定 |
| 实付金额 | 当 `showPaid` 为 true 时展示 |
| 支付方式 | 始终展示（V1.4 条件渲染已保留） |
| 支付时间 | 当 `showPaid` 为 true 时展示 |

#### 车辆卡 / 资料入口

| 组件 | 展示规则 |
|---|---|
| 车辆卡 | 当 dSt ∈ {progress, return, overdue, complete, breach} 时展示 |
| 客户补充资料 | 始终展示入口，编辑权限由 `canEditDocs` 控制 |

> 车辆卡（适用状态）：VIN、车型、在线示意 → 跳转车辆（本期可 toast，二期真跳）。

---

## 7. 业务规则

### 7.1 筛选与列表

| # | 规则 |
|---|---|
| O1 | Pill、搜索、筛选面板共同过滤；空态可清除筛选 |
| O1a | **订单状态双向同步**：快捷 Pill 与筛选 sheet「订单状态」共用 `ordersFsSt`；点 Pill 即时写状态；sheet 点「确定」后回写状态并刷新 Pill 高亮。快捷 Pill 仅 **5 个常用状态**（全部/待取用/进行中/待归还/已超期）；sheet 选了非 Pill 状态（已创建/已完成/已取消/已违约）时 Pill 回「全部」 |
| O2 | 筛选：订单状态、支付渠道（2C2P/线下）、支付状态、**创建时间** |
| O2a | **创建时间**：默认**近一个月**（含今天共 30 日）；起止**不可置空**；重置/清除筛选时回到默认近一个月（不计入「有筛选」角标） |
| O2b | **创建时间交互**：只读触发框 → 底部抽屉 **年/月/日** 滚轮（对齐轨迹时间 sheet）；取消不改；确定回填；起>止时自动对齐另一端 |
| O3 | 下拉刷新列表 |

### 7.2 状态 → 底栏动作矩阵（与原型矩阵对齐）

> 底栏动作由四维状态模型共同决定。以下为完整的主/次按钮矩阵。

#### 主按钮（showPrimaryAction）

| 订单状态 | 支付状态 | 支付方式 | 主按钮 | 样式 | 触发动作 |
|---|---|---|---|---|---|
| created | pending | online | 取消订单 | 危险红 | 取消确认 |
| created | pending | offline | 确认支付 | cobalt | 确认支付弹窗 |
| pickup | paid | — | 取消订单 | 危险红 | 取消确认 |
| progress / return | paid | — | 归还车辆 | cobalt | 打开归还/退款 sheet |
| overdue / breach | paid | — | 归还车辆 | cobalt | 打开归还/退款 sheet |
| complete | paid / partialRefund | — | 退款 | cobalt | 打开退款 sheet |
| complete | refunding / fullRefund | — | 退款记录 | cobalt | 跳转退款记录页 |
| cancelled | refunding / partialRefund / fullRefund | — | 退款记录 | cobalt | 跳转退款记录页 |

#### 次按钮（showDangerAction）

| 订单状态 | 支付状态 | 支付方式 | 次按钮 | 样式 | 触发动作 |
|---|---|---|---|---|---|
| created | pending | offline | 取消订单 | 中性灰 | 取消确认 |
| overdue / breach | paid | — | 车辆报失 / 解除报失 | 中性灰 / 危险红 | 报失确认 |

#### 速查表（按状态）

| 状态 | 主按钮 | 次按钮 | 备注 |
|---|---|---|---|
| 已创建 | 取消订单（危险样式）/ **确认支付**（未支付·线下） | 取消订单（未支付·线下） | 支付状态独立判断 |
| 待取用 | 取消订单 | — | |
| 进行中 | 归还车辆 | — | 打开归还/退款 sheet |
| 待归还 | 归还车辆 | — | |
| 已超期 | 归还车辆 | 车辆报失 / 解除报失 | 双按钮 |
| 已违约 | 归还车辆 | 车辆报失 / 解除报失 | 双按钮 |
| 已完成 | 退款 | — | 打开退款 sheet |
| 已取消 | **无底栏** | — | 可看退款记录（若适用） |

| # | 规则 |
|---|---|
| O4 | `showActions = 状态 ≠ 已取消`（或 cancelled 但有退款记录时也展示「退款记录」入口） |
| O5 | 主按钮危险色：已创建/待取用 的取消类（未支付·线下时主按钮为「确认支付」，非危险样式） |
| O6 | 报失态下次按钮文案切换为「解除报失」，样式中性 |

### 7.3 dualMode 判定

`dualMode = true` 当且仅当：
- dSt ∈ {overdue, breach}，或
- dPaySt='pending' 且 dPm='offline'

即底栏同时展示主按钮 + 次按钮的双按钮模式。

### 7.4 退款 / 归还

| # | 规则 |
|---|---|
| O7 | sheet 标题：归还流程用「归还车辆」，完成后再次退用「退款」 |
| O8 | 租金退款、押金退款可编辑，有上限（原型：租金 ≤800、押金 ≤500，**上限以后端为准**） |
| O9 | 超限钳制；原因选填；展示合计；确认提交 |
| O10 | 退款记录入口：`已完成` 或 `已取消` 时顶栏展示「退款记录」 |
| O11 | 退款记录字段：时间、状态、租金退款、押金退款、操作账号、来源、原因；空态友好 |

### 7.5 客户补充资料

| # | 规则 |
|---|---|
| O12 | 入口卡片始终可进（只读或可编） |
| O13 | **可编辑**状态：已创建、待取用 |
| O14 | 其它状态只读：不可添加/删除 |
| O15 | 类型：拍照上传（证件类标签）/ 文本资料 |
| O16 | 删除需二次确认；图片可预览 |

### 7.6 与其它模块

| # | 规则 |
|---|---|
| O17 | 订单逾期/超期/违约 **不进** 告警中心 |
| O18 | 消息中心可有「逾期提醒」类运营消息，与告警分离 |
| O19 | 待归还/超期自动规则天数在「我的 · 订单配置」维护 |

---

## 8. 原型操作面板（demo-ops）规则

> demo-ops 面板是原型侧的状态切换工具，用于演示各状态组合下的信息卡与底栏联动。非产品功能。

### 8.1 面板结构（ordersOV===detail 时展示）

```
订单状态        ← 8 chip，单选，allStatus
支付状态        ← 3 chip，单选，demoPaySts（待支付/部分退款/全部退款）
报失状态        ← 2 chip，单选，仅当 dSt ∈ {overdue, breach} 时展示
支付方式        ← 2 chip，单选，仅当 dPaySt='pending' 时展示
取消前状态      ← 2 chip，单选，仅当 dSt='cancelled' 时展示
```

### 8.2 联动规则

| 操作 | 联动影响 |
|---|---|
| 切换 订单状态 | 自动重置 `dPaySt`（created→pending，其余→paid）；触发报失状态/取消前状态的显隐 |
| 切换 支付状态 | 触发支付方式 section 的显隐 |
| 切换 报失状态 | 联动 `do_.lost`；联动底栏次按钮文案 |
| 切换 支付方式 | 联动底栏主/次按钮（线下待支付=双按钮） |
| 切换 取消前状态 | 联动底栏按钮 |

---

## 9. 实现指引（代码修改点位）

> 以下为联动规则对应的代码修改清单，供研发参考。

### 9.1 原型操作面板（template）

| 点位 | 修改内容 |
|---|---|
| 报失状态 section | `v-if="['overdue','breach'].includes(dSt)"` |
| 支付方式 section | 保持 `v-if="dPaySt==='pending'"` |
| 取消前状态 section | 保持 `v-if="dSt==='cancelled'"` |

### 9.2 底栏动作 computed

| 函数 | 修改内容 |
|---|---|
| `showActions` | `dSt.value !== 'cancelled'` → `dSt.value !== 'cancelled' \|\| curRefunds.length > 0` |
| `showPrimaryAction` | 增加 complete + refunding/fullRefund → true；增加 cancelled + refund status → true |
| `primaryLabel` | 增加 complete+refunding/fullRefund → '退款记录'；增加 cancelled+refund → '退款记录' |
| `primaryDanger` | complete/cancelled 退款类 → false |
| `showDangerAction` | cancelled 退款类 → false（保持） |
| `ordersDoPrimary` | 增加 refunding/fullRefund → openRefundRecords()；增加 cancelled+refund → openRefundRecords() |
| `dualMode` | 保持现状（overdue/breach 或 pending+offline） |

### 9.3 return 暴露列表

| 函数/变量 | 是否已暴露 | 操作 |
|---|---|---|
| `dSt` | ✓ | 无需改动 |
| `dPaySt` | ✓ | 无需改动 |
| `dPm` | ✓ | 无需改动 |
| `orderLost` | ✓ | 无需改动 |
| `dCanceledPaid` | ✓ | 无需改动 |
| `curRefunds` | ✓ | 无需改动 |
| `showActions` | ✓ | 无需改动 |
| 新增/修改的 computed | — | 按 9.2 修改后自动响应 |

---

## 10. 功能需求

| ID | 需求 | 优先级 |
|---|---|---|
| F-O1 | 多 scope 搜索 + Pill + 筛选 | P0 |
| F-O2 | 订单列表卡片与空态 | P0 |
| F-O3 | 详情信息 + 状态动作矩阵 | P0 |
| F-O4 | 取消等确认弹窗 | P0 |
| F-O5 | 归还/退款 sheet 与金额校验 | P0 |
| F-O6 | 退款记录页 | P0 |
| F-O7 | 客户补充资料 CRUD（状态门控） | P0 |
| F-O8 | 超期/违约报失联动 | P0 |
| F-O9 | 详情 → 真车辆详情 | P1 |
| F-O10 | 告警详情关联订单可点回本单 | P1 |

---

## 11. 验收清单

**列表与筛选**
- [ ] 八种状态标签展示正确
- [ ] 快捷 Pill 仅 5 个常用状态（全部/待取用/进行中/待归还/已超期），单行横向排布、可左右滑动不换行
- [ ] Pill 数量与列表一致
- [ ] 订单状态双向同步（Pill ↔ 筛选 sheet）
- [ ] 创建时间默认近一月、不可置空、底部日期滚轮

**详情与底栏**
- [ ] 各状态底栏按钮与 §7.2 矩阵一致（含退款记录类场景 7/8）
- [ ] 已取消无底栏操作（有退款记录时可进退款记录）
- [ ] 线下待支付可确认支付 + 取消
- [ ] 信息卡字段按冻结规则展示
- [ ] 报失状态 section 仅 overdue/breach 展示

**退款与资料**
- [ ] 退款金额不超上限，合计正确
- [ ] 退款记录在完成/取消可进
- [ ] 资料仅创建/待取用可编辑
- [ ] 删除资料有确认

**跨模块**
- [ ] 订单异常未出现在告警中心

---

## 12. 版本规划

| 阶段 | 内容 |
|---|---|
| **本期** | 列表搜筛、详情状态机、退款/归还、资料、退款记录；V1.57 状态双向同步；**V1.58** 创建时间（默认近一月、不可置空、底部日期滚轮 O2a/O2b）；联动规则对齐（详情信息卡冻结 + 底栏矩阵 + demo-ops 面板） |
| **二期** | 车辆/告警真深链；逾期 SOP 文案与强制联系；批量导出 |
| **三期** | 商户创建订单、优惠券、分账 |

---

## 13. 待决问题

| # | 问题 | 当前默认 |
|---|---|---|
| 1 | 退款金额上限固定还是按实付动态？ | 原型固定示意；**研发须按实付剩余可退** |
| 2 | 「确认支付」是否写支付流水号？ | 本期一键确认即可 |
| 3 | 报失是否同步车辆资产报失状态？ | 产品倾向同步车辆 `lost` |
| 4 | 已取消是否允许再次退款入口？ | 可看记录；是否发起新退款跟支付态 |

---

## 14. 附录

### 14.1 冻结内容确认

以下内容保持冻结状态，不做任何改动：

> 注：V1.59 调整（移除订单状态「待支付」、重命名「待取用/待归还」）涉及 `allStatus`、`sl()`/`stg()`、`ordersOpenDetail()`、`primaryLabel`/`primaryDanger`、`ordersDoPrimary()`、`watch(dSt)`、`canEditDocs` 及示例数据，冻结内容按调整后版本执行。V1.60 调整（快捷 Pill 精简为 5 个常用状态并支持左右滑动）涉及 `quickPills`/`qCnt` 定义及 `#orderPillRow` 样式，冻结内容按调整后版本执行。

- `allStatus` / `paySts` / `demoPaySts` 定义
- `sl()` / `stg()` 映射函数
- `ordersOpenDetail()` 打开逻辑
- `ordersDoDanger()` 报失/取消逻辑
- `ordersConfirmMod()` 确认弹窗逻辑
- `openRefund()` / `submitRefund()` 退款 sheet 逻辑
- `openRefundRecords()` / `curRefunds` 退款记录逻辑
- 客户补充资料模块（`canEditDocs` / `curDocs`）
- 车辆卡模块（`showVehicle`）
- 订单信息卡 / 支付信息卡的展示规则（§6.6）
- 订单列表、筛选、搜索
- 全部样式 token 与 CSS

### 14.2 联动规则变更影响评估

| 维度 | 评估 |
|---|---|
| 需求确认 | 按 8 组场景重建订单详情的展示-操作联动，信息卡条件字段、底栏动作矩阵、原型面板显隐三端对齐 |
| 修改范围 | template 条件展示 2 处；computed 调整 6 处；template 原型面板 1 处；return 无新增 |
| 间接影响 | 退款记录页、退款 sheet、确认弹窗、客户资料、车辆卡、订单列表均不受影响 |
| 冻结内容 | 见 §14.1 |
| 风险评估 | 中风险（涉及 6 处 computed 联动），但均为现有 ref 的条件扩展，无新数据模型 |

---

## 15. Next step

1. 业务确认 §7.2 按钮矩阵与真实状态机无偏差，确认 §4.2 的 8 组场景无遗漏。
2. 研发按状态表实现动作，金额以后端可退余额为准。
3. 设计补：支付中、退款失败、无网。
4. 测试以 §11 为主用例，重点「状态 × 按钮」全组合。
5. 联动规则按 §9 实现指引逐项执行代码修改，完成后逐场景走查信息卡展示 + 底栏按钮 + 原型面板联动。

---

*文档结束。本文档由 `订单-PRD.md` 与 `订单详情-联动规则计划.md` 合并而成，原两文件内容已完整整合，可移除原始文件。*
