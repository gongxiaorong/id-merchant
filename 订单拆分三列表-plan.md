# 订单拆分三列表 · 设计方案（目标版本 V1.62）

> 计划文档 · 在 Design 模式落地前先审阅确认。基线与当前 `index.html`（V1.61）对齐。

## 一、需求确认

1. 将现有「订单」模块拆分为 **三个独立列表**：车辆租赁、电池、车电，列表样式与当前订单列表**完全一致**。
2. 三个列表的入口**统一放到首页**：首页新增「订单管理」模块，内置三个入口卡片。
3. 底部「订单」Tab 本期先以**空电池列表占位**，后续版本再决定去向。

## 二、现状梳理（V1.61）

- 底部 Tab（`index.html:2056-2066`）：首页 / 车辆 / 订单 / 我的。
- 订单列表（`index.html:1104-1162`）：搜索框（范围切换）→ 类型 Tab（全部/车辆租赁/电池租赁/车电套餐）→ 状态 Pill → 卡片列表 → 空状态。
- 订单详情 / 退款记录 / 客户资料 / 筛选 / 退款弹层（`index.html:1164-1395`）。
- 数据：`orders[]` 每条含 `type: 'vehicle' | 'battery' | 'package'`（`index.html:3359-3385`）。
- 类型过滤逻辑：`ordersType` + `typePills` + `typeCnt` + `oFiltered`（`index.html:2840-2841,2872`）。
- 首页模块（`index.html:373-446`）：经营数据 → 车辆运营 → 最近告警。
- 权限：`order.list` 系列权限码（`index.html:2727-2728`）；底部 Tab 权限 `TAB_PERM_ITEMS`（`index.html:2710`）。

## 三、目标方案

1. **首页新增「订单管理」模块**：三个入口卡片「车辆租赁 / 电池 / 车电」，卡片显示名称 + 图标 + 对应类型订单数。
2. **三入口跳转**：点击后进入订单列表并**锁定对应类型**（`ordersType` 预设为 vehicle / battery / package），列表布局与当前订单 list 一致。
3. **底部「订单」Tab 占位**：保留 Tab 与图标，列表展示为**空电池列表**（电池租赁样式 + 无数据空状态）。

## 四、模块设计

### 4.1 首页「订单管理」模块
- 位置建议：**车辆运营之后、最近告警之前**（待确认，见开放问题 O1）。
- 视觉：复用首页现有区块体系——区块标题「订单管理」+ 白卡片内三行入口（图标 + 名称 + 数量 + chevron），与「最近告警」行样式同源；比车辆运营的 2 列网格字重更高（入口性质）。
- 权限：模块整体由 `can('order.list')` 控制显示；不新增权限码（见开放问题 O4）。
- 空数据处理：某类型订单数为 0 时不隐藏入口，数量显示 0（入口常驻，方便直达空列表）。

### 4.2 三个列表页（车辆租赁 / 电池 / 车电）
- 复用现有订单列表全部能力：搜索（范围）、状态 Pill、筛选弹层、下拉刷新、骨架屏、空状态、详情、退款记录、客户资料、退款/归还弹层、demo-ops 订单状态面板。
- 与现状唯一差异：
  - 类型锁定：进入时 `ordersType` 预置为当前条目类型；
  - 标题/空状态文案按类型区分（如「暂无电池租赁订单」「当前筛选下无匹配电池订单」）；
  - 藏掉列表内「类型 Tab」整行（类型已由入口决定，避免重复筛选）。
- 跳转方式：保留底部 Tab 结构，入口通过 `setTab('orders')` + 设置 `ordersType` 打开，返回走底部 Tab / 详情返回，与现状导航一致。

### 4.3 底部「订单」Tab 占位
- 保留 Tab 及 `order.list` 权限控制。
- 列表展示：电池租赁列表样式，数据为空 → 走现有「暂无订单」空状态（图标 + 文案「暂无电池租赁订单」）。
- 不接订单数据、不放搜索（占位期）。后续迭代再决定：隐藏 Tab / 收敛为「全部订单」/ 去掉。

## 五、数据与状态

- 复用 `ordersType` / `typePills` / `oFiltered` 过滤链，不新增数据模型。
- 「订单」Tab 占位与首页三入口共用同一列表模板，仅数据源不同（占位=空数据）。
- 状态同步：进入三列表时将 `ordersType` 置为对应值；切换底部 Tab 回首页/其他页时不强刷 `ordersType`（保持上下文，详见验收 V6）。

## 六、冻结内容

- 订单详情、退款记录、客户资料、筛选/退款/归还弹层、demo-ops 面板、权限树、底部 Tab 结构、视觉 token、PRD 与 locales 文件。
- 车辆、首页告警、我的模块所有内容。

## 七、风险与开放问题

- **O1 首页模块位置**：订单管理放在「车辆运营」之后 / 「最近告警」之后，或独立置顶——需确认。
- **O2 入口命名**：需求用「车辆租赁 / 电池 / 车电」，现类型 Tab 文案为「电池租赁 / 车电套餐」。三入口是否按需求文案，页面内空状态/标题用哪种命名。
- **O3 类型 Tab 隐藏**：三个列表页是否隐藏「全部/车辆租赁/电池租赁/车电套餐」类型 Tab 行（方案为隐藏）——需确认。
- **O4 按类目拆分权限**：是否新增 `order.list.vehicle/battery/package` 级权限（现权限树无此分层，涉及权限体系，本期**默认不动**）——需确认是否需要。
- **O5 订单 Tab 最终去向**：占位下车列表后，最终是隐藏 Tab、保留为「全部订单」还是移除——本期不决定，仅占位。
- **O6 排序优先级**：三入口展示顺序为「车辆租赁 / 电池 / 车电」——按需求固定。

## 八、验收清单

- [ ] V1 首页出现「订单管理」模块，三入口可点击进入对应类型列表。
- [ ] V2 三列表样式与当前订单列表一致（搜索/状态 Pill/筛选/骨架/空状态/下拉刷新）。
- [ ] V3 列表内类型 Tab 行已隐藏，标题与空状态文案按类型区分。
- [ ] V4 底部「订单」Tab 展示空电池列表占位（无数据空状态）。
- [ ] V5 详情/退款/资料/筛选/归还等二级流程在三个列表中功能正常。
- [ ] V6 切换底部 Tab 后返回，之前选定的列表类型上下文不丢失。
- [ ] V7 demo-ops「订单状态/支付方式/支付状态/报失/取消前状态」面板在三个列表详情页可用。
- [ ] V8 冻结内容零改动。

## 九、落地顺序（切 Design 模式后）

1. 首页新增「订单管理」模块（三入口 + 数量 + 权限）。
2. 订单列表适配「类型锁定 + 隐藏类型 Tab + 文案区分」；底部订单 Tab 切占位空列表。
3. 自检：布局不溢出、空状态/交互态对比度、demo-ops 联动、权限开关表现。

## 下一步（Next step）

请审阅本计划：确认或修改「开放问题 O1/O2/O3」，其余按默认执行。确认后告诉我「按计划生成」，我切换到 Design 模式基于当前 `index.html`（V1.61）迭代出 V1.62。