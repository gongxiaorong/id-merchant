# 登录模块 PRD（产品版）

> 由当前原型 `index.html` 登录全屏层倒推，覆盖国际区号、多登录方式与表单状态校验。  
> 退出登录入口见 `我的-PRD.md` 系统设置。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App |
| 模块 | 全屏登录（非 Tab） |
| 原型 | `index.html` · `showLogin`（默认 `true`，打开即登录页） |
| 状态 | P0 已落地（原型简化：非空 + 勾选协议即可登录，无格式/凭证校验） |

---

## 2. 目标

| 目标 | 说明 |
|---|---|
| 合规 | 未勾选隐私协议不可登录、不可发码 |
| 多方式 | 手机 / 邮箱 / 账号；手机与邮箱支持密码与验证码 |
| 跨国 | 手机号支持国际区号选择与搜索 |
| 状态清晰 | 按钮置灰/高亮规则可预期 |

---

## 3. 信息架构

```
登录主页
├─ 区号选择（搜索 + 列表）
├─ 切换语言
├─ 手机号登录 ─┬─ 密码
│              └─ 验证码
├─ 邮箱登录   ─┬─ 密码
│              └─ 验证码
└─ 账号 + 密码登录
```

---

## 4. 公共能力

| 能力 | 规则 |
|---|---|
| 顶栏 | 无返回；右语言 chip（地球图标 + 完整语种名 + 下拉箭头）；语种清单：简体中文 / 繁體中文（中國香港） / English / Bahasa Indonesia |
| 多语言 | 方案 1：外置 `locales/{zh-CN,zh-HK,en,id}.json` 按需 `fetch`；`t(key)` 热切换 P0/P1 壳文案；缺 key 回退 zh-CN；演示数据/权限码中文可不翻 |
| 品牌 | 中部品牌色圆角 Logo（摩托图标），无产品名/登录方式副标题 |
| 页面底 | 全屏 canvas `#F6F7F9`（与 Tab 内二级页一致） |
| 表单卡 | 主表单包在 `.form-card`：白底 `#fff` + 1px `#E5E5E5`、12px 圆角、内边距 16px；字段 label 在卡内 `.form-label` |
| 输入框 | 与区号控件共用 `.login-field`：浅灰底 `#F7F8FA` + 1px `#EBEDF0`、12px 圆角、高 48px；内层 input 无独立边框；有内容展示清除；**禁止**输入直接贴 canvas（须经白卡） |
| 区号触发 | 独立同款圆角框（与手机号输入并排 gap），仅 +XX + 下拉箭头，无国旗/国家代码；进入区号列表 |
| 协议 | 未勾选空心框；勾选后橙色对勾底；《用户隐私政策》橙色高亮可点 |
| 字段标签 | 每个输入框上方展示 label（手机号 / 邮箱 / 账号 / 密码 / 验证码） |
| 密码显隐 | 眼睛图标切换明文/密文 |
| 验证码 | 手机/邮箱非空且已勾协议可点（蓝字浅蓝底）；发送后 60s 倒计时；禁用灰态 |
| 登录按钮 | 条件不满足灰禁用；满足后深蓝可点 |
| 方式切换 | 主切换「验证码/密码登录」accent 高亮；次级入口带图标（邮箱/账号/手机） |

---

## 5. 登录方式

### 5.1 手机号

- 区号 + 手机号（原型仅非空校验，不校验位数/号段）  
- 子模式：密码 / 验证码，可互切  
- 可跳转：邮箱登录、账号登录  

### 5.2 邮箱

- 邮箱非空即可（原型不校验格式）  
- 子模式：密码 / 验证码  
- 可跳转：手机号、账号  

### 5.3 账号密码

- 自定义账号 + 密码（工号等），均非空即可  
- 可跳转：手机号、邮箱  

### 5.4 区号页（全局复用）

- 登录、绑定手机号、账号新增/编辑、客服编辑共用同一区号页（`showDialPicker`）  
- 来源：`login` / `bind` / `account` / `cs`  
- 搜索国家名 / 区号  
- 列表：国旗、名称、区号；当前项浅蓝底 + 描边 + 文字高亮（无单选圆点）  
- 点击任一项：按来源回填 `loginDial` / `bpArea` / `fa.code` / `fc.code` 并关闭 

---

## 6. 按钮状态

| 控件 | 可点条件 |
|---|---|
| 获取验证码 | 对应账号字段非空 + 已勾选协议 + 非倒计时 |
| 登录 | 必填字段非空 + 已勾选协议 |

置灰：输入为空 / 未勾协议 / 验证码冷却中。  
**原型阶段不做**：手机号/邮箱格式校验、验证码长度校验、固定演示账号鉴权。

---

## 7. 流程

1. 设置 → 退出登录 → 确认 → 进入登录  
2. 选方式 → 填表（任意非空即可）→ 勾选协议 →（可选发码）→ 登录  
3. 条件满足即进首页（无凭证错误态）  

---

## 8. 验收

- [ ] 未勾选协议：登录与发码置灰  
- [ ] 五种表单路径可互切  
- [ ] 区号搜索回填  
- [ ] 密码显隐、清除  
- [ ] 验证码 60s 限流  
- [ ] 必填非空 + 已勾协议：登录成功进首页（不校验格式/凭证）  
- [ ] 打开应用默认进入登录页（`showLogin=true`）  
- [ ] 区号与手机号为并排同款独立圆角框（浅灰底 + 1px `#EBEDF0`），高度一致  
- [ ] 其它输入框边框与区号控件一致，内层无加粗/光环  
- [ ] 登录页 canvas 为 `#F6F7F9`；主表单包在白卡 `.form-card` 内  
- [ ] 区号触发仅展示 +区号（无国旗/国家代码）  
- [ ] 登录主页无返回按钮  
- [ ] 协议勾选橙色、政策名橙色高亮  
- [ ] 登录按钮灰/深蓝双态  
- [ ] 次级登录入口带图标  
- [ ] 切换语言后登录表单 label / 按钮 / 协议 / 区号页标题随 `locales/*.json` 更新（P0/P1）  
- [ ] 非简体语种加载失败时回退中文包，界面不空白  

---

## 9. 本期不做

- 手机号/邮箱格式与号段校验（真机对接时补）  
- 真实短信/邮件通道与风控  
- 注册、找回密码  
- 第三方 OAuth  
- 生物识别  
- 固定演示账号鉴权  

---

## 10. 关联

| 文档 | 关系 |
|---|---|
| `我的-PRD.md` | 退出登录入口与系统设置语言；设置页与登录共用 `appLang` + 语言包 |
| `locales/*.json` | P0/P1 文案真源（方案 1 外置包） |
| 其它 Tab | 登录成功后可访问；未登录全屏拦截 |
