# 车辆列表 · 地图视图 PRD（产品版）

> 本文从现有商户端「车辆」能力与**多轮沟通后已落地的原型**倒推整理，面向产品、业务、设计、研发对齐「为什么做 / 做什么 / 做到什么算完成」。  
> 交互与视觉以当前线框原型 `index.html`（车辆 Tab · 列表/地图 · FAB · 选中底卡）为唯一产品参照；本文不写接口字段与技术实现细节。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 车辆列表 · 地图视图 |
| 文档类型 | 产品需求说明（由现状与已确认决策倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（车辆 Tab：列表 / 地图、FAB 组、选中底卡） |
| 状态 | P0 已对齐并落地原型；本文已按多轮迭代同步 |
| 关联 | 首页「车辆运营」在线/离线钻取；车辆详情「查看车辆位置 / 历史轨迹」见 `车辆-PRD.md`（已落地，非二期） |
| 修订 | 相对初版：去掉次 FAB 无定位角标；明确离线上图；底卡恢复精简布局并统一「最后信号时间」 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

商户管车主要在 **车辆 Tab**：搜车架号、按状态 Pill、筛在线/型号/报失，点进详情做车控。

列表擅长：

- 精确找某一 VIN  
- 扫状态、电量、最后信号时间  
- 批量心智（有多少台、筛完剩多少）

但真实运维还经常要问：

1. **车都在哪一片？** —— 调度、找附近车、看离线是否扎堆  
2. **这台离线车最后落在哪？** —— 决定要不要派人  
3. **报失车在地图上能不能一眼抓住？** —— 比在长列表里翻更快  

现状若只有列表，没有空间维度；「看分布 / 找位置」要靠想象或跳到别的系统。

### 2.2 核心痛点（倒推）

| # | 痛点 | 业务影响 |
|---|---|---|
| 1 | **只有列表，没有空间视图** | 无法快速理解车队地理分布，调度靠经验 |
| 2 | **筛选结果和「在地图上看到的」容易变成两套** | 若地图另开一页、另套条件，运维会懵：筛了离线为何图上还有在线 |
| 3 | **部分车没有有效定位** | 若静默丢点，用户以为「车少了」；需要在地图内说明「少了几台」 |
| 4 | **入口占顶栏会挤搜索/筛选** | 列表本就信息密；切换又必须好找 → 已定为 FAB 组 |
| 5 | **预览卡与加车/切换按钮抢右下角** | 通栏底卡压住 FAB → 已定为方案 D（选中时 FAB 上移） |
| 6 | **若把「离线」理解成「不上图」** | 运维最需要看的「最后落点」反而消失 |

### 2.3 产品机会

在**不改底部导航、不新开「地图」一级 Tab、不重做车辆详情与车控**的前提下，给车辆列表增加**同一套筛选结果的第二呈现——地图视图**，让商户在手机上完成「筛车 → 看落点（含离线最后位置）→ 点车预览 → 进详情处置」。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **同一结果集** | 列表与地图共用搜索、状态 Pill、筛选面板；只换呈现，不换数据口径 |
| **空间可扫** | 有最后有效坐标的车在图上可见；**在线 / 离线同一上图规则**；报失可区分 |
| **少一步进详情** | 点标记先看摘要卡，再决定是否进车辆详情 |
| **无定位说清楚** | 无坐标车不进图；地图模式用**顶条**说明「有 N 辆仅列表可见」（**不做**次 FAB 角标） |
| **入口不抢主路径** | 新增车辆仍是主 FAB；列表↔地图用次 FAB，不挤顶栏 |
| **控件不打架** | 选中底卡贴底时，FAB 上移到卡上方，始终可点 |

### 3.2 成功怎么量（产品口径）

- 运维能在**不重置筛选**的情况下在列表与地图间来回切换  
- 地图上车辆集合 = 当前筛选结果中**有有效坐标**的子集（**含离线**）  
- 无定位数量在**地图模式**可被感知（顶条）；列表模式次 FAB **无数字角标**  
- 从「看到某点」到「打开该车详情」≤ 2 次点击（点标记 → 点底卡/查看详情）  
- 选中车辆时仍能点到「切回列表」与「新增」  
- 筛「离线」后，有最后定位的离线车**全部出现在图上**

### 3.3 本期不做（明确边界）

| 不做 | 说明 |
|---|---|
| 真地图 SDK 选型与瓦片商用策略 | 原型为示意底图；研发可替换，产品口径不变 |
| 标记聚合（zoom 低时数字簇） | P2；车少时可先全散点 |
| 地图上直接开锁 / 设防 / 寻车 | 避免误触；车控只在详情 |
| 路径规划、围栏、轨迹回放 | 非本模块 |
| 车辆详情「查看车辆位置」与列表地图打通聚焦 | 二期 |
| 按地理围栏筛选、画圈找车 | 二期 |
| 强制从首页在线/离线进入时打开地图 | 默认保持列表或沿用会话记忆即可 |
| 次 FAB 无定位数量角标 | **已明确不做**（曾讨论过，落地后移除） |

---

## 4. 用户与使用场景

### 4.1 主用户

**商户侧运营 / 运维**（门店或中心化车队管理员）

- 设备：手机 App  
- 动机：快速定位车在哪、谁该派人、报失是否显眼  
- 能力预期：会筛列表、会捏合缩放；不配置 GIS  

### 4.2 关键场景

**场景 A｜筛完离线，上图看最后落点**  
车辆列表筛「离线」→ 点次 FAB 进地图 → 图上展示**有最后有效坐标的离线车**（橙点）→ 点一台看摘要（含最后信号时间）→ 进详情寻车/处理。  
*无坐标的离线车不在图上，若 N>0 则地图顶条提示。*

**场景 B｜有车没点，在地图里知道「少了几台」**  
筛选结果 10 台，其中 3 台无定位 → 进地图顶条「3 辆无定位，仅列表可见」→ 切回列表处理无点车。  
*列表态次 FAB 不展示数字角标。*

**场景 C｜报失车一眼抓住**  
地图上已报失用红色强调（优先于在线/离线色）→ 点开底卡确认 VIN 与状态 → 进详情做解除报失等操作。

**场景 D｜看分布不改筛选**  
先按「租用中」Pill 扫列表，再切地图看这些在租车是否扎堆；切回列表时 Pill/搜索条件都还在。

**场景 E｜点错标记**  
点空白地图取消选中 → 底卡收起 → FAB 落回默认位置。

**场景 F｜从首页钻车辆**  
首页「车辆运营」点在线/离线 → 进车辆 Tab 并带上对应在线筛选 → **不强制切地图**（视图默认列表或沿用上次）；需要时再点 FAB 上图。

---

## 5. 范围与边界

### 5.1 在范围内（In Scope · P0）

1. 车辆 Tab 内 **列表 / 地图** 双视图  
2. **次 FAB** 切换视图 + **主 FAB** 新增车辆  
3. 地图只展示当前 `筛选结果 ∩ 有有效坐标`（**不因离线排除**）  
4. 标记颜色：在线绿 / 离线橙 / 报失红（报失优先）  
5. 点标记 → **底部摘要卡** → 进入车辆详情  
6. **无定位数量**仅在地图模式用**顶条**提示（无次 FAB 角标）  
7. 选中时底卡贴底、**FAB 上移避让**（方案 D）  
8. 切换视图不重置搜索 / Pill / 筛选；切换时清空地图选中  

### 5.2 明确不在范围内

- 新的一级导航「地图」  
- 与告警中心地图化（告警仍走告警详情 → 车辆）  
- 多选车辆、地图批量操作  
- 离线地图包、室内图  
- 次 FAB 上的无定位数字角标  

### 5.3 与相邻模块的边界

| 模块 | 关系 |
|---|---|
| **车辆列表** | 地图是其第二视图，不是独立业务域 |
| **车辆详情** | 地图只负责发现与预览；车控、编辑、报失在详情 |
| **首页车辆运营** | 只负责跳转列表 + 在线/离线筛选，不负责打开地图 |
| **告警「查看车辆」** | 进车辆详情，不直接落地图（可二期） |
| **详情「查看车辆位置」** | 可二期复用同底图并聚焦该 VIN |

---

## 6. 核心概念（产品语言）

### 6.1 两种呈现，一套结果

| 概念 | 含义 |
|---|---|
| **筛选结果** | 搜索 + Pill + 筛选面板之后的车辆集合（与现网列表一致） |
| **列表可见集** | = 筛选结果（含无定位车） |
| **地图可见集** | = 筛选结果中具备有效经纬度的车辆（**含离线、含报失**） |
| **无定位数 N** | 筛选结果中无法打点的车辆数 |

用户心智一句话：**「我筛的是谁，列表全给；地图给有最后位置的（在线离线都算），并在地图上告诉我少了几个。」**

### 6.2 定位含义（关键拍板）

| 概念 | 产品含义 |
|---|---|
| **地图上的点** | 该车**最后有效位置**，不承诺实时直播 |
| **上图条件** | **仅看是否有有效坐标**；**是否在线不是上图条件** |
| **在线 + 有坐标** | 绿点；点位可理解为较新的最后位置 |
| **离线 + 有坐标** | **必须上图**（橙点）；点位 = **最后定位**，供派人/研判 |
| **无有效坐标** | 从未上报或当前无可用坐标 → **不进图**（与是否在线无关） |
| **已报失 + 有坐标** | 红点优先；仍可上图 |

### 6.3 视图状态

| 状态 | 说明 |
|---|---|
| `list` | 列表模式（默认） |
| `map` | 地图模式 |
| 会话记忆 | P0 原型可会话内记住；是否持久化到本地由研发评估，产品希望「进 Tab 别每次强制回列表」为佳 |

---

## 7. 业务规则

### 7.1 数据与筛选

| # | 规则 |
|---|---|
| R1 | 列表与地图**必须**共用同一筛选结果，禁止地图另维护一套过滤 |
| R2 | 切换 list ↔ map **不得**清空搜索词、Pill、筛选面板条件 |
| R3 | 切换视图时 **取消**当前地图选中（底卡关闭） |
| R4 | 筛选结果变化后：若原选中车已不在结果中，自动取消选中 |

### 7.2 打点规则

| # | 规则 |
|---|---|
| R5 | 仅 `地图可见集` 打点；**是否在线不作为上图条件** |
| R5a | **离线车辆若有最后有效坐标，必须上图**；点位含义为最后定位（非实时保证） |
| R6 | 标记色：报失优先红；否则在线绿 / 离线橙（与列表状态点语义一致） |
| R7 | 选中：主色描边 + 略放大；同时只允许一辆选中 |
| R8 | 点击地图空白：取消选中 |

### 7.3 无定位提示

| # | 规则 |
|---|---|
| R9 | N = 0：不展示地图顶条 |
| R10 | **不做**列表模式次 FAB 数字角标（已拍板移除） |
| R11 | 地图模式且筛选结果非空且 N > 0：顶部轻提示「N 辆无定位，仅列表可见」（N>99 显示 99+） |
| R12 | 筛选结果非空但地图可见集为空：地图空态「当前车辆暂无位置信息」，引导切回列表 |
| R13 | 筛选结果为空：与列表一致的「暂无车辆」空态（含有筛选时的调整提示） |

### 7.4 入口与 FAB

| # | 规则 |
|---|---|
| R14 | **主 FAB（+）**：始终「新增车辆」，列表/地图模式都在 |
| R15 | **次 FAB**：列表态图标为地图；地图态图标为列表；点击切换；**无数量角标** |
| R16 | 次 FAB 在主 FAB **正上方**，组成 FAB 组，不进顶栏 |
| R17 | 进入车辆详情 / 新增 / 筛选等上层页时，FAB 组随列表页被覆盖（与现网 + 行为一致） |

### 7.5 底卡与避让（方案 D）

| # | 规则 |
|---|---|
| R18 | 未选中：不展示底卡；FAB 在默认右下安全区 |
| R19 | 选中：底卡贴在底部 Tab 上方，通栏展示摘要 |
| R20 | 选中时 FAB 组整体上移至底卡顶部之上，保持可点；取消选中后落回默认（带短过渡） |
| R21 | 点底卡（或「查看详情」）→ 进入该车车辆详情 |
| R22 | 地图上**不提供**开锁/设防/寻车等车控 |

### 7.6 与首页联动

| # | 规则 |
|---|---|
| R23 | 首页在线/离线卡片 → 车辆 Tab + 对应在线筛选；**视图不强制为地图** |
| R24 | 若产品启用视图记忆：进入后保持用户上次的 list/map；否则默认 list 更稳 |

---

## 8. 功能需求（按用户旅程）

### 8.1 切换视图

| 需求 | 描述 | 优先级 |
|---|---|---|
| F1 | 用户可通过次 FAB 在列表与地图间切换 | P0 |
| F2 | 切换不丢失筛选上下文 | P0 |
| F3 | 次 FAB **不**展示无定位数量角标 | P0 |
| F4 | 主 FAB 在任意视图可新增车辆 | P0 |

### 8.2 地图浏览

| 需求 | 描述 | 优先级 |
|---|---|---|
| F5 | 展示当前地图可见集的标记（含离线有坐标车） | P0 |
| F6 | 支持拖动、缩放（真图阶段）；原型示意即可 | P0 |
| F7 | 默认视野覆盖当前所有可见点（fit）；单点时合理缩放 | P1 |
| F8 | 低缩放聚合 | P2 |

### 8.3 选中与预览

| 需求 | 描述 | 优先级 |
|---|---|---|
| F9 | 点标记打开底部摘要卡 | P0 |
| F10 | 底卡字段（与列表信息密度对齐的**精简版**）：车架号；在线/离线 · 运营状态；已报失（若有）；主/副电量；位置文案（有则展示，无则「最后位置未知」）；**最后信号时间**；查看详情 | P0 |
| F11 | 点底卡进入车辆详情 | P0 |
| F12 | 选中时 FAB 上移避让底卡 | P0 |
| F13 | 点空白取消选中 | P0 |

> **底卡不做**：单独「最后定位」大段分行、与列表不一致的第二套时间语义。时间字段与列表/详情统一为 **「最后信号时间」**。

### 8.4 空态与异常

| 需求 | 描述 | 优先级 |
|---|---|---|
| F14 | 无筛选结果：暂无车辆 + 引导清除筛选或新增 | P0 |
| F15 | 有结果全无坐标：暂无位置信息 + 引导列表 | P0 |
| F16 | 部分无坐标：地图顶条提示 N | P0 |

### 8.5 后续增强（非 P0）

| 需求 | 描述 | 优先级 |
|---|---|---|
| F17 | 详情「查看车辆位置」打开同能力地图并聚焦该车 | P1 |
| F18 | 告警/订单跳转车辆时可带「打开地图并选中」 | P2 |
| F19 | 地图模式顶栏快捷「仅看报失」 | P2 |

---

## 9. 信息架构与界面结构

### 9.1 页面结构（车辆 Tab · 无下层页时）

```
┌─────────────────────────────────────┐
│ 搜索车架号                    [筛选] │  ← 列表/地图共用
│ Pill：全部 / 空闲 / 占用中 / 租用中   │  ← 共用
├─────────────────────────────────────┤
│  【列表】卡片流                       │
│  或【地图】打点区                     │
│     （顶条：N 辆无定位… · 仅地图态）   │
│     （底卡：选中时贴底 Tab 之上）     │
│                          [列表|地图] │  ← 次 FAB（无角标）
│                          [   +    ] │  ← 主 FAB
├─────────────────────────────────────┤
│           底部主导航 Tab              │
└─────────────────────────────────────┘
```

### 9.2 列表模式 vs 地图模式

| 区域 | 列表 | 地图 |
|---|---|---|
| 搜索 / Pill / 筛选 | ✅ | ✅ 同 |
| 主内容 | 车辆卡片列表 | 地图 + 标记 |
| 次 FAB 角标 | **无** | **无** |
| 地图顶条 | — | N>0 显示 |
| 选中底卡 | — | 选中时显示 |
| 主 FAB | 新增 | 新增 |
| 次 FAB | 图标=地图 | 图标=列表 |

### 9.3 底卡信息结构（已定稿 · 与原型一致）

```
车架号：{VIN}
在线|离线 · {运营状态}          [已报失]
主电 xx% · 副电 xx%             {位置文案}
最后信号时间：{time}            查看详情 ›
```

优先级：

1. **是谁**：车架号  
2. **现在怎样**：在线/离线 · 运营状态 · 报失  
3. **能不能跑**：主/副电量  
4. **在哪 / 何时**：位置短文案 + **最后信号时间**（与列表用语一致）  
5. **下一步**：查看详情  

不在底卡做车控，避免与详情能力重复和误触。

### 9.4 标记图例（产品说明用，界面可不单独做图例页）

| 颜色 | 含义 |
|---|---|
| 绿 | 在线（且未报失） |
| 橙 | 离线（且未报失）— **有最后坐标仍展示** |
| 红 | 已报失（优先） |
| 主色描边放大 | 当前选中 |

---

## 10. 关键路径（产品流程）

### 10.1 列表 → 地图 → 详情

```
车辆 Tab（列表）
  → 搜索 / Pill / 筛选（可选）
  → 次 FAB「地图」
  → 浏览标记 / 读无定位顶条（若有）
  → 点标记 → 底卡
  → 查看详情 → 车辆详情（车控）
  → 返回仍在地图 + 原筛选（选中建议清空，筛选保持）
```

### 10.2 地图 → 列表

```
地图 → 次 FAB「列表」→ 同一筛选下的完整列表（含无定位车）
```

### 10.3 与首页

```
首页「在线车辆 / 离线车辆」
  → 车辆 Tab + 对应在线筛选
  → 默认列表（或记忆视图）
  → 用户按需再进地图（离线筛选 + 地图 = 看最后落点分布）
```

---

## 11. 验收清单（产品验收）

### 11.1 必须通过

- [ ] 列表与地图筛选结果口径一致（改搜索/Pill/筛选后两边同步）  
- [ ] 切换视图不丢失筛选条件  
- [ ] 无坐标车不出现在地图上  
- [ ] **有坐标的离线车出现在地图上**（橙点，除非已报失则红）  
- [ ] N>0 时：地图有顶条文案「N 辆无定位，仅列表可见」  
- [ ] N>0 时：列表次 FAB **无**数字角标  
- [ ] N=0 时无地图顶条  
- [ ] 全无坐标时出现「当前车辆暂无位置信息」类空态  
- [ ] 点标记出底卡；点空白收起；点底卡进详情  
- [ ] 底卡时间文案为 **「最后信号时间」**（与列表/详情一致）  
- [ ] 报失 / 在线 / 离线标记可区分  
- [ ] 选中时 FAB 不被底卡遮挡，可切换视图、可新增  
- [ ] 地图上无开锁/设防/寻车入口  
- [ ] 主 FAB 始终为新增；次 FAB 只负责视图切换  
- [ ] 首页在线/离线进入后带上筛选，且不破坏上述规则  

### 11.2 体验建议（不阻塞 P0）

- [ ] 切换/FAB 上移有短过渡，不闪跳  
- [ ] 多点时默认视野尽量看到全部可见点  
- [ ] 底卡长 VIN 不溢出、不与安全区冲突  

---

## 12. 版本规划建议

| 版本 | 内容 |
|---|---|
| **P0（本期 · 已原型）** | 双视图 + FAB 切换 + 打点色（含离线上图）+ 底卡进详情 + 地图顶条无定位提示 + 底卡与 FAB 避让（D）+ 底卡「最后信号时间」 |
| **P1** | 真地图 SDK；fit 视野；详情「车辆位置」聚焦；视图偏好持久化 |
| **P2** | 聚合；告警/订单深链到地图选中；围栏/画圈等高级能力 |

---

## 13. 待决问题（产品）

| # | 问题 | 倾向 | 影响 |
|---|---|---|---|
| Q1 | 进入车辆 Tab 是否**持久记住** list/map？ | 会话记住即可；跨天默认可 list | 存储与预期 |
| Q2 | 从详情返回地图时，是否**保持选中**该车？ | 可清空选中，筛选保持 | 实现复杂度 |
| Q3 | 定位「过时」是否在底卡单独文案？ | P1：可与告警「定位过时」对齐 | 文案与数据 |
| Q4 | ~~无定位角标用红点还是灰点？~~ | **已关闭**：不做次 FAB 角标 | — |
| Q5 | 首页钻取是否允许「长按进地图」？ | 本期不做，避免双入口 | 范围 |

---

## 14. 已确认决策追溯（倒推来源）

| 决策 | 结论 | 来源 |
|---|---|---|
| 地图定位 | 车辆列表第二视图，非独立 Tab | 方案讨论 |
| 数据口径 | 与列表共用 filtered | 方案讨论 + 原型 |
| 切换入口 | **FAB 组**：次 FAB 切换，主 FAB 新增；不采用顶栏分段 | 用户指定 |
| 无定位提示 | **仅地图顶条**；**移除**次 FAB 数量角标 | 用户指定（多轮后定稿） |
| 离线与上图 | **有最后有效坐标即上图**，不因离线排除 | 用户明确要求 + 原型 |
| 点位语义 | 最后有效位置，非实时直播 | 方案讨论 |
| 车控 | 地图不做，只进详情 | 方案讨论 |
| 底卡与 FAB | **方案 D**：卡贴底，选中时 FAB 上移 | 用户指定 |
| 底卡布局 | 精简版（车架号 / 状态 / 电量 / 位置短文案 / 最后信号时间 / 详情）；不做「最后定位」分行加长版 | 用户指定恢复上一版 |
| 时间文案 | 统一 **「最后信号时间」**（列表 · 底卡 · 详情） | 用户指定 |
| 标记色 | 在线绿 / 离线橙 / 报失红 | 方案 + 原型 |
| 首页联动 | 带筛选进列表，不强制地图 | 方案讨论 |

---

## 15. 与初版 PRD 的差异摘要（便于评审）

| 项 | 初版 | 现版（对齐落地） |
|---|---|---|
| 次 FAB 无定位角标 | 列表态 N>0 显示 | **不做** |
| 无定位提示 | 角标 + 地图顶条 | **仅地图顶条** |
| 离线上图 | 规则有，强调不足 | **R5a 强制**；验收单列 |
| 底卡字段 | 一度含「最后定位」分行 | **恢复精简布局** |
| 时间用语 | 「最后定位时间」等混用 | **统一「最后信号时间」** |

---

## 16. Next step

1. 产品 / 业务审阅本文第 3、5、6.2、7、11 节，确认边界与验收（尤其离线上图、无角标）。  
2. 关闭或拍板第 13 节待决项（尤其 Q1/Q2）。  
3. 研发按 P0 对照原型拆任务：视图状态、地图可见集（含离线）、FAB、底卡避让、地图顶条空态。  
4. 设计出图时保持与车辆列表、FAB、底 Tab 的现有视觉体系一致；底卡字段以 §9.3 为准。  
5. 确认后可进入研发实现；原型对照文件：`index.html` 车辆 Tab。

---

*文档结束。*
