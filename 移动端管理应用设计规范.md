# 移动端管理应用设计规范

> 适用于：电动车租赁 / 车队管理 / 商户后台等移动端管理类 App 原型设计。
> 基于实际项目提炼，可直接作为新项目的设计基线。

---

## 一、技术栈选型

### 1.1 框架与核心依赖

| 层 | 选型 | 版本 | 引用方式 | 用途 |
|---|---|---|---|---|
| **JS 框架** | Vue 3 | `vue@3` UMD prod | CDN（jsdelivr + unpkg 双源） | Composition API，`createApp` + `setup()` |
| **CSS 引擎** | UnoCSS Runtime | `@unocss/runtime` | CDN，运行时 JIT | 类 Tailwind 方括号语法（`w-[393px]`、`text-[15px]`） |
| **图标库** | Lucide | `lucide@0.344.0` UMD | CDN | 通过 `luci(name, cls)` 函数按需内联 SVG |
| **状态管理** | 无 Pinia/Vuex | — | — | 使用 `ref` / `computed` 局部状态，单根组件集中托管 |
| **路由** | 无 vue-router | — | — | 状态机式切换：`tab` + `*OV`（overlay）+ `*Sub`（二级页） |
| **动画** | Vue `<transition>` | 内置 | — | `fd`（fade）、`sl`（slide-right）、`sh`（sheet-up）等 |
| **构建** | 无构建步骤 | — | — | 零打包，纯 CDN + 内联 `<style>` / `<script>` |

### 1.2 架构形态

- **单文件交付**：一个 `index.html` 包含全部代码，浏览器直开，零依赖。
- **单根组件**：一个 `createApp` 实例，所有视图通过 `v-if` 在设备模拟框内叠加渲染。
- **Mock 数据**：所有数据为 `ref` 持有的前端 mock（如 `ALERTS_SEED`），无后端调用。
- **权限演示**：前端 RBAC，`can(code)` / `deny()` / `enforceTabPerms()`，侧栏实时切换权限码。

### 1.3 交互实现

| 场景 | 实现方式 |
|---|---|
| 二级页进入/返回 | `<transition name="sl">` 右滑入 + 自定义指令 `v-swipe-close` 左滑返回 |
| 底部 sheet | `<transition name="sh">` 上滑入 + 遮罩 fade |
| Toast 通知 | 顶部居中浮起，`backdrop-filter: blur(10px)`，2s 自动消失 |
| 按压反馈 | `active:opacity-70` / `active:scale-95` / `active:bg-[#F6F7F9]` |
| 滚轮选择器 | `track-wheel-col` scroll snap |

---

## 二、色彩体系

### 2.1 基础调色板

| 角色 | 色值 | 用途 |
|---|---|---|
| **外部舞台背景** | `#E8EAED` | 桌面端 iPhone 外部灰底 |
| **App 内底色** | `#F6F7F9` | App 内容区域底色 |
| **卡片/弹窗** | `#FFFFFF` | 卡片、弹窗、列表项背景 |
| **主文字** | `#111111` | 标题、正文主文字 |
| **次要文字** | `#6B6B6B` | 标签、描述文字 |
| **占位/禁用** | `#9CA3AF` / `#999` | 占位符、禁用态 |
| **边框/分割线** | `#E5E5E5` | 卡片边框、列表分割线 |

### 2.2 强调色与状态色

| 角色 | 色值 | 用途 |
|---|---|---|
| **强调色（Primary）** | `#2F6FEB`（钴蓝） | 主 CTA、激活态、链接、选中 Tab、焦点框 |
| **成功** | `#17A34A` | 在线状态、正向操作 |
| **警告** | `#EAB308` / `#F59E0B` | 警告 chip、待处理提示 |
| **危险** | `#DC2626` | 离线状态、待处理告警、删除 |
| **局部品牌色** | `#FF6A00`（登录协议）/ `#E67E22`（导航去车） | 仅特定场景使用 |

### 2.3 色彩使用规则

1. **强调色克制**：`#2F6FEB` 全局每屏约 1–2 处（按钮 fill + 选中态 + 链接）。
2. **状态色 chip 模式**：浅底深字呈现，如 `bg-[#ECFDF3] text-[#17A34A]`。
3. **禁止渐变**：除强调色 hover 微调外，不使用任何渐变背景。
4. **不使用纯黑/纯白背景**：背景始终为 `#F6F7F9` 或 `#E8EAED`，卡片为 `#FFFFFF`。

### 2.4 CSS 变量绑定（推荐用于多项目复用）

```css
:root {
  /* Surface */
  --bg-stage: #E8EAED;
  --bg-app: #F6F7F9;
  --surface: #FFFFFF;

  /* Foreground */
  --fg: #111111;
  --muted: #6B6B6B;
  --placeholder: #9CA3AF;

  /* Border */
  --border: #E5E5E5;

  /* Accent */
  --accent: #2F6FEB;
  --accent-on: #FFFFFF;

  /* Semantic */
  --success: #17A34A;
  --warn: #EAB308;
  --danger: #DC2626;

  /* 状态色浅底（chip 用） */
  --success-bg: #ECFDF3;
  --warn-bg: #FEF3C7;
  --danger-bg: #FEF2F2;
}
```

---

## 三、排版规范

### 3.1 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text', 'PingFang SC', system-ui, sans-serif;
```

苹果系统字体栈，确保 iOS 原生观感，中文回退到苹方。

### 3.2 字号层级

| 层级 | 字号 | 字重 | 用途 |
|---|---|---|---|
| 大数字 | 22px | 700 | 统计数字、价格 |
| 页面标题 | 18–20px | 600 | 二级页顶部标题 |
| 卡片标题 | 16px | 600 | 卡片标题、弹窗标题 |
| 正文 | 14–15px | 400 | 列表内容、表单值 |
| 辅助 | 13px | 400 | 描述文字 |
| 标签 | 10–12px | 500–600 | chip、标签、按钮文字 |

### 3.3 字重规则

- `400`：正文（默认）
- `500`：次要强调（标签、描述）
- `600`：按钮文字、卡片标题
- `700`：主按钮、大数字

### 3.4 数字排版

- 统计数字使用 `tabular-nums`（等宽数字），确保对齐。
- 价格/数量前可加货币符号或单位，字号比数字小一级。

---

## 四、圆角与阴影

### 4.1 圆角层级

| 元素 | 圆角 | UnoCSS 类 |
|---|---|---|
| 卡片 | 12px | `rounded-xl` |
| 按钮 | 12px | `rounded-xl` |
| chip / 标签 | 8px | `rounded-lg` |
| 头像 / 圆形控件 | 50% | `rounded-full` |
| 筛选胶囊 | 20px | `rounded-full` |
| 底部 sheet 顶部 | 20px | `rounded-t-[20px]` |
| 设备外框 | 44–55px | `rounded-[44px]` / `rounded-[55px]` |

### 4.2 阴影层级

| 级别 | 值 | 用途 |
|---|---|---|
| **卡片（微浮起）** | `0 1px 3px rgba(0,0,0,.04), 0 1px 2px rgba(0,0,0,.06)` | 默认卡片 |
| **浮层（sheet/弹窗）** | `0 2px 8px rgba(0,0,0,.08)` | 底部 sheet、modal |
| **无阴影** | — | 列表项、表单字段（用边框分隔） |

**规则**：默认不加阴影，用边框和留白分隔；仅卡片和浮层使用微弱阴影。

---

## 五、布局规范

### 5.1 设备容器

- **模拟设备**：iPhone 15 Pro 物理框
- **尺寸**：393 × 852px（视口）
- **外框圆角**：55px
- **Dynamic Island**：顶部药丸形刘海
- **桌面端布局**：iPhone 居中 + 右侧 `demo-ops` 侧栏

### 5.2 底部 Tab Bar

| 属性 | 值 |
|---|---|
| 高度 | 83px（含 22px safe-area 底部安全区） |
| 背景 | `backdrop-blur-[20px]` 半透明白 |
| Tab 数量 | 4 个（首页 / 车辆 / 订单 / 我的） |
| 选中态 | 钴蓝 `#2F6FEB` 图标 + 文字 |
| 未选中 | `#9CA3AF` 灰色 |

### 5.3 z-index 分层

| 层级 | z-index | 用途 |
|---|---|---|
| Tab 内容 | 100 | 底部 Tab 主内容 |
| 二级页 | 500 / 510 / 520 | 二级覆盖页 |
| 底部 sheet | 620+ | 筛选、操作 sheet |
| 全局弹窗 | 900+ | 确认弹窗、Toast |
| 登录页 | 1900 | 登录覆盖层 |

### 5.4 间距系统

- **基准单位**：4px
- **卡片内 padding**：16px（`p-4`）
- **列表行垂直 padding**：14px
- **卡片间距**：12px
- **段落间距**：16px / 24px

---

## 六、组件规范

### 6.1 表单

| 属性 | 值 |
|---|---|
| 容器 | 白卡（`form-card`） |
| 输入框底色 | `#F7F8FA`（`login-field`） |
| 边框 | 无边框，浅灰底区分 |
| 圆角 | 12px |
| focus 高亮 | 无（iOS 风格克制） |
| 标签 | 输入框上方，12px，`#6B6B6B` |

### 6.2 列表项

| 属性 | 值 |
|---|---|
| 背景 | `#FFFFFF` |
| 底部边框 | 1px `#E5E5E5` |
| 按压反馈 | `active:bg-[#F6F7F9]` |
| 内边距 | 左右 16px，上下 14px |

### 6.3 筛选 Pills

| 属性 | 值 |
|---|---|
| 形状 | 胶囊（`rounded-full`） |
| 默认态 | 白底 + 灰边 |
| 选中态 | 浅蓝底（`bg-[#EBF2FF]`）+ 蓝边（`border-[#2F6FEB]`）+ 蓝字 |
| 滚动 | 横向 `overflow-x-auto`，`flex-nowrap` |

### 6.4 弹窗类型

| 类型 | 场景 | 样式 |
|---|---|---|
| 底部 sheet | 筛选、操作菜单 | `rounded-t-[20px]`，遮罩 fade，上滑入 |
| 中央 modal | 确认弹窗 | `rounded-xl`，遮罩 fade，缩放进入 |
| 全屏覆盖页 | 详情、编辑 | `rounded-[44px]`，右滑入 |

### 6.5 状态标签（Chip）

浅底深字模式，圆角 8px，内边距 2px 8px：

```html
<!-- 成功 -->
<span class="bg-[#ECFDF3] text-[#17A34A] rounded-lg px-2 py-[2px] text-[11px] font-medium">在线</span>

<!-- 警告 -->
<span class="bg-[#FEF3C7] text-[#EAB308] rounded-lg px-2 py-[2px] text-[11px] font-medium">警告</span>

<!-- 危险 -->
<span class="bg-[#FEF2F2] text-[#DC2626] rounded-lg px-2 py-[2px] text-[11px] font-medium">离线</span>
```

### 6.6 空状态

- 圆形浅色图标背景（`bg-[#F3F4F6]`）+ Lucide 图标 + 文字描述。
- 克制不插画化，不使用手绘 SVG。

### 6.7 骨架屏

- `shimmer` 动画渐变占位，渐变方向 `linear-gradient(90deg, #f0f0f0, #e5e5e5, #f0f0f0)`。
- 用于数据加载等待态。

---

## 七、交互范式

### 7.1 页面切换动画

| 类型 | transition name | 效果 |
|---|---|---|
| 二级页进入 | `sl` | 从右侧滑入，duration 300ms |
| 二级页返回 | `sl` | 向右侧滑出 |
| 底部 sheet | `sh` | 从底部滑入，duration 250ms |
| Tab 切换 | `tab-fade` | 淡入淡出，duration 200ms |
| Toast | `toast` | 顶部浮起 + 淡入，duration 200ms |

### 7.2 手势

- **左滑返回**：自定义指令 `v-swipe-close`，检测 `touchstart` → `touchmove` → `touchend`，水平位移 > 80px 触发关闭。
- **下拉刷新**：`touchmove` 垂直位移检测，顶部下拉显示刷新指示器。

### 7.3 按压反馈

```css
/* 通用按压反馈 */
.btn { @apply active:opacity-70 transition-opacity; }
.card { @apply active:bg-[#F6F7F9] transition-colors; }
.icon-btn { @apply active:scale-95 transition-transform; }
```

---

## 八、原型辅助层（非产品 UI）

### 8.1 demo-ops 侧栏

- 登录后常驻右侧，仅桌面端显示。
- 功能：实时切换权限码、触发场景演示、查看版本信息。
- **不属于产品 UI**，仅用于原型评审演示。

### 8.2 data-od-id 标注

- 全量标注于页面、卡片、按钮、表单字段。
- 供 Open Design 评审模式定位元素。
- 命名规则：kebab-case，如 `data-od-id="vehicle-card-online"`。

---

## 九、CDN 引用清单

```html
<!-- Vue 3 -->
<script src="https://cdn.jsdelivr.net/npm/vue@3/dist/vue.global.prod.js"></script>
<script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>

<!-- UnoCSS Runtime -->
<script src="https://cdn.jsdelivr.net/npm/@unocss/runtime@0.65.0/dist/uno.global.js"></script>

<!-- Lucide Icons -->
<script src="https://cdn.jsdelivr.net/npm/lucide@0.344.0/dist/umd/lucide.min.js"></script>
```

---

## 十、复用 Checklist

新项目启动时，逐项确认：

- [ ] 引入 Vue 3 + UnoCSS Runtime + Lucide CDN
- [ ] 绑定 `:root` CSS 变量（色彩体系 §2.4）
- [ ] 设置字体栈（§3.1）
- [ ] 搭建 iPhone 设备外框（§5.1）
- [ ] 实现底部 Tab Bar（§5.2）
- [ ] 定义 z-index 分层（§5.3）
- [ ] 预置 transition 动画（§7.1）
- [ ] 实现 `v-swipe-close` 指令（§7.2）
- [ ] 封装 `luci(name, cls)` 图标函数
- [ ] 标注 `data-od-id`（§8.2）
- [ ] 部署 `demo-ops` 侧栏（§8.1，如需评审）

---

## 十一、版本冻结记录

### V1.1.1 — 2026-08-13

> **冻结状态：已冻结（Frozen）**
>
> 自本版本起，以下所有设计、代码、配置内容进入冻结状态，未经用户明确书面授权不得改动。

**冻结范围：**

1. 技术栈选型（Vue 3 + UnoCSS Runtime + Lucide CDN，零构建步骤）
2. 色彩体系（钴蓝 `#2F6FEB` 强调色 + 灰白底 + 三色状态语义）
3. 排版规范（苹果系统字体栈 + 字号/字重层级）
4. 圆角与阴影（12px 卡片圆角 + 极浅双层阴影）
5. 布局规范（iPhone 15 Pro 设备框 + 底部 Tab Bar + z-index 分层）
6. 组件规范（表单 / 列表项 / 筛选 / 弹窗 / Chip / 空状态 / 骨架屏）
7. 交互范式（右滑入页 + 底部 sheet + Toast + 按压反馈）
8. 原型辅助层（demo-ops 侧栏 + data-od-id 标注）
9. 页面结构（登录 + 四 Tab 全链路）
10. 业务流程（订单状态机、车辆管理、告警处理、权限 RBAC）

**冻结文件清单：**

- `index.html` — 原型主体
- `移动端管理应用设计规范.md` — 设计规范文档
- `首页-PRD.md` / `首页-告警管理-PRD.md`
- `车辆-PRD.md` / `车辆-地图视图-PRD.md`
- `订单-PRD.md` / `订单详情-联动规则计划.md`
- `我的-PRD.md`
- `权限字典.md`

**解冻条件：**

仅以下用户指令可解除冻结：

- 「重构」「全量调整」「整体优化」「整体改版」「全局重设计」

**版本迭代规则：**

- 基准版本：V1.0
- 当前版本：**V1.1.1**
- 后续迭代：V1.1.2、V1.1.3 ……依次递增
- 每次更新仅记录增量变更，禁止覆盖历史版本

---

### 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0 | — | 项目初始基准版本 |
| V1.1.0 | — | 登录 + 四 Tab 全链路 + 版本检测功能 |
| **V1.1.1** | **2026-08-13** | **设计规范文档输出，全量冻结** |
