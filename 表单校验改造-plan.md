# 表单校验改造计划

> 目标：将现有 `index.html` 的表单校验逻辑，对齐用户提供的 12 类字段校验规范（账号/手机号/邮箱/密码/确认密码/验证码/姓名/金额/排序/备注/天数/版本号），补齐缺失规则、修正错误文案与正则，并统一提示口径。

## 一、现状概览

当前校验分散在以下位置（均为 `index.html` 内的 Vue setup 作用域）：

| 位置 | 函数/字段 | 职责 |
|---|---|---|
| L4171–4172 | `canSendCode` / `canLogin` | 登录表单可用性（仅非空判断） |
| L4184 | `doLogin` | 登录提交 |
| L4063–4066 | `canConfirmPwd` | 修改密码可用性（8–32 位 + 两次一致） |
| L4092–4101 | `nextPwdVerify` / `doResetPwd` | 修改密码提交流程 |
| L4241 | `accValid(isEdit)` | 账号新增/编辑校验 |
| L4195 / 4204 / 4217 / 4219 | `saveModel` / `saveBattModel` / `submitPackageAdd` / `savePackage` | 车型/电池型号/套餐 价格·押金·排序 |
| L4267 / 4273 | `savePos` / `saveCs` | 职位/客服 名称·电话 |
| L4316 | `cfg`（pre/breach） | 订单配置·违约提前/天数 |
| L2922–2923 | `vErr` / `vClr` | 通用非空校验 / 清除单字段错误 |
| L3998–4008 | `latestVersion` 等 | 版本检测（硬编码 demo，无用户输入） |

现状特点：**非空校验基本只有登录/账号模块有，格式校验零散、多为「手写正则 + 内联 message」，缺统一校验器；金额/排序/天数只有 `min` 或「非负」级校验，无范围与小数位校验。**

## 二、逐字段改造清单（现状 → 目标）

> 说明：规范原文存在个别转写噪声（如 `\d {6}` 应为 `\d{6}`、`\d {0,9}` 应为 `\d{0,9}`、`(.\d{1,2})` 应为 `(\.\d{1,2})`、`(\d+.){2}\d+` 应为 `(\d+\.){2}\d+`、`[!#-&'()(-~]` 实为「可打印 ASCII `!`–`~`」）。**字段提示文案以「三、最终文案口径」为准，本节仅记录校验逻辑与正则映射。**

### 1. 账号（account）✅ 注册 4–20
- 现状：`accValid` 仅 `^[a-zA-Z]` 开头 + `length>20`，文案「账号必须以字母开头，长度为1-20位」（V1.26 改过）；登录无格式校验。
- 目标：
  1. 全部场景 非空 → 「请输入账号」。
  2. 注册（账号新增）格式 `^[a-zA-Z][a-zA-Z0-9_]{3,19}$` → 「账号必须以字母开头，长度为4-20位」。
  3. 登录格式 `^[a-zA-Z0-9_]{1,20}$` → 「账号仅支持字母、数字、下划线」。
- 落地：把 V1.26 误改的「1-20 位」回改为「4-20 位」，正则补全为 `^[a-zA-Z][a-zA-Z0-9_]{3,19}$`；登录补格式校验。

### 2. 手机号（phone）✅ 模拟 libphonenumber · 提示语不变 · 账号新增/编辑手机号可为空
- 现状：`accValid` 仅 `^\d+$`；登录仅非空，输入层已 `replace(/\D/g,'')`。
- 目标：
  1. 登录/注册 非空 → 「请输入手机号」（账号新增/编辑手机号可为空，不做非空）。
  2. 注册/登录 非法字符 `^\d+$` → 「手机号仅允许数字」。
  3. 注册/登录 区号匹配（**模拟**）→ 按区号校验号码位数（如 +86→11 位、+62→9–12 位等），提示语固定「手机号格式不正确，请检查号码与区号是否匹配」。
- 落地：新增 `simulatePhoneValid(areaCode, number)` 模拟函数，内置常见区号位数表；仅登录/注册场景触发第 3 条。

### 3. 邮箱（email）
- 现状：`accValid` 仅 RFC 简化正则；登录仅非空。
- 目标：
  1. 全部场景 非空 → 「请输入邮箱」。
  2. 全部场景 格式（RFC 5322）→ 「邮箱格式不正确」。
  3. 全部场景 字符超限 255 → 「邮箱地址不能超过 255 个字符」（`maxlength=255` + 校验）。
- 落地：复用/增强现有正则，补充 255 上限。

### 4. 密码（password）
- 现状：登录无格式校验；`doResetPwd` 用 8–32 位。
- 目标：
  1. 全部场景 非空 → 「请输入密码」。
  2. 注册/修改密码 长度 `^[!-~]{6,64}$`（6–64 位，可打印 ASCII）→ 「密码长度需为 6–64 位字符」。
  3. 注册/修改密码 非法字符（同上字符集）→ 「仅支持大小写字母、数字及键盘符号」。
  4. 登录 非法字符 `^[A-Za-z0-9!-~]{1,64}$`（最长 64）→ 「仅支持大小写字母、数字及键盘符号」。
- 落地：`doResetPwd` 8–32 → 6–64；登录密码补格式校验（当前登录流程只有非空）。

### 5. 确认密码（confirm）
- 现状：`canConfirmPwd` 用 8–32 + 一致。
- 目标：
  1. 全部场景 非空 → 「请再次输入密码」。
  2. 注册/修改密码 两次不一致 → 「两次密码不一致」。
- 落地：长度判断从「8–32」改为跟随密码「6–64」，或直接只做「非空 + 一致」（长度归密码字段负责）。

### 6. 验证码（code）
- 现状：登录验证码 `maxlength=6` + 非数字剔除；`nextPwdVerify` 仅非空。
- 目标：
  1. 全部场景 非空 → 「请输入验证码」。
  2. 注册/找回密码 格式 `^\d{6}$` → 「验证码格式不正确」。
- 落地：补齐「请输入验证码」非空提示；6 位纯数字已由输入层基本保证，可加显式正则兜底。

### 7. 姓名（name）
- 现状：`accValid` `length>64` → 「姓名不能超过64个字符」。
- 目标：全部场景 字符超限 64 → 「姓名不能超过 64 个字符」。
- 落地：几乎一致，仅统一文案空格。

### 8. 金额（amount）✅ 允许为 0
- 现状：价格/押金 `min=0` + `<0` 判负；退款 `max=800/500`。
- 目标：
  1. 全部场景 非空 → 「请输入金额」。
  2. 格式/范围 `^(0|[1-9]\d{0,9})(\.\d{1,2})?$`（0 或正数，最多两位小数，最大 9999999999.99）→ 「请输入有效金额，最多两位小数」。
- 落地：作用于车型/电池型号/套餐的「价格·押金」输入（允许 0）；退款金额/押金结合业务上限（800/500）与通用金额规则叠加校验。

### 9. 排序（sort）
- 现状：`<0` → 「排序不能为负数」。
- 目标：`^[1-9]\d?$`（整数 1–99）→ 「请输入 1-99 之间的整数」。
- 落地：车型/电池型号/套餐三处 `sort` 统一改 1–99 范围校验（当前允许 0，规范为 1 起）。

### 10. 备注（remark）
- 现状：`accValid` 255；其余 `maxlength=255`（V1.26 已加）。
- 目标：全部场景 255 → 「备注不能超过 255 个字符」。
- 落地：基本一致，仅统一文案（退款原因/客户资料备注目前无校验，需补 255 上限提示，`maxlength` 已限但无「超过」报错文案）。

### 11. 天数（days）
- 现状：`cfg.pre` / `cfg.breach` `type=number min=1`，无上限。
- 目标：`^[1-9]\d{0,2}$`（整数 1–365）→ 「请输入 1-365 之间的整数」。
- 落地：`saveCfg` 补范围校验 1–365。

### 12. 版本号（version）
- 现状：**无用户输入版本号字段**（`latestVersion` 为硬编码 demo）。
- 目标：`^(\d+\.){2}\d+$`（三段式）→ 「请输入合法三段式版本号」。
- ⚠️ 开放问题：当前 UI 无版本号输入位。**需确认是否新增「版本号」输入场景，或该规则仅用于后端/未来功能，本轮跳过。**

## 三、最终文案口径（已锁定，落地以此为准）

> 提示文案逐字使用，含 en dash「–」（U+2013）、长度统一用「位」、范围前后带空格。

### 账号
1. 请输入账号
2. 账号必须以字母开头，长度为 4–20 位
3. 账号仅支持字母、数字、下划线

### 手机号
1. 请输入手机号
2. 手机号仅支持数字
3. 手机号格式不正确，请检查号码与区号是否匹配

### 邮箱
1. 请输入邮箱
2. 邮箱格式不正确
3. 邮箱地址不能超过 255 位

### 密码
1. 请输入密码
2. 密码长度需为 6–64 位
3. 密码仅支持大小写字母、数字、键盘符号（注册/修改密码·非法字符）
4. 密码仅支持大小写字母、数字、键盘符号（登录·非法字符）

### 确认密码
1. 请再次输入密码
2. 两次输入的密码不一致

### 验证码
1. 请输入验证码
2. 验证码格式不正确

### 姓名
- 姓名不能超过 64 位

### 金额
1. 请输入金额
2. 请输入有效金额，最多两位小数

### 排序
- 请输入 1–99 之间的整数

### 备注
- 备注不能超过 255 位

### 天数
- 请输入 1–365 之间的整数

### 版本号
- 请输入合法三段式版本号

### 风格要点（与现状/前稿的差异）
- 范围连字符统一 en dash「–」：4–20、6–64、1–99、1–365（前稿误写为半角「-」）。
- 长度文案统一用「位」（不用「个字符」）：如「长度为 4–20 位」「密码长度需为 6–64 位」「不能超过 255 位」。
- 用词统一「仅支持」：手机号也改为「手机号仅支持数字」（不再用「仅允许」）。
- 密码字符集文案为「键盘符号」（非「键盘常规符号」）。
- 确认密码用「两次输入的密码不一致」（非「两次密码不一致」）。
- 占位符结尾去省略号：「请输入退款原因」「请输入备注内容」。

## 四、建议的落地方式

1. **抽一个统一校验器**（新增 `validate(rules)` 或扩展 `vErr`），支持规则类型：`required` / `regex` / `len(max)` / `match(field)` / `range(min,max)`，返回 `{field: message}`。避免每处手写正则 + 内联 message。
2. **统一文案常量**：把规范中的中文提示抽为常量表（`MSG = {accountRequired: '请输入账号', ...}`），保证同字段多场景文案一致。
3. **校验触发时机**：保持现有「提交时校验 + 输入时 `vClr` 清错」模式，不为每个字段单独加实时校验（除非用户要求），减少改动面。
4. **严格局部**：只改校验逻辑与文案，不改页面结构、布局、数据模型、业务流程。

## 五、影响范围 & 冻结内容

- 影响：`accValid`、`canLogin`/`doLogin`、`canConfirmPwd`/`doResetPwd`/`nextPwdVerify`、`saveModel`/`saveBattModel`/`submitPackageAdd`/`savePackage`、`savePos`/`saveCs`、`saveCfg`，及对应表单字段的 `maxlength`/提示。
- 冻结：页面结构、导航、权限、路由、数据模型、全局 UI 变量（`--accent` 等）、已有计数与按钮样式（V1.25–V1.27 成果）。

## 六、已确认决策 & 剩余开放问题

已确认：
1. 账号注册长度 4–20（回改 V1.26 的 1–20）。
2. 手机号区号匹配：模拟 libphonenumber（按区号校验位数），提示语不变；账号新增/编辑手机号可为空。
3. 金额允许为 0。
4. 排序范围 1–99（按规范，1 起）。
5. 全部提示文案已锁定（见「三、最终文案口径」）。

剩余开放问题：
1. 版本号：当前 UI 无版本号输入位，是否本轮新增输入场景，还是跳过（仅留规则备用）。
2. 密码字符集正则为 `^[!-~]{6,64}$`（可打印 ASCII，含大小写字母、数字、键盘符号，不含空格/全角），落地前请确认是否含空格。

## 七、Next step

1. 请批注「剩余开放问题」2 条的取舍（版本号是否新增输入场景 / 密码字符集是否含空格）。
2. 确认后切到 Design 模式，我将按「逐字段清单 + 统一校验器 + 已锁定文案口径」落地到 `index.html`，保持最小变更。
