# 商户端权限字典

> **用途**：职位权限勾选、按钮显隐/禁用、研发鉴权与测试验收的**统一码表**。  
> **范围**：商户端 App（`index.html` 四 Tab + 登录后能力）。  
> **版本**：V2.5 · 2026-07-23  
> **状态**：主树 = **线下已有功能权限**（用户最新稿）；原型阶段新增能力见 **§4.1 差异清单**（默认不鉴权或待补码）。  
> **切换门店不进 RBAC**。原型演示：`index.html` 侧栏「原型操作」+ `can`/`deny`/`enforceTabPerms`。职位树 `mkP`/`mkPM` 仍为旧桩。

---

## 1. 文档约定

| 项 | 约定 |
|---|---|
| 权限码 | 英文点分，稳定不可随意改名；展示名可中文 |
| 粒度 | **功能权限**（能不能点 / 能不能进页） |
| 不在本表 | **数据范围**（本店 / 多店 / 全商户）——见 §6 |
| 端 | 本表仅 **商户端**；平台端另建树 |
| 无权限默认 | 见各行「无权限表现」；全局默认见 §2 |

### 1.1 关联文档

| 文档 | 关系 |
|---|---|
| `我的-PRD.md` | 职位管理配置 UI、权限树展示 |
| `首页-PRD.md` / `首页-告警管理-PRD.md` | 首页区块、告警处置 |
| `车辆-PRD.md` / `车辆地图视图-PRD.md` | 列表/地图/位置/轨迹/车控 |
| `订单-PRD.md` | 订单列表与状态动作 |
| `登录-PRD.md` | 登录/区号/语言（多数**不进**本 RBAC 表） |

### 1.2 字段说明

| 字段 | 含义 |
|---|---|
| **权限码** | 鉴权与职位树叶子节点 id |
| **名称** | 职位勾选区 / 原型侧栏展示名 |
| **所属页面 / 入口** | 主要作用面 |
| **无权限表现** | 无码时的产品行为（须前后端一致） |
| **依赖** | 父级或前置码；无父码时子码不生效 |

### 1.3 码名演进（历史对照，非权限范围）

| 历史码 | 当前线下主树码 |
|---|---|
| `home.list` / 更早 `home.view`（短名树） | **`home.view`**（首页总览） |
| `home.view_stats` | `home.stats.business` |
| `home.view_vehicle_stats` | `home.stats.vehicle` |
| `home.view_alerts` | `home.alert.recent` |
| `vehicle.view_list` / `view_detail` | `vehicle.list` / `vehicle.detail` |
| `vehicle.unlock` 等 | `vehicle.command.*` / `vehicle.status.*` |
| `order.view_*` / `order.cancel` 等 | `order.list` / `order.detail` / `order.operate.*` / `order.status.*` / `order.refund.*` |
| `order_config.manage` / `order_config.detail` | **`order_config.view`** + `order_config.edit` |

---

## 2. 无权限全局规则

| 规则 ID | 规则 |
|---|---|
| R1 | **主 Tab 查看权**：缺 `home.view` / `vehicle.list` / `order.list` → **隐藏对应底部 Tab**；应用默认落地 **「我的」**（「我的」Tab 始终保留）。 |
| R2 | **进不了的管理/二级页**：无列表或菜单码 → **隐藏对应入口**；深链/推送直达 → Toast「无权限」并回退安全页。 |
| R3 | **详情类**（`*.detail`）：列表/入口**仍可点击**，进入前 **Toast「无权限」**，不进入详情页。 |
| R4 | **动作入口（非车控）**：有查看无动作码 → **隐藏**对应操作入口/按钮。 |
| R5 | **车辆远程控制 + 报失状态**：缺 `vehicle.command.*` / `vehicle.status.*` 对应码时，**按钮仍展示**，点击后 **Toast「无权限」**。位置页鸣铃并入 `vehicle.command.find_car`（若未来单拆见 §4.1）。 |
| R6 | **父子**：无父模块列表权时，不单独暴露子动作入口；子码不生效。 |
| R7 | **系统配置入口**：无 `model.list`、`account.list`、`position.list`、`cs_info.list`、`order_config.view` 中**对应**码 → **隐藏该管理菜单项**。 |
| R8 | 告警页「寻车」默认校验 `vehicle.command.find_car`（策略 A）。 |
| R9 | 本期原型职位树为配置展示；真鉴权接中台后以本字典为准。 |

**不进功能权限树（登录即可 / 个人偏好）**

| 能力 | 说明 |
|---|---|
| 登录、退出、区号、登录内语言 | 会话与合规 |
| 系统设置 · 版本号 | 全员只读 |
| 设置 · 切换语言 | 个人偏好 |
| 个人信息、绑手机/邮箱、改密 | 账号自助 |
| **切换门店** | 登录即可；**无功能码**；可见门店范围见 §6 |

---

## 3. 权限字典总表（线下已有 · 主树）

### 3.1 首页

| 权限码 | 名称 | 所属页面 / 入口 | 无权限表现 | 依赖 |
|---|---|---|---|---|
| `home.view` | 首页总览 | 底部 Tab「首页」 | **隐藏首页 Tab**；默认跳转 **「我的」** | — |
| `home.stats.business` | 经营数据看板 | 首页经营数据入口/卡片区 | **隐藏经营数据入口** | `home.view` |
| `home.stats.vehicle` | 车辆运营统计 | 首页车辆运营入口/在线离线 | **隐藏车辆运营入口**；不可钻取车辆列表 | `home.view` |
| `home.alert.recent` | 最近告警列表 | 首页最近告警区块、「查看全部」 | **隐藏最近告警入口** | `home.view` |

### 3.2 车辆管理

| 权限码 | 名称 | 所属页面 / 入口 | 无权限表现 | 依赖 |
|---|---|---|---|---|
| `vehicle.list` | 车辆列表 | 底部 Tab「车辆」、列表/搜索/筛选 | **隐藏车辆 Tab**；默认 **「我的」** | — |
| `vehicle.detail` | 车辆详情 | 车辆列表/地图 → 详情 | 点击 → **Toast「无权限」**，不进详情 | `vehicle.list` |
| `vehicle.add` | 新增车辆 | 车辆列表 → 添加 | **隐藏**新增入口 | `vehicle.list` |
| `vehicle.edit` | 编辑车辆 | 车辆详情/列表 → 编辑 | **隐藏**编辑入口 | `vehicle.detail` |
| `vehicle.command.unlock` | 开锁 | 车辆详情 → 开锁 | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.command.lock` | 关锁 | 车辆详情 → 关锁 | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.command.find_car` | 寻车鸣笛 | 车辆详情 → 寻车；告警一键寻车（策略 A） | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.command.arm` | 设防 | 车辆详情 → 设防 | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.command.disarm` | 解除设防 | 车辆详情 → 解除设防 | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.status.lost` | 车辆报失 | 车辆详情 → 报失 | 按钮**保留**；点后 Toast | `vehicle.detail` |
| `vehicle.status.unlost` | 解除报失 | 车辆详情 → 解除报失 | 按钮**保留**；点后 Toast | `vehicle.detail` |

### 3.3 订单管理

| 权限码 | 名称 | 所属页面 / 入口 | 无权限表现 | 依赖 |
|---|---|---|---|---|
| `order.list` | 订单列表 | 底部 Tab「订单」 | **隐藏订单 Tab**；默认 **「我的」** | — |
| `order.detail` | 订单详情 | 订单列表 → 详情 | 点击 → **Toast「无权限」**，不进详情 | `order.list` |
| `order.operate.cancel` | 取消订单 | 订单详情 → 取消 | **隐藏**取消入口 | `order.detail` |
| `order.operate.confirm_payment` | 确认支付 | 订单详情 → 确认支付 | **隐藏**确认支付入口 | `order.detail` |
| `order.operate.return_vehicle` | 归还车辆 | 订单详情 → 还车 | **隐藏**还车入口 | `order.detail` |
| `order.status.report_lost` | 订单车辆报失 | 订单详情 → 报失 | **隐藏**入口；成功会改车辆报失态 | `order.detail` |
| `order.status.report_unlost` | 订单解除报失 | 订单详情 → 解除报失 | **隐藏**入口；成功会改车辆报失态 | `order.detail` |
| `order.refund.create` | 发起退款 | 订单详情 → 退款 | **隐藏**退款入口 | `order.detail` |
| `order.refund.list` | 查看退款记录 | 订单详情 → 退款记录 | **隐藏**退款记录入口 | `order.detail` |

### 3.4 系统配置

| 权限码 | 名称 | 所属页面 / 入口 | 无权限表现 | 依赖 |
|---|---|---|---|---|
| `model.list` | 车型列表 | 我的 → 车型管理 | **隐藏**车型管理入口 | — |
| `model.edit` | 编辑车型 | 车型管理 → 编辑 | **隐藏**编辑入口 | `model.list` |
| `account.list` | 账号列表 | 我的 → 账号管理 | **隐藏**账号管理入口 | — |
| `account.detail` | 账号详情 | 账号管理 → 详情 | 点击 → Toast「无权限」 | `account.list` |
| `account.add` | 新增账号 | 账号管理 → 新增 | **隐藏**新增入口 | `account.list` |
| `account.edit` | 编辑账号 | 账号管理 → 编辑 | **隐藏**编辑入口 | `account.list` |
| `account.status.disable` | 禁用账号 | 账号管理 → 禁用 | **隐藏**禁用入口 | `account.list` |
| `account.status.enable` | 启用账号 | 账号管理 → 启用 | **隐藏**启用入口 | `account.list` |
| `position.list` | 职位列表 | 我的 → 职位管理 | **隐藏**职位管理入口 | — |
| `position.detail` | 职位详情 | 职位管理 → 详情 | 点击 → Toast「无权限」 | `position.list` |
| `position.add` | 新增职位 | 职位管理 → 新增/新增下级 | **隐藏**新增入口 | `position.list` |
| `position.edit` | 编辑职位 | 职位管理 → 编辑 | **隐藏**编辑入口 | `position.list` |
| `position.delete` | 删除职位 | 职位管理 → 删除 | **隐藏**删除入口 | `position.list` |
| `cs_info.list` | 客服列表 | 我的 → 客服信息 | **隐藏**客服入口 | — |
| `cs_info.add` | 新增客服 | 客服信息 → 新增 | **隐藏**新增入口 | `cs_info.list` |
| `cs_info.edit` | 编辑客服 | 客服信息 → 编辑 | **隐藏**编辑入口 | `cs_info.list` |
| `cs_info.delete` | 删除客服 | 客服信息 → 删除 | **隐藏**删除入口 | `cs_info.list` |
| `order_config.view` | 订单配置 | 我的 → 订单配置 | **隐藏**订单配置入口 | — |
| `order_config.edit` | 编辑配置 | 订单配置 → 保存 | **隐藏**保存入口 | `order_config.view` |

---

## 4. 职位勾选树（线下主树 · 与最新业务稿一致）

```text
权限系统
├── 首页
│   ├── 首页总览 [home.view]
│   ├── 经营数据看板 [home.stats.business]
│   ├── 车辆运营统计 [home.stats.vehicle]
│   └── 最近告警列表 [home.alert.recent]
├── 车辆管理
│   ├── 车辆列表 [vehicle.list]
│   ├── 车辆详情 [vehicle.detail]
│   ├── 车辆基础操作
│   │   ├── 新增车辆 [vehicle.add]
│   │   └── 编辑车辆 [vehicle.edit]
│   ├── 车辆远程控制
│   │   ├── 开锁 [vehicle.command.unlock]
│   │   ├── 关锁 [vehicle.command.lock]
│   │   ├── 寻车鸣笛 [vehicle.command.find_car]
│   │   ├── 设防 [vehicle.command.arm]
│   │   └── 解除设防 [vehicle.command.disarm]
│   └── 车辆报失状态
│       ├── 车辆报失 [vehicle.status.lost]
│       └── 解除报失 [vehicle.status.unlost]
├── 订单管理
│   ├── 订单列表 [order.list]
│   ├── 订单详情 [order.detail]
│   ├── 订单常规操作
│   │   ├── 取消订单 [order.operate.cancel]
│   │   ├── 确认支付 [order.operate.confirm_payment]
│   │   └── 归还车辆 [order.operate.return_vehicle]
│   ├── 订单车辆报失
│   │   ├── 订单车辆报失 [order.status.report_lost]
│   │   └── 订单解除报失 [order.status.report_unlost]
│   └── 退款管理
│       ├── 发起退款 [order.refund.create]
│       └── 查看退款记录 [order.refund.list]
└── 系统配置
    ├── 车型管理
    │   ├── 车型列表 [model.list]
    │   └── 编辑车型 [model.edit]
    ├── 账号管理
    │   ├── 账号列表 [account.list]
    │   ├── 账号详情 [account.detail]
    │   ├── 新增账号 [account.add]
    │   ├── 编辑账号 [account.edit]
    │   ├── 禁用账号 [account.status.disable]
    │   └── 启用账号 [account.status.enable]
    ├── 职位管理
    │   ├── 职位列表 [position.list]
    │   ├── 职位详情 [position.detail]
    │   ├── 新增职位 [position.add]
    │   ├── 编辑职位 [position.edit]
    │   └── 删除职位 [position.delete]
    ├── 客服信息
    │   ├── 客服列表 [cs_info.list]
    │   ├── 新增客服 [cs_info.add]
    │   ├── 编辑客服 [cs_info.edit]
    │   └── 删除客服 [cs_info.delete]
    └── 订单配置
        ├── 订单配置 [order_config.view]
        └── 编辑配置 [order_config.edit]
```

---

## 4.1 原型阶段新增功能 · 相对线下主树的权限差异（必读）

> **定义**  
> - **线下主树**：上文 §3 / §4，与现网/线下已上线能力对齐，**职位勾选只认此树**。  
> - **原型增量**：`index.html` 已实现、但**不在**线下主树中的能力。  
> - **当前策略**：原型增量**默认不挂功能码**（登录即可用），避免与线下中台码表冲突；若上线要控权，采用下表「建议补码」。

### A. 能力清单与现状

| # | 原型能力 | 主要入口 | 相对线下主树 | 当前鉴权 | 建议后续权限码（若纳入 RBAC） | 建议无权限表现 |
|---|---|---|---|---|---|---|
| 1 | **消息中心** | 首页铃铛 | 线下树无 | **不鉴权** | `message.view`（可选 `message.read`） | 隐藏铃铛；推送深链 Toast |
| 2 | **告警中心列表** | 最近告警「查看全部」 | 仅有首页最近告警入口码 | 进中心**不鉴权**；入口受 `home.alert.recent` | `alert.list` | 无 `home.alert.recent` 则无入口；若补 `alert.list` 则藏中心 |
| 3 | **告警详情** | 首页/中心点告警 | 线下树无 | **不鉴权** | `alert.detail` | 点后 Toast 或不进页 |
| 4 | **告警处置**（开始处理/忽略/标记已处理/重新打开） | 告警详情底栏 | 线下树无 | **不鉴权** | `alert.start_process` / `alert.ignore` / `alert.resolve` / `alert.reopen` | **隐藏**对应按钮（建议走 R4，非车控） |
| 5 | **告警一键寻车** | 告警详情 | 线下无独立码 | 复用 **`vehicle.command.find_car`**（策略 A） | 保持 A；或 `alert.find_car`（策略 B） | 同 R5 点后 Toast 或隐藏 |
| 6 | **告警跳转车辆** | 告警详情 | 线下无 | 需 `vehicle.list` 才能跳列表/详情 | 可并入 `vehicle.detail` | Toast 无权限 |
| 7 | **车辆地图视图** | 车辆列表次 FAB | 线下树无 | **不鉴权** | `vehicle.view_map` | 隐藏地图切换 FAB |
| 8 | **车辆实时位置** | 详情「查看车辆位置」 | 线下树无 | **不鉴权** | `vehicle.view_location` | 隐藏入口 |
| 9 | **历史轨迹 + 时间筛选** | 详情「历史轨迹」 | 线下树无 | **不鉴权** | `vehicle.view_track` | 隐藏入口 |
| 10 | **位置页导航** | 位置底栏橙色导航 | 线下树无 | **不鉴权** | `vehicle.navigate` | 隐藏导航按钮 |
| 11 | **位置页远程鸣铃** | 位置右侧竖钮 | 线下树无独立码 | 复用 **`vehicle.command.find_car`** | 可选拆 `vehicle.command.ring` | 同寻车 R5 |
| 12 | **位置页自身定位 / 复位车辆** | 位置右侧竖钮 | 线下树无 | **不鉴权**（交互控件） | 一般不单拆 | — |
| 13 | **订单客户资料查看** | 订单详情补充资料 | 线下树无 | **不鉴权** | `order.view_docs` | 隐藏资料入口 |
| 14 | **订单客户资料编辑** | 资料页保存 | 线下树无 | **不鉴权**（仍受订单状态可写约束） | `order.edit_docs` | 只读或隐藏保存 |
| 15 | **登录全套 / 区号 / 改密 / 绑定** | 登录、个人信息 | 非业务 RBAC | **不鉴权**（会话） | 不进主树 | — |
| 16 | **系统设置 / 语言 / 退出** | 我的 → 设置 | 非业务 RBAC | **不鉴权** | 不进主树 | — |
| 17 | **切换门店** | 我的 | 明确不进 RBAC | **不鉴权** | — | 数据范围见 §6 |

#### A.1 原型增量建议权限码详情（与 §3 总表格式对齐）

> 以下为原型阶段新增功能的**建议权限码**（若上线需纳入 RBAC），格式与 §3 总表一致。当前均**默认不鉴权**。

| 权限码 | 名称 | 所属页面 / 入口 | 无权限表现 | 依赖 |
|---|---|---|---|---|
| `message.view` | 消息中心 | 首页 → 铃铛入口 | **隐藏**铃铛；推送深链 Toast | `home.view` |
| `alert.list` | 告警中心列表 | 首页最近告警 → 查看全部 | **隐藏**告警中心入口 | `home.alert.recent` |
| `alert.detail` | 告警详情 | 告警中心 → 告警行 | 点击 → **Toast「无权限」** | `alert.list` |
| `alert.start_process` | 开始处理 | 告警详情 → 底栏 | **隐藏**按钮 | `alert.detail` |
| `alert.ignore` | 忽略 | 告警详情 → 底栏 | **隐藏**按钮 | `alert.detail` |
| `alert.resolve` | 标记已处理 | 告警详情 → 底栏 | **隐藏**按钮 | `alert.detail` |
| `alert.reopen` | 重新打开 | 告警详情 → 底栏 | **隐藏**按钮 | `alert.detail` |
| `vehicle.view_map` | 车辆地图视图 | 车辆列表 → 次 FAB | **隐藏**地图切换 FAB | `vehicle.list` |
| `vehicle.view_location` | 车辆实时位置 | 车辆详情 → 查看车辆位置 | **隐藏**入口 | `vehicle.detail` |
| `vehicle.view_track` | 历史轨迹 | 车辆详情 → 历史轨迹 | **隐藏**入口 | `vehicle.detail` |
| `vehicle.navigate` | 导航前往 | 车辆位置详情 → 底栏导航 | **隐藏**导航按钮 | `vehicle.view_location` |
| `order.view_docs` | 查看客户资料 | 订单详情 → 补充资料 | **隐藏**资料入口 | `order.detail` |
| `order.edit_docs` | 编辑客户资料 | 客户资料页 → 保存 | **隐藏**保存按钮 | `order.view_docs` |

**说明**：
- 告警一键寻车 / 位置页鸣铃：复用线下 `vehicle.command.find_car`（策略 A），不单独建码
- 告警跳转车辆：复用 `vehicle.detail`，不单独建码
- 位置页自身定位/复位车辆：交互控件，不进 RBAC
- 登录/改密/绑定/设置/切店：会话或个人偏好，不进业务权限树

### B. 与线下主树「同名能力、表现需对齐」的注意点

| 点 | 说明 |
|---|---|
| 首页最近告警 | 线下有 `home.alert.recent`；**只控制首页区块**，不控制告警中心处置（处置为原型增量） |
| 寻车 | 线下 `vehicle.command.find_car`；原型告警/位置鸣铃**共用**此码时，关码后三处都应点后 Toast |
| 列表进页码 | 进入客服/车型等列表后，侧栏**不展示** `*.list` / `order_config.view`（入口只在「我的」/底部 Tab） |

### C. 产品决策建议（迭代用）

| 优先级 | 建议 |
|---|---|
| P0 | 上线前：告警处置四码是否进中台；不进则接受「有最近告警入口即可全量处置」 |
| P0 | 地图 / 位置 / 轨迹是否必须控权；外勤岗常只需位置+轨迹而无车控写权 |
| P1 | 消息中心是否全员；若是则保持不鉴权 |
| P1 | 客户资料是否财务/合规敏感 → 建议补 `order.view_docs` / `order.edit_docs` |
| P2 | 鸣铃是否与寻车拆码 |

### D. 差异一句话

**线下主树 = 首页四块 + 车辆列表详情与车控报失 + 订单列表详情与常规/报失/退款 + 系统配置五块。**  
**原型多出来的 = 消息中心、告警中心与处置、车辆地图/位置/轨迹/导航、订单客户资料、登录与账号自助、设置与切店（切店明确无码）。**

---

## 4.2 寻车策略

| 策略 | 做法 | 适用 |
|---|---|---|
| **A（默认）** | 详情寻车、告警寻车、位置鸣铃 → 只校验 `vehicle.command.find_car` | 与线下一致、实现简单 |
| B | 告警侧单独 `alert.find_car` | 处置岗无车辆详情权但要寻车（需另批） |

## 4.3 无权限表现策略摘要

| 类型 | 表现 |
|---|---|
| 主 Tab（`home.view` / `vehicle.list` / `order.list`） | 隐藏 Tab → 默认「我的」 |
| 首页区块 / 管理菜单 / 多数动作 | **隐藏入口** |
| 详情类 | **点后 Toast**，不进页 |
| 车辆车控与报失七码 | **按钮保留**，点后 Toast |
| 订单报失/解除 | 隐藏入口 + 成功改车辆报失态 |
| 原型增量（§4.1） | **默认不鉴权**，直至产品补码 |

---

## 5. 角色示例包（可选）

| 角色模板 | 建议勾选（摘要） |
|---|---|
| **店长** | 主树全开 |
| **运维** | `home.view` + 车辆统计/最近告警；车辆 list/detail + 车控；订单 list/detail；系统配置关 |
| **前台/收银** | 首页 + 经营；订单 list/detail + operate；车辆 list/detail；配置关 |
| **只读巡店** | 各模块仅 list/detail；无动作码（车控按钮可见但点后 Toast） |

---

## 6. 数据权限（不进功能码表）

| 维度 | 建议取值 | 与功能码关系 |
|---|---|---|
| 门店范围 | 本门店 / 指定多店 / 全商户 | **切换门店无功能码**；列表仅展示数据范围内门店 |
| 对象可见 | 车/单/告警随当前门店过滤 | 功能码通过后仍受数据范围约束 |

---

## 7. 与原型职位树差距

当前 `index.html` 中 `mkP` / `mkPM` **远少于**本字典 §4。迭代：

| # | 事项 | 优先级 |
|---|---|---|
| 1 | 用本字典 §4 替换 `mkP`/`mkPM` | P0 |
| 2 | 主树按钮映射 | **原型已做** |
| 3 | 后端中台码表对齐 §3（`home.view` / `order_config.view`） | P0 |
| 4 | §4.1 原型增量是否补码、上线范围 | P0 产品决策 |
| 5 | 平台端权限另文 | P2 |

### 7.1 原型演示说明

| 能力 | 实现要点 |
|---|---|
| 主树码 | 与 §3 / §4 一致：`home.view`、`order_config.view` 等 |
| 底部 Tab（常驻） | `home.view` / `vehicle.list` / `order.list`（展示名：首页/车辆/订单） |
| 本页权限 | 页面名 + 全开/全关；**已进列表/配置页不露进入码**（如 `cs_info.list`、`order_config.view`） |
| 主 Tab（R1） | 缺权藏 Tab →「我的」 |
| 详情（R3） | 点后 Toast |
| 动作（R4） | 订单/后台入口隐藏 |
| 车控（R5） | 按钮保留，点后 Toast |
| 后台（R7） | 菜单按 list/`order_config.view` 过滤；保存 `order_config.edit` |
| 原型增量 | §4.1 能力**无 `can` 守卫**（寻车复用 `vehicle.command.find_car` 除外） |
| 切换门店 | 无码，菜单常驻 |

---

## 8. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0–V1.7 | 2026-07-22~23 | 初版树、表现规则、侧栏 UX、切店移出 RBAC |
| V2.0 | 2026-07-23 | 点分码重构；树外不鉴权 |
| V2.1 | 2026-07-23 | 列表页侧栏不展示进入列表码 |
| **V2.2** | **2026-07-23** | **对齐线下最新树**：`home.view`、`order_config.view`；新增 **§4.1 原型增量 vs 线下差异**；`index.html` V1.47 |
| **V2.3** | **2026-07-23** | 中文名：「订单配置页面」→「订单配置」、「编辑配置参数」→「编辑配置」；`index.html` V1.48 |
| **V2.4** | **2026-07-23** | §3 总表「所属页面/入口」列统一为「父级页面 → 入口」格式（修复 `position.add` 等与父级不一致） |
| **V2.5** | **2026-07-23** | §4.1 新增 **A.1 原型增量建议权限码详情**，格式与 §3 总表对齐（名称/所属页面/无权限表现/依赖） |

---

## 9. Next step

1. 中台码表按 §3 落地（注意 `home.view`、`order_config.view` 与历史 `home.list` / `order_config.detail` 的迁移）。  
2. 产品确认 §4.1 哪些增量必须补码上线。  
3. 职位树 `mkP`/`mkPM` 替换为 §4。  
4. 冒烟：关 `home.view`→无首页 Tab；关详情→Toast；关车控→点后 Toast；关 `order_config.view`→无配置菜单；关 `order_config.edit`→无保存。
