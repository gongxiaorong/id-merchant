# 首页 Tab PRD（产品版）

> 本文从当前商户端 App 原型 `index.html` **倒推**整理，对齐「首页能看到什么 / 能钻到哪里 / 与告警·消息如何分工」。  
> 告警处置闭环的完整规则见 **`告警管理-PRD.md`**（本文只定义首页入口与展示边界，不重复告警状态机）。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 底部导航 · **首页** Tab |
| 文档类型 | 产品需求说明（由现状原型倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（`tab==='home'`：经营数据 / 车辆运营 / 最近告警 / 告警中心 / 告警详情 / 消息中心） |
| 状态 | 与当前线框一致，可进评审 |
| 关联 | `告警管理-PRD.md`；车辆 Tab（在线/离线钻取） |

---

## 2. 背景：为什么要做

### 2.1 业务现状

门店/车队管理员打开 App 的第一屏需要同时回答三件事：

1. **今天生意怎么样？** —— 收款与订单量  
2. **车队在线是否健康？** —— 多少在线、多少离线  
3. **有没有必须立刻跟的异常？** —— 最近告警与待处理数量  

首页是「经营 + 运维」的汇合点，而不是单独的报表页或单独的告警页。

### 2.2 核心痛点（倒推）

| # | 痛点 | 业务影响 |
|---|---|---|
| 1 | 经营与车队状态分散在多入口 | 开门店要连点多次才能摸清现状 |
| 2 | 告警若只在二级页 | 待处理容易漏看 |
| 3 | 告警与系统通知混在一起 | 术语乱、红点语义不清 |
| 4 | 离线车辆只看数字无法下钻 | 知道「有 8 台离线」却不知道去哪处置 |

### 2.3 产品机会

在**不改底部四 Tab 架构**的前提下，把首页做成：

- **一眼经营**：收款与订单概览（本期只读）  
- **一眼车队**：在线/离线可钻车辆列表  
- **一眼异常**：最近告警 + 进告警中心完整队列  
- **系统通知独立**：消息中心只承载运营/系统类消息  

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **早班 30 秒摸清** | 打开首页即可看到收款、在线/离线、待处理告警 |
| **可下钻** | 在线/离线 → 车辆列表带筛选；告警 → 详情或告警中心 |
| **职责清** | 车侧异常走告警；系统/订单运营通知走消息 |
| **不抢主路径** | 首页不承载车控、不承载订单状态机操作 |

### 3.2 成功怎么量

- 打开 App 默认落在首页  
- 待处理/处理中告警数量与「全部」旁红点、最近列表口径一致  
- 点在线/离线进入车辆 Tab 后筛选正确  
- 消息未读角标与消息中心一致  

### 3.3 本期不做

- 经营数据下钻到订单明细页（卡片只读）  
- 首页大盘图表、自定义看板  
- 推送到达后的强制弹窗编排（可二期）  
- 权限改造、多门店数据合并视图（切换门店在「我的」）  

---

## 4. 用户与场景

### 4.1 主用户

商户侧店长 / 运维（手机 App）。

### 4.2 关键场景

| 场景 | 路径 |
|---|---|
| A 早班扫一眼 | 看经营四卡 → 看在线/离线 → 看最近告警有无红点 |
| B 跟异常 | 点最近告警一条 → 告警详情处置（规则见告警 PRD） |
| C 清队列 | 点「全部」→ 告警中心筛状态/类型 |
| D 查离线 | 点「离线车辆」→ 车辆 Tab 自动筛离线 |
| E 看系统通知 | 点铃铛 → 消息中心已读/未读 |

---

## 5. 范围与信息架构

### 5.1 页面清单

| 页面 | 层级 | 说明 |
|---|---|---|
| 首页主屏 | Tab 根 | 门店名、铃铛、经营数据、车辆运营、最近告警 |
| 告警中心 | 二级覆盖 | 状态 Pill + 类型 Pill + 列表 |
| 告警详情 | 三级覆盖 | 见 `告警管理-PRD.md` |
| 消息中心 | 二级覆盖 | 系统/运营消息列表 |

### 5.2 模块边界

| 能力 | 首页负责 | 不负责 |
|---|---|---|
| 经营数据 | 展示四指标 | 改数、下钻订单 |
| 车辆运营 | 在线/离线数字 + 跳转 | 列表本身、地图 |
| 告警 | 最近条、红点、入口 | 规则引擎后台 |
| 消息 | 列表与已读 | 车侧告警重复条目 |

---

## 6. 核心概念

### 6.1 首页区块

| 区块 | 内容 | 交互 |
|---|---|---|
| 顶栏 | 当前门店名称；消息铃铛 + 未读角标 | 点铃铛进消息中心 |
| 经营数据 | 今日收款、本月收款、进行中订单、待还订单；部分带环比趋势 | 只读 |
| 车辆运营 | 在线车辆、离线车辆（离线可危险色） | 点击 → 车辆 Tab + 在线筛选 |
| 最近告警 | 最多约 5 条仍异常（待处理/处理中）；「全部」+ 待处理计数红点 | 点行进详情；点全部进告警中心 |

### 6.2 最近告警展示字段

- 车架号  
- 类型标签（带等级色）  
- **最近触发**时间  
- 状态标签  

空态：无待处理/处理中时展示「暂无待处理告警」正向空态（非报错）。

### 6.3 消息 vs 告警

| | 告警中心 | 消息中心 |
|---|---|---|
| 内容 | 车辆离线 / GPS 丢失 / 定位过时 / 疑似断电 | 系统维护、版本、订单运营提醒等 |
| 红点 | 待处理+处理中数量（首页「全部」旁） | 未读消息数（铃铛） |
| 处置 | 认领 / 忽略 / 结案 / 寻车 | 点开已读即可 |

---

## 7. 业务规则

| # | 规则 |
|---|---|
| H1 | 默认启动 Tab = 首页 |
| H2 | 经营数据四卡只读，不进订单 Tab 自动筛选（本期） |
| H3 | 车辆运营「在线/离线」→ `tab=vehicles`，写入在线筛选，**不强制地图视图** |
| H4 | 最近告警仅 `pending` ∪ `processing`，按最近触发新→旧，上限展示约 5 条 |
| H5 | 「全部」红点 = 待处理+处理中总数（>99 显示 99+） |
| H6 | 首页点告警进详情，返回落回**首页**（非告警中心） |
| H7 | 告警中心进详情，返回落回**告警中心列表** |
| H8 | 消息点开 → 该条已读；未读角标即时更新 |
| H9 | 消息中心**不得**再放与告警中心重复的「车辆离线告警」类条目 |
| H10 | 支持下拉刷新首页数据（加载骨架与现网一致） |
| H11 | 告警忽略弹窗主按钮文案为「确认」 |

告警状态机、聚合、生命周期等：**全部以 `告警管理-PRD.md` 为准**。

---

## 8. 功能需求

| ID | 需求 | 优先级 |
|---|---|---|
| F-H1 | 展示门店名与经营四卡 | P0 |
| F-H2 | 展示在线/离线并可钻车辆列表 | P0 |
| F-H3 | 最近告警列表 + 空态 | P0 |
| F-H4 | 进入告警中心（状态/类型筛选） | P0 |
| F-H5 | 进入告警详情并完成处置（见告警 PRD） | P0 |
| F-H6 | 消息中心列表与已读 | P0 |
| F-H7 | 下拉刷新与加载骨架 | P1 |

---

## 9. 页面说明（摘要）

### 9.1 首页主屏

- 顶栏门店名左对齐，铃铛右上  
- 区块标题：经营数据 / 车辆运营 / 最近告警  
- 卡片：白底、细边框、轻阴影，与全局 Neutral Modern 一致  

### 9.2 告警中心 / 详情

字段、底栏按钮、忽略原因、生命周期折叠等 → **`告警管理-PRD.md` §6–9**。

### 9.3 消息中心

- 行：未读蓝点 / 已读空心点、标题、摘要、时间  
- 空态：暂无消息  

---

## 10. 验收清单

- [ ] 默认 Tab 为首页  
- [ ] 经营四卡只读展示  
- [ ] 在线/离线跳转车辆并带正确筛选  
- [ ] 最近告警仅待处理/处理中；红点口径一致  
- [ ] 首页进详情返回首页；中心进详情返回中心  
- [ ] 消息未读角标正确；点开已读  
- [ ] 消息无重复车侧告警  
- [ ] 告警闭环用例以告警 PRD 第 11 节为准  

---

## 11. 版本规划

| 阶段 | 内容 |
|---|---|
| **本期** | 经营概览 + 车辆钻取 + 告警入口闭环 + 消息拆分 |
| **二期** | 经营卡下钻订单；首页按类型的待处理摘要；推送刷新策略 |
| **三期** | 可配置首页模块顺序 |

---

## 12. 待决问题

| # | 问题 | 当前默认 |
|---|---|---|
| 1 | 经营「进行中/待还」是否可点进订单并带 Pill？ | 本期不可点 |
| 2 | 下拉刷新是否刷新告警+经营同一接口？ | 产品不绑接口，体验上应整页刷新 |

---

## 13. Next step

1. 与 `告警管理-PRD.md` 一并冻结首页入口口径。  
2. 研发实现时首页只做展示与跳转，告警逻辑复用同一状态服务。  
3. 设计补：无网、刷新失败、经营数据为空。  

---

*文档结束。告警细则以 `告警管理-PRD.md` 为准。*
