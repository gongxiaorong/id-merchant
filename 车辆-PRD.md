# 车辆 Tab PRD（产品版）

> 本文从当前原型 `index.html`（车辆 Tab 全链路）**倒推**整理。  
> **列表 · 地图双视图**的专项规则以 **`车辆地图视图-PRD.md`** 为准；本文覆盖列表、详情、车控、新增/编辑、**车辆位置详情**、**历史轨迹**及与首页/告警的边界。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 产品 | 商户端车辆运营 App（租车 / 车联网） |
| 模块 | 底部导航 · **车辆** Tab |
| 文档类型 | 产品需求说明（由现状原型倒推） |
| 读者 | 产品、业务、设计、研发、测试 |
| 原型 | `index.html`（列表/地图、详情、位置、轨迹+时间滚轮、新增、编辑、筛选、车控弹窗） |
| 状态 | 列表+地图 P0 已对齐；位置详情 / 历史轨迹已按最新交互落地 |
| 关联 | `车辆地图视图-PRD.md`、`告警管理-PRD.md`、首页车辆运营钻取 |

---

## 2. 背景：为什么要做

### 2.1 业务现状

商户管车核心动作：

1. **找车** —— 按 VIN / 状态 / 在线 / 报失筛出来  
2. **看车** —— 电量、最后信号、在哪  
3. **控车** —— 开锁/关锁、寻车、设防、报失  
4. **回溯** —— 某时段怎么走的（轨迹）  
5. **找过去** —— 实时位置 + 导航去车边  

列表解决「找与扫」；地图解决「分布」；详情解决「控」；位置页解决「此刻在哪」；轨迹页解决「那段时间怎么走」。

### 2.2 核心痛点

| # | 痛点 | 影响 |
|---|---|---|
| 1 | 只有列表无空间感 | 调度靠经验 |
| 2 | 位置与轨迹入口弱 | 现场找车路径长 |
| 3 | 车控若放在列表/地图 | 误触风险高 |
| 4 | 无定位车若静默消失 | 以为车少了 |

### 2.3 产品机会

在**不新开「地图」一级 Tab、不把车控放到地图**的前提下，车辆 Tab 完成：

**搜筛 → 列表/地图预览 → 详情车控 → 位置/轨迹专项页**。

---

## 3. 目标与成功标准

### 3.1 产品目标

| 目标 | 说明 |
|---|---|
| **可找到** | VIN 搜索 + 状态 Pill + 筛选面板 |
| **可看见** | 列表字段完整；地图同一结果集（见地图 PRD） |
| **可处置** | 详情内长按开关锁、寻车、设防、报失 |
| **可定位** | 位置详情：实时点、近期轨迹、自身/车辆视角、鸣铃、导航 |
| **可回溯** | 轨迹页：时段筛选 + 起/终/折线；无底部摘要卡 |

### 3.2 成功怎么量

- 从列表/地图进详情 ≤ 2 次点击  
- 从详情到位置或轨迹 1 次点击  
- 列表与地图筛选口径一致（地图 PRD）  
- 车控仅在详情（及位置页「鸣铃」这一寻车变体）  

### 3.3 本期不做

| 不做 | 说明 |
|---|---|
| 真地图 SDK / 瓦片商用 | 原型示意底图；产品口径不变 |
| 地图上开锁/设防 | 防误触 |
| 轨迹页底部距离卡 | **已明确移除** |
| 电子围栏、画圈找车 | 二期+ |
| 车辆详情内「历史告警库」 | 告警 PRD 三期 |
| 权限体系改造 | 账号权限在「我的」展示/配置，不改车辆数据权限模型本期范围外的底层 |

---

## 4. 用户与场景

| 场景 | 路径 |
|---|---|
| A 按 VIN 找车 | 搜索 → 点卡片 → 详情 |
| B 筛离线上看图 | 筛选离线 → 次 FAB 地图 → 点标记 → 底卡 → 详情（地图 PRD） |
| C 现场找车 | 详情 → 查看车辆位置 → 看地址/距离 → 导航 / 鸣铃 |
| D 查时段轨迹 | 详情 → 历史轨迹 → 改起止时间 → 看起终折线 |
| E 新车入库 | 主 FAB → 选车型 → 填 VIN/中控 → 确认 |
| F 改车牌车型 | 详情 → 编辑 → 保存 |
| G 报失 | 详情 → 报失确认 → 列表/地图红点 |

---

## 5. 范围与页面清单

### 5.1 In Scope

1. 车辆列表（搜索、状态 Pill、筛选、下拉刷新、空态）  
2. 地图视图（**细则见地图 PRD**）  
3. 车辆详情（位置入口、轨迹入口、四宫格车控、联系人、基础信息）  
4. 车辆位置详情（VIN 标题、地图、右侧三钮、底栏地址+导航）  
5. 车辆轨迹（时间栏、地图起/终/线、时间滚轮弹窗；**无底卡**）  
6. 新增车辆、编辑车辆、车型选择 sheet  
7. 设防/报失确认弹窗；长按开关锁；寻车 loading  

### 5.2 页面层级（示意）

```
车辆 Tab 根（list | map）
  ├─ 筛选 sheet / 车型 sheet
  ├─ 新增车辆
  └─ 车辆详情
        ├─ 车辆位置详情
        ├─ 车辆轨迹 + 时间滚轮
        └─ 编辑车辆
```

---

## 6. 核心概念

### 6.1 车辆状态（列表 Pill）

| 值 | 含义（产品） |
|---|---|
| 全部 | 不限业务状态 |
| 空闲 | 可租/未占用 |
| 占用中 | 被占用未进入租用完成态等（与原型一致） |
| 租用中 | 在租 |

另有维度（筛选面板，非 Pill）：

- **在线 / 离线**  
- **未报失 / 已报失**  
- **车型**  

### 6.2 列表卡片字段

- 车架号  
- 中控编号  
- 在线状态点 + 业务状态点  
- 主/副电量  
- **最后信号时间**（文案与详情、地图底卡统一）  

### 6.3 车控四宫格

| 能力 | 交互 | 说明 |
|---|---|---|
| 开锁 / 关锁 | **长按**进度 | 防误触 |
| 寻车 | 单击，loading「寻车中…」 | 闪烁鸣笛类指令 |
| 设防 / 解除设防 | 单击 → 确认弹窗 | |
| 报失 / 解除报失 | 单击 → 确认弹窗（报失可为危险样式） | |

### 6.4 位置页 vs 轨迹页 vs 列表地图

| | 列表地图 | 车辆位置详情 | 车辆轨迹 |
|---|---|---|---|
| 对象 | 多车筛选结果 | **单车**实时 | **单车**时段路径 |
| 标题 | （无独立顶栏标题页） | **车架号** | 「车辆轨迹」 |
| 线 | 无（散点） | **用户 ↔ 车辆** 连线（非历史轨迹） | 时段蓝线 + 橙起/蓝终 |
| 底栏 | 选中摘要卡（可关） | **常驻**：地址+距您+步行+导航 | **无底卡** |
| 侧钮 | 无 | 自身定位 / 复位车辆 / 鸣铃 | 无 |

---

## 7. 业务规则

### 7.1 列表与筛选

| # | 规则 |
|---|---|
| V1 | 搜索对象为**车架号**；回车或点「搜索」生效 |
| V2 | Pill 与筛选面板、搜索共同构成「筛选结果」 |
| V3 | 筛选 sheet：车型、在线状态、报失；重置/确定；有条件时筛选按钮高亮 |
| V4 | 空态：无车引导新增；有筛选无结果可「清除筛选」 |
| V5 | 首页在线/离线进入：写入在线筛选，默认列表视图（地图 PRD R23） |

### 7.2 地图

全部遵循 **`车辆地图视图-PRD.md`**（同一结果集、离线上图、顶条无定位、FAB 组、底卡避让等）。

### 7.3 详情

| # | 规则 |
|---|---|
| V6 | 顶区**地图 + 底卡一体块**（地图与底卡紧贴）：点地图或底卡左侧**地址文案**（仅位置，无「当前位置」标签/信号时间）→ **车辆位置详情**；左右**无竖线**分隔 |
| V7 | 底卡右侧「历史轨迹」文字入口 → 车辆轨迹页（非独立列表行） |
| V8 | 右上角「编辑」→ 编辑页；VIN 只读展示 |
| V9 | 车控仅详情四宫格 + 位置页鸣铃；列表/地图不做车控 |
| V10 | 联系人区可一键呼叫（原型 toast 模拟） |
| V11 | 信息区：VIN、报失/在线标签、中控、车型、车牌、电量、业务状态、最后信号时间 |

### 7.4 车辆位置详情

| # | 规则 |
|---|---|
| V12 | 标题展示**完整车架号**（唯一识别） |
| V13 | 地图：蓝色车辆点 + 悬浮位置短标签；**用户点与车辆点之间的连线**（表达距您关系）；POI 地标；**不画**历史行驶轨迹（历史见轨迹页） |
| V14 | 右侧上→下：① 定位到**操作者自身** ② 复位到**车辆** ③ **远程鸣铃**（寻车告警） |
| V15 | 底栏左侧：完整属地地址；距您 X 米；步行 Y 分钟 |
| V16 | 底栏右侧橙色圆形按钮：唤起**外部导航**（系统/三方，产品只要求发起导航意图） |
| V17 | 返回 → 车辆详情 |
| V18 | 加载中展示「定位加载中…」 |

### 7.5 车辆轨迹

| # | 规则 |
|---|---|
| V19 | 顶栏标题「车辆轨迹」；返回 → 详情 |
| V20 | 顶区时间栏：起始时间、结束时间，点任一端打开时间滚轮 |
| V21 | 地图：橙「起」、蓝「终」、蓝折线、POI；**不展示**通栏底卡、不展示车牌/定位点数字摘要 |
| V22 | 无轨迹空态：「该时段暂无轨迹」 |
| V23 | 时间弹窗：取消不改；确定回填并**重新查询渲染**轨迹 |
| V24 | 滚轮维度：日、月、年、时、分（与原型一致） |
| V25 | 起始不得晚于结束（若用户选反，确定时应拦截或自动校正——**待决**，见 §13） |

### 7.6 新增 / 编辑

| # | 规则 |
|---|---|
| V26 | 新增：必选车型后才可填 VIN/中控；三者齐全可提交 |
| V27 | 编辑：可改中控、车型、车牌；VIN 不可改 |
| V28 | 车型 sheet 支持搜索选择 |
| V29 | 新增/编辑表单视觉同方案 A：灰底 canvas + 白卡 `.form-card` + 可编辑字段 `.login-field`；VIN/状态只读无 field 边框；车型选择条可保留虚线蓝底强调 |

---

## 8. 功能需求

| ID | 需求 | 优先级 |
|---|---|---|
| F-V1 | 列表搜索/Pill/筛选/刷新/空态 | P0 |
| F-V2 | 列表↔地图双视图（地图 PRD） | P0 |
| F-V3 | 车辆详情 + 四宫格车控 | P0 |
| F-V4 | 新增 / 编辑车辆 | P0 |
| F-V5 | 车辆位置详情（三钮+底栏导航） | P0（已原型） |
| F-V6 | 历史轨迹 + 时间滚轮 | P0（已原型） |
| F-V7 | 告警「查看车辆」落入详情 | P0 |
| F-V8 | 轨迹起止合法性校验 | P1 |
| F-V9 | 位置页与列表地图视野打通（从告警/订单直达选中） | P2 |

---

## 9. 页面说明（关键交互）

### 9.1 车辆位置详情

- 全屏地图 + 顶栏返回 + VIN 标题  
- 右侧悬浮三圆钮，当前聚焦态可用描边强调  
- 鸣铃中按钮可进入 loading/强调色  
- 底栏贴底安全区，导航钮橙色实心  

### 9.2 车辆轨迹

- 时间栏双控件并列，「—」分隔  
- 时间 sheet 圆角顶、左取消右确定、中部五列滚轮  
- 主页面地图区域在弹窗时遮罩变暗  
- **无**底部距离/车牌信息卡  

### 9.3 与地图列表底卡差异

列表地图选中底卡：VIN、状态、电量、位置短文案、最后信号时间、进详情。  
轨迹页：**不使用**该类底卡。

---

## 10. 验收清单

### 列表 / 详情 / 车控

- [ ] 搜索、Pill、筛选组合正确  
- [ ] 最后信号时间文案统一  
- [ ] 长按才开关锁；寻车有反馈  
- [ ] 设防/报失有确认弹窗  

### 地图

- [ ] 以 `车辆地图视图-PRD.md` §11 为准  

### 位置详情

- [ ] 标题为 VIN  
- [ ] 三钮：自身 / 车辆 / 鸣铃 行为正确  
- [ ] 底栏地址 + 距您 + 步行 + 橙色导航  
- [ ] 返回详情  

### 轨迹

- [ ] 起止时间可改并刷新轨迹  
- [ ] 起/终/折线/POI 可见  
- [ ] **无**底部通栏卡、无车牌/「N 个定位点」类文案  
- [ ] 取消时间不改；确定回填  

### 新增 / 编辑

- [ ] 灰底 + 白卡 `.form-card` + 可编辑 `.login-field`（方案 A）  
- [ ] 新增：未选车型时 VIN/中控不可填；三者齐全可提交  
- [ ] 编辑：VIN/状态只读；可改中控、车型、车牌  

---

## 11. 版本规划

| 阶段 | 内容 |
|---|---|
| **P0（当前原型）** | 列表+地图+详情车控+新增编辑+位置详情+轨迹时段 |
| **P1** | 真地图 SDK；轨迹校验；详情→地图聚焦；视图偏好持久化 |
| **P2** | 聚合；告警/订单深链地图选中；围栏/回放增强 |

---

## 12. 与专项 PRD 的关系

| 文档 | 关系 |
|---|---|
| `车辆地图视图-PRD.md` | 列表地图视图权威；本文不重复打点细则 |
| `告警管理-PRD.md` | 「查看车辆」「一键寻车」落到本模块详情/车控 |
| `首页-PRD.md` | 在线/离线钻取入口 |

> 注：地图 PRD 初版写「详情查看车辆位置为二期」——**以当前原型为准，位置详情与轨迹已纳入本期车辆能力**；地图 PRD 该条视为已落地，评审时以本文 §7.4–7.5 为准。

---

## 13. 待决问题

| # | 问题 | 倾向 |
|---|---|---|
| 1 | 起始 > 结束时如何处理？ | 确定时 toast 拦截 |
| 2 | 轨迹默认时段？ | 最近 20 分钟或最近一次行程（需业务定） |
| 3 | 位置页用户点是否默认展示？ | 原型默认展示用户点 + 与车辆连线；侧钮「自身定位」仅切换视角焦点 |
| 4 | 导航调起高德/苹果/系统面板？ | 跟系统分享 sheet，不绑死一家 |

---

## 14. Next step

1. 冻结位置页 / 轨迹页与地图列表的边界表（§6.4）。  
2. 研发拆：车辆域列表详情、位置服务、轨迹查询、车控指令。  
3. 设计补：无定位进位置页、轨迹超长、权限失败。  
4. 同步修订地图 PRD「二期位置」表述为已交付（可选文档卫生）。  

---

*文档结束。*
